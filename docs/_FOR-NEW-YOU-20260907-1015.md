# FOR NEW YOU — 2026-09-07 (suffix 1015) — Blocs 1, 2 i 3 del PLA FETS i commitejats. Sessió nova per al BLOC 4: numeració i imatges, mai mesurats

**Escrit:** 2026-09-07, en tancar el bloc 3 (Josep: «fem un handoff amb suffix 20260907-1015, per continuar amb bloc 4 en una sessió
nova amb tota la finestra de context disponible»). Substitueix `_FOR-NEW-YOU-20260907-1500.md` (bloc 3 tancat) i `-1230` (bloc 2);
conserva'ls. Branca `experiment/nivell-a-2026-08`, in-place a `g3dt-prod/` (estat esperat; `production/g3dt-eva-v1` @ `c46bc69` sense
push, mai proposar pull/merge a l'Eva). **Arbre net** (només `reference-material/4001607 LINYOLA/validation/photo_selection.json`
sense versionar, que ja hi era).

## On som (una frase per bloc)

- **Bloc 1** (matí): càlcul i taules dels 4 projectes sense sondeig (P5 geometria del tall llegit, P6 un sol classificador, CTE T-1,
  rang de taules per TAULES, data de signatura). Commits `6d2d79b` · `eb1c2a4` · `375b7c9` · `b4e0286`.
- **Bloc 2** (migdia): la cua del grup A era FORMAT (plantes, honorífic «SR./SRA.», article del tipus d'edificació, «1.284»,
  «+188.20», «SPT-1», municipi del padró) i 4 forats de la plantilla porten la preposició (`{{ client_de }}`, `{{ building_type_de }}`,
  `{{ municipality_de }}`, `{{ architect_company_de }}`). Commits `bb1e82b` · `33410c5` · `b23a901` · `d8c7698`.
- **Bloc 3** (tarda): l'extractor de referència arreglat per clau (bucle SPT, fila portant pel que DIU el signat → `bearing_layer_idx`,
  taules per etiqueta + assignació global, capçaleres ES, àncora amb forat) i les 7 veritats re-extretes a TOTES les claus. Commits
  `229aef6` · `f3e977c` · `953eec4` · docs i runs. Descobert: Vilanova recolza els pous al 2n nivell (Df de M341 0,3 → 1,1).

**Mesura de referència (la que has de batre): `docs/wizard-headless/mesures/runs/2026-09-07-m341-bloc3b`** (M341, `viaA`, 7 projectes)
i `2026-09-07-informe-bloc2b` (12 informes; `-bloc3-veritats` és idèntic). `viaA`: escalars+narrativa **234 · 43 · 98 · 33 → 74 %**
(A 78 %, calc 77 %, narrativa 61 %, resta 87 %), **taules 292 · 99 · 88 → 82 %**. Per projecte: Castellar 82, Rubí 79, Bell-lloc 87,
Linyola 79, Alcoletge 75, Vilanova 55 (es), Anciles 53 (es). Suite: 31 vermells amb els mateixos NOMS que
`docs/wizard-headless/mesures/suite-vermells-esperats.txt` / 2431 verds / 5 omesos.

Detall de tot el dia: DECISION-LOG 2026-09-07 (tres entrades: bloc 1, migdia, tarda), `docs/REGISTRE-PERDUES-MESURA.md` (#1-#7),
`docs/PREGUNTES-EVA-PENDENTS.md` (22-27), PLA `docs/PLA-QUE-QUEDA-DESPRES-DE-A-B-I-NARRATIVA-2026-09-07.md` (estat als blocs 1-3).

## Bloc 4 — el que faràs (del PLA §Bloc 4: «el que mai s'ha mesurat»)

**Numeració (S).** La plantilla té **22 variables de numeració** (`fig_aerea_num`, `fig_cadastre_num`, `fig_correlation_num`,
`fig_geological_num`, `fig_main_plan_num`, `fig_spt_cullera_num`, `photo_dpsh_num`, `photo_materials_num`, `photo_sondeig_num`,
`section_empentes_num`, `section_estabilitat_num`, `section_excavabilitat_num`, `section_rado_num`, `section_resum_num`,
`section_sismica_num`, `section_sondeig_num`, `section_spt_num`, `table_lab_num`, `table_lab_values_num`, `table_permeability_num`,
`table_seismic_num`, `table_soil_chars_num`). Cap s'ha comptat mai contra els 7 signats: avui és una creença.

- **On es calculen:** `automation/report_generator.py` → `_build_numbering_context()` (l.656) i els blocs condicionals de seccions
  (l.1215-1252: «2.4.2/2.4.3/2.4.4» amb sondeig, «3.5/3.6/3.7/3.8» amb excavabilitat…). La peça 3 del 2026-09-06 va arreglar el
  «+2» de les fotos; la resta no s'ha verificat.
- **On són a la plantilla:** peus d'imatge i de taula amb text fix al voltant («Taula {{ table_lab_num }}. Resum dels assaigs de
  laboratori realitzats.», «Fotografia {{ photo_dpsh_num }}. Vista de la màquina…», «Figura {{ fig_cadastre_num }} i Figura
  {{ fig_aerea_num }}. Detall de la ubicació…»), i capçaleres de secció dins `{%p if %}` («{{ section_empentes_num }}. EMPENTES DE
  TERRES»).
- **Per què la veritat no les té:** `automation/reference_extractor.py` les SALTA a propòsit (`SKIP_PREFIXES = ("fig_", "photo_",
  "section_", "taula_")`, `SKIP_SUFFIXES = ("_image", "_num")`, `_should_skip_variable`). Pla: deixar de saltar `_num` (mantenir
  `_image`) i comprovar que `_extract_single_var` les talla bé pel prefix («Taula », «Fotografia ») i el sufix («. Resum…»); el peu
  amb dos forats («Figura X i Figura Y. …») va per `_extract_multi_vars` (estàtics entre forats: « i Figura » → funciona); les
  capçaleres de secció tenen prefix buit i sufix llarg («. EMPENTES DE TERRES») → `_extract_single_var` amb sufix.
- **Com es puntuen a M341:** `mesura_341.group_of` ja envia `*_num` al grup **`fix`**; `compare_scalars` compara qualsevol clau de la
  veritat que no sigui taula → text exacte («5» = «5», «2.4.2» = «2.4.2») via `CPE.status_for` (`_norm_text`). Res més a tocar a la
  mesura, llevat de mirar que l'agregat imprimeixi el grup `fix` (avui surt com a grup buit).
- **Disciplina (la de sempre):** (1) `refresh_eva_narrativa.py --all-keys` en dry-run → només han de moure `*_num`; (2)
  `--keys all --apply`; (3) `mesura_341.py 2026-09-<dd>-m341-bloc4-num` i diff cel·la a cel·la contra `-bloc3b` (procediment de sota);
  (4) cada X de numeració o és un bug del generador (arreglar i tornar a mesurar) o és una incoherència del signat (registre de
  pèrdues, append-only). Els signats es llegeixen amb `soffice --headless --convert-to txt:Text --outdir <dir> <fitxer.doc>`
  (fitxer del signat: clau `source_file` de cada `eva_reference_values.json`, o `RE.KNOWN_REPORTS`).

**Imatges (M la primera vegada).** Cinc figures per informe (plànol de situació, ortofoto, mapa geològic, tall de correlació,
cullera SPT) més 3-4 fotos; cap veritat comparable per text. Proposta del PLA: un full de control visual (7 projectes × 5 figures:
hi és / és la correcta / retall i mida) fet UNA vegada a mà a partir dels `.docx` de `~/g3dt-e2e/informes-mesura/2026-09-07-m341-bloc3b/`
(un `.docx` per projecte i variant), i després un test de presència a M341 (la imatge existeix i no és el `PLACEHOLDER`). Les fonts
de cada figura: memòria `report_figure_sources` (Eva, 2026-03-14). Abans de proposar-li res a l'Eva, llegeix la memòria
`feedback_no_pull_eva_success_criterion`.

**Què NO fer al bloc 4:** no tocar la narrativa, els criteris de càlcul ni la plantilla per pujar el número; no re-extreure veritats
sense el dry-run; no tornar la Df de Vilanova a 0,3; no «arreglar» p60 (forats adjacents `{{ architect_name_upper }}{{
architect_company_de }}`: pregunta 26 abans).

## Preguntes obertes a l'Eva (desbloquegen cel·les; `docs/PREGUNTES-EVA-PENDENTS.md`)

22 Alcoletge plantes · 23 Linyola «Taula 3, 4 i 5» · 24 Rubí assentament 1,50 amb Qa 3,5 · 25 Alcoletge Qa 3,5 i SPT al 2n nivell ·
26 frase dels antecedents (3 formes; decisió del Josep: inferir-la dels correus/pressupostos amb LLM, MÉS ENDAVANT) i nom de la
portada · 27 Vilanova SPT P-1/P-3 invertits i construïda total d'Anciles. Les 1-21 anteriors segueixen obertes (N30, cotes relatives,
P4, superfícies de Vilanova, nivell de la mostra, criteris E/c, adjacents, estat del solar, fotos, castellà…).

## Procediments

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"; git branch --show-current; git status --short | grep -v runs/; git log --oneline -6
PYTHONPATH=$PWD .venv/bin/python -m pytest tests/test_bloc3_extractor.py tests/test_reference_extractor.py tests/test_bloc2_format_a.py tests/test_bloc1_calc_taules.py -q --no-header -p no:cacheprovider
PYTHONPATH=$PWD .venv/bin/python docs/wizard-headless/mesures/refresh_eva_narrativa.py --all-keys        # dry-run; avui: 0 diferències
PYTHONPATH=$PWD .venv/bin/python docs/wizard-headless/mesures/refresh_eva_narrativa.py --keys all --apply
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_341.py 2026-09-<dd>-m341-<motiu>
M=docs/wizard-headless/mesures/runs; for s in castellar rubi bell-lloc linyola alcoletge vilanova anciles; do diff <(grep '^- ' $M/2026-09-07-m341-bloc3b/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') <(grep '^- ' $M/2026-09-<dd>-m341-<motiu>/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') | grep '^[<>]' | sed "s/^/$s: /"; done
grep -E "^\| \*\*Total|^\| \`viaA\` \| (A|calc|narr|resta|fix) " $M/2026-09-<dd>-m341-<motiu>/_AGREGAT-341.md
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_informe.py 2026-09-<dd>-informe-<motiu>
for d in $(cd $M/2026-09-07-informe-bloc2b && find . -name '_compare_informe.txt'); do diff -q $M/2026-09-07-informe-bloc2b/$d $M/2026-09-<dd>-informe-<motiu>/$d >/dev/null || { echo "DIFF $d"; diff $M/2026-09-07-informe-bloc2b/$d $M/2026-09-<dd>-informe-<motiu>/$d | grep '^[<>]' | head -6; }; done
PYTHONPATH=$PWD .venv/bin/python -m pytest tests -q --no-header -p no:cacheprovider > /tmp/suite.txt 2>&1; diff <(grep '^FAILED' /tmp/suite.txt | sed 's/ - .*//' | sort) <(grep -v '^#' docs/wizard-headless/mesures/suite-vermells-esperats.txt | sort); rm -f docs/diagnostics/ai_pipeline_demo-project_$(date +%Y%m%d).md
```

Gotchas: el hook `rtk` reescriu `grep`/`git show`/`git diff` i pot buidar pipelines → `rtk proxy grep …` / `rtk proxy git …`. Les
mesures (≈ 3-4 min) i la suite (≈ 4-5 min) es poden llançar alhora en segon pla (directoris de run i de `.docx` independents).
Cost del dia: 0 crides LLM (tot determinista); cada bloc s'ha mesurat abans i després amb el codi quiet.

## Seqüència d'obertura

1. `git branch --show-current` (una línia al Josep), `git status`, `git log --oneline -6` (docs del bloc 3 al capdamunt, després
   `953eec4`, `f3e977c`, `229aef6`, `d8c7698`).
2. PLA §Bloc 4 i DECISION-LOG 2026-09-07 (tarda) § Limitacions.
3. Numeració primer (S): extractor → dry-run → apply → mesura → arreglar el generador on toqui → DECISION-LOG del bloc 4.
   Imatges després (M): full de control + test de presència.
