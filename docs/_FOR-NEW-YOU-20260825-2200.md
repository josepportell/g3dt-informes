# FOR NEW YOU — 2026-08-25 22:00 — Fase 12 FETA: consolidació Python-first (0,04 s, 0 erroni de fons, 0 pèrdues), LLM només per a conflictes reals (`--only-fields`, provat a Bell-lloc); següent: normalitzadors del comparador d'or, Fase 11, 13-15

**Escrit:** 2026-08-25, 22:00 · **Per a:** la sessió següent. **Llegeix abans:** `docs/_FOR-NEW-YOU-20260825-2025.md` (marc fixat pel Josep, §2-§4: tot vigent).
**Recap:** el Josep ha dit "arrenca la Fase 12". S'ha construït `automation/lectura/consolidate.py` (consolidació determinista amb
guards del Pas 3/3b), un harness d'acceptació sobre els 5 jocs de perdoc ja llegits, la integració al runner (`G3DT_LECTURA_CONSOLIDA=auto`
per defecte), el mode `--only-fields` al skill (v1.5), i s'ha exercitat la crida LLM real a Bell-lloc. Commits sobre `c4b7cf4`: `879bf7b`
(codi+tests+skill+harness) i el de docs d'aquest handoff.

## 1. Ordre de lectura (15 min)
1. Aquest document. 2. `docs/wizard-headless/fase12-consolida/_RESULTATS.md` (taula d'acceptació, regles codificades, run real, Bell-lloc).
3. `docs/DECISION-LOG.md` entrada «2026-08-25 (nit)». 4. `automation/lectura/consolidate.py` docstring de capçalera + `decide()`.
5. `.claude/commands/g3dt-llegir-projecte.md` v1.5: Pas 5b «Mode `--only-fields`». 6. `mesures/LEDGER.md` fila `2026-08-25-opus48-docs-python-consolida`.

## 2. Estat
- **Fase 12 acceptada segons l'annex §7.2/§11:** 0 erroni-amb-confiança de fons als 5 jocs (Sonnet v2 / Fable / Opus 4.8 / Sonnet v1.3 a
  Castellar; Bell-lloc v1.2); 0 cel·les per sota de Fable, `consolida2` i Opus (cel·la a cel·la); consolidació 0,02-0,04 s (abans 450-680 s).
- **Runner:** `auto` (defecte) = Python + crida `--only-fields` només si `conflicts[]` no és buit; `python` = mai LLM; `llm` = via antiga
  (`--consolida` sencera, per mesurar/pla B). El runner escriu `_decisions.json` sempre (també degradat). Tests: suite lectura 193 passed.
- **Crida real `--only-fields` (Bell-lloc, `street_address` cantonada):** 228 s / 18 turns / 20k tokens; el skill fa exactament el Pas 5b,
  cap invenció, fusió correcta; però només confirma l'ordre de Python. n=1. Si es repeteix a 2-3 projectes més → defecte `python`.
- Castellar amb els 3 models: 0 conflictes → 0 crides LLM.

## 3. Regles interpretatives noves (a més de les del handoff anterior)
- **"Cel·la per sota" es mesura contra la consolidació LLM del MATEIX joc de perdoc** (harness): és l'única comparació justa, perquè separa
  lectura de consolidació. Els ERR que queden són de format (`-4 m`/`-4,0 m`, `1,0 - 1,2 m`/`-1,00 a -1,20 m`) o de fixture (`sondeig cota`
  de Castellar anterior a la regla del sistema de cotes; `de_a_estat`).
- **A = confiança del skill** (claude ≥ 0,8; g3 ≥ 0,9; Python), no una taula per tipus de document — la confiança ja codifica la posició al
  document (Pas 3). Excepció: cel·les de taula (Pas 3b: autoritat per tipus de document, `_CELL_A_DOC_TYPES`).
- **Contradicció:** camps → qualsevol senyal ≥ 0,4 (client_name 0,6); taules → només documents A de la cel·la. Conflicte LLM → només
  A-vs-A de documents DIFERENTS.
- **Derivats permesos = els que el Pas 3 prescriu**, sempre `font` que comença per `(derivat`/`(coneixement previ`/`(practica Eva`, mai `segur`.
- `candidates` ≤ 3 (contracte) + `altres` (tot el que queda, amb font i cita) + `descartats` (NOT_*) + `extra_concepts` (fora de les 22 claus).
  Test automàtic: cap `tier_a` es perd (7 jocs del llibre).

## 4. Supòsits que aguanten el pes (nous)
| On | Què | No desfer |
|---|---|---|
| `consolidate.py::value_key` | dates parcials, numèrics (`abs` per fondàries), text prefix/sufix, MAI contenció interior | la contenció interior fusionava "entre el carrer X i el carrer Y" amb X (cantonada → segur fals) |
| `consolidate.py::decide` | `segur` només si totes les formes A tenen la mateixa clau exacta; si no, `candidats` amb totes les formes | evita "segur" sobre una forma parcial de l'adreça |
| `_cell_signals` | files no-A de taula: conf 0,3 per defecte → candidats, no bloquegen | amb 0,6, el tall gràfic (`≈`) i el manuscrit deixaven 13-16 cel·les per sota |
| `_group_spt_rows` | fusió de files SPT per punt + solapament d'interval (≤ 15 cm) | el tall `≈0,8-1,3` feia fila pròpia i quedava primera |
| `_decide_sondeig_cota` | sistema relatiu només per cotes DPSH A; relativa primer forçada després de `decide()` | `decide` reordena per pes (l'absoluta és A) |
| `_promote_entre_carrers` | frase "entre el carrer X i el carrer Y" dels annexos → candidat 1 | regla Pas 3 (redacció de l'informe) |
| `runner._consolidate_python_first` | escriu `_decisions.json`; `_consolida_only.json` s'esborra abans de la crida; fusió només si el conjunt passa el contracte | el skill ja no escriu `_decisions.json` en producció |
| `normalize.wrap_flat_cells` | només a la via LLM (`soft_normalize`); identificadors de fila queden plans | Python no hi passa; no dobla embolcalls |
| `tests/test_lectura_runner.py::base_env` | `G3DT_LECTURA_CONSOLIDA=llm` per als tests històrics | proven la via LLM; els tests Fase 12 fixen `auto`/`python` |

## 5. Procediments
```bash
.venv/bin/python docs/wizard-headless/fase12-consolida/harness.py [--write DIR] [--only LABEL]   # acceptació, 0 LLM, 1 s
# Consolidació sola in situ (docs en cache): esborra _decisions.json i run_lectura(force=False) — driver a l'scratchpad de la sessió
G3DT_LECTURA_CONSOLIDA=python|auto|llm   # runner; run.sh ja ho registra a START (també effort=)
# Depurar una cel·la: consolidate.load_corpus + collect_field_signals (vegeu dbg.py de la sessió: signals amb A/conf/key per camp)
```

## 6. Què NO fer
- No tornar a `llm` com a defecte "perquè el LLM raona més": el llibre i el harness diuen que perd senyals i inventa; Python no.
- No afegir regles semàntiques "a ull" al consolidador: cada regla nova ha de sortir del Pas 3/3b del skill (i idealment d'un cas de l'or).
- No editar els fixtures d'or per fer quadrar el comparador; anotar-ho (`_RESULTATS.md` §1) i resoldre-ho amb normalitzadors o revisió explícita.
- Vigents: no pull/merge a l'Eva; no `git stash`; no `docs/diagnostics/ai_pipeline_demo-*.md` ni `photo_selection.json` als commits.

## 7. Tasques obertes (ordre)
1. **Normalitzadors del comparador d'or** (`compare_consolida.py`): dates, signes/decimals, formes d'adreça → els ERR/ALERTA de format
   deixen de fer soroll; regenerar `escalars.txt`/`taules.txt` dels 8 runs i el llibre (totals canviaran: anotar-ho a `meta.json`).
   Revisar 2 fixtures: `golden-read-taules/3001621…` `sondeig cota` (regla del sistema de cotes) i `de_a_estat`.
2. Fase 11 (delta-sync) → 13 (`auto_result` + fonts Cadastre/ICGC al consolidador: tanca del tot el forat 1) → 14 (UI tres botons) → 15 → 16 → 17.
3. Bell-lloc complet al llibre (lectura v1.4 + Python) i files d'effort — ara costen només la lectura (la consolidació és gratis).
4. Decisió `auto` vs `python` com a defecte després de 2-3 projectes més amb conflictes (§2).
5. Pendents anteriors: política de neteja `_preext/`, pla de subscripció (Josep), Fase 8b, Tier B, preguntes a l'Eva, objectiu 341 variables.
