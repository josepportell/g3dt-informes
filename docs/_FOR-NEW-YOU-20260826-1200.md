# FOR NEW YOU — 2026-08-26 (migdia) — Fase 8b FETA (les taules llegides arriben al .docx); següent: **Fase 13**, peça 2 del tram 1

**Escrit:** 2026-08-26, migdia · **Per a:** la sessió que continuï el tram 1.
**Recap:** el Josep ha dit «comencem el tram 1» i s'ha fet la peça 1 sencera — **Fase 8b**: `_decisions.json` + les tries de l'Eva ara
arriben al `.docx`. Commit únic `6a36515` sobre `653311b`. Res pujat. Mesurat contra els informes signats: Castellar 54 %→78 %,
Rubí 64 %→82 %, Bell-lloc 76 %→75 % (una cel·la de format). Fases 11, 13-17 NO començades.

## 1. Ordre de lectura (10 min)

1. Aquest document.
2. `docs/wizard-headless/fase8b-taules/_RESULTATS.md` — què fa la 8b, la mesura i les regles de format. Curt.
3. `STATUS.md` (bloc «2026-08-26 — Fase 8b» al capdamunt de la secció del wizard headless).
4. `docs/_TAULA-FASES-VS-EVA-2026-08-25.md` §«Proposta de priorització» — el tram 1 és **8b → 13 → forat capa vegetal → 14a → 15**.
   La 8b ja està; **toca la 13**.
5. `docs/DISSENY-ANNEX-TRES-BOTONS-JOBS-NOTIFICACIONS-2026-08-24.md` **§7.1** (`auto_result` a disc), **§8** (residus Groq),
   **§9** (restriccions) i la fila 13 de §10. És l'especificació de la feina d'avui.
6. `docs/wizard-headless/fase8-e2e/_RESULTATS.md` «Forats trobats» punt 1 (fonts Python invisibles al consolidador) — l'altra
   meitat de la Fase 13 segons la taula de priorització.
7. `docs/PREGUNTES-EVA-PENDENTS.md` (7 obertes; la 7 és nova d'avui).
8. MEMORY.md: `feedback_no_pull_eva_success_criterion`, `feedback_full_report_341_variables_goal`,
   `concept_format_per_concept_priority`, `project_prod_audit_fixes_2026_08`.

## 2. Marc fixat (no re-litigar)

- Objectiu: **l'informe complet a la millor qualitat** (341 variables). El nivell A és el pas 1.
- **Mai** proposar pull/merge/desplegament a l'Eva. Aquest annex és feina de branca.
- Via B (`automation/ai_pipeline/`, `web/vision_groq.py`, `auto_extractor.py`, `web/vision_fast.py`) **PROHIBIT editar-la**.
  La Fase 13 es fa **envoltant** des de `lectura_service`/`wizard_service`, mai dins d'`auto_extractor.py` (§9 del disseny).
- Cap font Python (Cadastre, ICGC, geocode) pot **guanyar** un camp contra la lectura: només omple forats.
- `building_type`: treure «entre mitgeres»/«aïllat»/un article = CLOSE. Ni afluixar-ho més ni endurir-ho.

## 3. Regles interpretatives (què és senyal i què és soroll)

- **L'instrument de la 8b és `scripts/compare_tables_vs_eva.py`** (generat vs informe signat, 11 taules, cel·la a cel·la). Compara
  nombres **numèricament** (`-4,0` = `-4.00`), així que el format decimal no fa soroll; el que compta és el fons.
- **Un delta d'1 cel·la no és una regressió de fons.** El −1 pp de Bell-lloc és `1/0` vs `1/--` a la columna SPT/MA: els 4 sondeigs
  dels informes signats la formaten de 4 maneres diferents (`1/--`, `1/0`, `1/0/0`, `1/0/1`). No hi ha regla derivable → pregunta 7.
- **Els MISMATCH que queden a la mesura de la 8b són de dos tipus, i cap és de taules:** càlculs (E, Qa, φ, K30, sísmica,
  permeabilitat → tram 3) i escalars (superfície de parcel·la, plantes, CTE → part d'això és el que ataca la Fase 13).
- **Compte amb el Cadastre com a "font de veritat":** el diagnòstic 23/08 diu **parcel·la equivocada a 4/8 projectes**. A Castellar
  la via B dona `440` i l'Eva escriu `1.284`. Connectar el Cadastre al consolidador no és automàticament un guany: cal comprovar
  projecte a projecte i, si discrepa amb la lectura, ha de sortir com a **candidat**, mai com a `segur`.
- Estat de partida per camp (real, `fase12-consolida/out/`): Castellar `referencia_catastral` i `superficie_parcela` = `no_trobat`;
  Bell-lloc els té `candidats` des dels documents. `utm_x/y` ja NO és un forat: `consolidate.python_signals()` llegeix
  `COORDENADES*.txt` des de la Fase 12. **No refacis aquesta part del forat 1: ja està feta.** El que falta és Cadastre i ICGC.

## 4. Supòsits que aguanten el pes (Fase 8b; no desfer)

| On | Què | Per què no desfer-ho |
|---|---|---|
| `automation/lectura/tables_report.py` (docstring) | les fondàries `de`/`a` de `soil_levels` viatgen al bloc però **no** toquen `depth_from_m`/`thickness_m` | alimenten gruixos i taula sísmica: canviar-les és càlcul (tram 3), no format de taula |
| `tables_report.resolve_cell` | resol també **llistes de candidats sense embolcall** | dialecte real (Bell-lloc `litologia: [...]`); sense això la llista sencera s'imprimia dins del `.docx` |
| `tables_report.fmt_text` | mai imprimeix un dict/list; cel·la buida abans que un bolcat | xarxa de seguretat per a formes de cel·la noves |
| `tables_report.levels_by_number` | alineació per NÚMERO de nivell, no per índex; la capa vegetal no és el nivell 1 | mateix criteri que `compare_consolida.row_key` v2; per índex, la litologia de la capa vegetal aniria al nivell 1 |
| `SoilLevel.description_verbatim` + `report_generator._level_material` | la litologia llegida **no s'escurça** | `_shorten_material_desc` talla pel primer punt: «Substrat rocós. Bretxes…» quedaria en «Substrat rocós» |
| `report_generator._build_template_context` | emet **sempre** `spt_ma_tests` (una fila amb els escalars de sempre, fins i tot buida) | la plantilla ja és un bucle; sense això la via B perdria la fila |
| `wizard_service._build_lectura_block` | el bloc es **congela** a `user_data.json` en desar | el que Eva ha validat és el que surt; recalcular-ho en generar li mouria l'informe sota els peus a la Fase 11 |
| `report_generator.lectura_tables` | fallback: `user_data` primer, `validation/lectura/_decisions.json` després | permet generar per CLI/arnès sense passar pel wizard (és l'acceptació de la 8b) |
| `automation/wizard.py::save_wizard_data(extra=)` | claus de nivell superior que NO són camps del formulari | `WIZARD_FIELDS` filtra tota la resta; sense `extra` el bloc no arribaria mai al disc |

## 5. Procediments

```bash
git branch --show-current      # → experiment/nivell-a-2026-08 (in-place a g3dt-prod/; esperat)
git log --oneline -3           # ha de començar per 6a36515

# Tests: rapids (8b) i suite sencera (linia base 32 failed CONEGUTS: SmartScan, ai_pipeline, fileminer Anciles)
.venv/bin/python -m pytest tests/test_lectura_tables_report.py -q -p no:cacheprovider          # 49, 0,4 s
.venv/bin/python -m pytest tests/ -q -p no:cacheprovider                                        # ~3,5 min → 1420 passed / 32 failed

# Generar un informe i mesurar-lo contra l'informe signat de l'Eva
.venv/bin/python -m automation.report_generator "reference-material/3001621 CASTELLAR DEL VALLES" \
    -u "reference-material/3001621 CASTELLAR DEL VALLES/_user_data_prev.json" -o /tmp/x.docx
.venv/bin/python scripts/compare_tables_vs_eva.py /tmp/x.docx docs/golden-read-taules/_eva_truth/castellar.json --tag prova

# Amb les taules llegides: injectar el bloc a un user_data de treball (mai dins de reference-material/)
.venv/bin/python -c "
import json; from automation.lectura.tables_report import build_report_tables
ud=json.load(open('reference-material/3001621 CASTELLAR DEL VALLES/_user_data_prev.json'))
ud['lectura_tables']=build_report_tables(json.load(open('docs/wizard-headless/fase12-consolida/out/sonnet-v2-c3/_decisions.json')))
json.dump(ud,open('/tmp/ud.json','w'),ensure_ascii=False)"

# Traduir un _decisions.json a files d'informe (CLI de desenvolupament)
.venv/bin/python -m automation.lectura.tables_report DECISIONS.json [SELECCIONS.json]

# Plantilla: la conversio de la taula SPT/MA en bucle es idempotent
.venv/bin/python scripts/template_spt_ma_loop.py     # "Ja és un bucle — res a fer."
```

- **Projectes amb `user_data` reutilitzable a `reference-material/`: només Castellar, Rubí i Bell-lloc** (`_user_data_prev.json`).
  Linyola, Alcoletge, Vilanova i Anciles no en tenen → no es poden generar per mesurar.
- **Veritats d'Eva:** `docs/golden-read-taules/_eva_truth/*.json` (7). **Or de taules:** `docs/golden-read-taules/*/_tables_decisions.json`
  (dialecte antic → passar per `automation.lectura.contract.adapt_legacy`). Tots dos són **read-only**.
- `rtk` reescriu `grep`/`find`: `find … -not` i patrons amb `\|` fallen → `rtk proxy find …` o patrons simples.
- `unzip` no existeix a la màquina: obrir el `.docx` amb `zipfile`/`python-docx`.
- Fitxers de treball d'avui a l'scratchpad de sessió (`8b/measure.sh`, docx generats): no versionats, reconstruïbles amb el bloc de dalt.

## 6. Seqüència d'obertura suggerida

1. `git branch --show-current` + `git log --oneline -3` (ha de començar per `6a36515`).
2. `.venv/bin/python -m pytest tests/test_lectura_tables_report.py -q -p no:cacheprovider` → 49 verds (confirma que la 8b viu).
3. Llegir §7.1, §8 i §9 del disseny de l'annex. La Fase 13 té **dues meitats** i convé decidir-ne l'ordre amb el Josep:
   (a) `auto_result` a disc — pura latència, sense risc de qualitat; (b) Cadastre/ICGC al consolidador + residus Groq — toca qualitat.
   **Recomanació: (a) primer** (mesurable, sense risc), i (b) després, amb el comparador d'or a sobre.
4. Abans de tocar res de (a): mapa de `web/wizard_service.py` — on es construeix `AutoExtractionResult`, què hi ha a
   `_prefill_cache`/`_auto_result_cache` (dicts en memòria, `wizard_service.py:53-55`) i quins fitxers d'entrada determinen
   `inputs_md5`. Acceptació de la fila 13: **2a càrrega sense xarxa ni Groq → prefills iguals**, i invalidació per `inputs_md5`.
5. Per a (b): decisió del Josep sobre les 4 peces de la taula §8 (probes ConceptScout 78 s, `groq_miner` 10 s, ortofoto, visió per
   tipus). Criteri escrit: si no aporten cap camp que la lectura no tingui, fora.
6. Cada peça: suite sencera (no `| tail -1`), commit per peça, i si toca qualitat, comparador d'or **escalars i taules** abans d'adoptar.
7. Quan la 13 estigui, seguir el tram 1: forat de la capa vegetal a `consolidate.py` → 14a → 15.

## 7. Què NO fer

- No tornar a implementar `utm_x/y` des de `COORDENADES.txt` al consolidador: ja hi és (`consolidate.python_signals`, Fase 12).
- No editar la via B (§9). `auto_result` a disc va **envoltant**, des de `lectura_service`/`wizard_service`.
- No deixar que el Cadastre pugi un camp a `segur` per damunt de la lectura (parcel·la equivocada a 4/8 segons el diagnòstic).
- No fer que el generador «netegi» anotacions dins de `litologia` («… (NIVELL 1, 0.50-1.20)»): això es corregeix al skill/consolidador.
  El generador només escapça anotacions on el que queda davant és verificablement una xifra (`n30`, `superficie_construida`).
- No tocar les fondàries `de`/`a` dels nivells per «millorar la taula»: mouen gruixos i taula sísmica (tram 3).
- No editar `docs/golden-read*` ni `_eva_truth/` per fer quadrar cap mesura.
- No commitejar `docs/diagnostics/ai_pipeline_demo-*.md` (els genera la suite) ni `photo_selection.json`.
- No `git stash` (pila compartida entre worktrees); WIP commit si cal apartar feina.
- No `--model opus` pensant que és 4.8 (`claude-opus-4-8`).

## 8. Tasques obertes (ordre del tram 1)

1. **Fase 13** — (a) `auto_result` a disc amb `inputs_md5` + TTL 30 dies per als HTTP; (b) Cadastre/ICGC al consolidador (forat 1,
   meitat que queda) + decisió sobre els residus Groq (§8). **Aquesta és la feina d'avui.**
2. Forat de la capa vegetal a `consolidate.py` (per què `de`/`a` de la capa surten `no_trobat` al joc `sonnet-v2-c3` quan
   `consolida2` les tenia): `harness.py --only sonnet-v2-c3` + `dbg` de senyals. **Sense** la regla del Pas 3b, que espera la pregunta 3.
3. **14a** (botons Preparar/Obrir + taula d'estat + `attach`) i **15** (toast + correu). *Actualitzar* espera l'11.
4. Tram 2: 11 + 14b → 16 (mesures) → 17 preparat en sec al Windows del Josep.
5. Tram 3 (bloquejat per l'Eva): càlculs v4 → regla `de` nivell 1 → multi-informe → narrativa → fotos.
6. Pendents menors: `consolidate.value_key("1,5-1,75")` → data (latent); Bell-lloc complet al llibre; hold-out amb carpetes noves de l'Eva.
7. Decisions del Josep encara obertes des del 25: enviar o no les preguntes/carpetes a l'Eva en vacances; pla/model (Fable @xhigh
   o Opus 4.8 @high).

## 9. Guanys ocults de la sessió

- **Hi havia una via per la qual una estructura de dades crua podia acabar impresa dins d'un informe signat** (`[{'value': …,
  'font': …}, …]` a la cel·la de litologia). Era latent des que existeix el dialecte de llistes; ha aparegut només perquè s'ha
  mesurat generant de veritat. Ara hi ha dues barreres (`resolve_cell` i `fmt_text`).
- **Tres bugs de la UI de la lectura que ningú havia vist perquè ningú havia mirat la pestanya amb dades reals a la mà**: la
  columna d'identificador de *totes* les taules sortia `—`, la de «Prof. Extracció» sempre buida, i els camps de laboratori no
  arribaven mai al document. Els tres afecten justament el que l'Eva hauria de revisar abans de generar.
- **Ara hi ha una mètrica de "quant s'assembla l'informe al seu"** que es pot recórrer en 30 s per projecte
  (`compare_tables_vs_eva.py`), no només una de "quant s'assembla la lectura a l'or". Serveix per a la taula abans/després que
  vol el Josep i per decidir si una fase val la pena abans de construir-la.
- La mesura deixa a la vista, ordenats, quins forats queden i de quin tipus són: pràcticament tot el que falta a les taules ja no
  és lectura, és **càlcul** (tram 3) i **escalars de fonts HTTP** (Fase 13).
