# FOR NEW YOU — 2026-09-07 (suffix 2000) — IMATGES: peces 0, 1, 2 i 3 FETES i commitejades. Ara toca la PEÇA 4 (figura dels assaigs)

**Escrit:** 2026-09-07 (nit), en acabar la peça 3 i l'inventari de pendents, a petició del Josep.
**Per a:** la sessió que farà la peça 4 i les que queden.
**Recap d'una frase:** de les 56 figures i fotos que l'Eva posa als 7 informes signats en clavem **17 exactes i 3 de la
mateixa font (47 %)**; les cinc ranures de FOTOS ja estan fetes i les quatre de FIGURES compostes o retallades són el
que queda.

Branca `experiment/nivell-a-2026-08`, in-place a `g3dt-prod/`. Arbre net, últim commit `f491422`.
`production/g3dt-eva-v1` @ `123b4f2` (sense push). Mai proposar pull ni merge a l'Eva (`feedback_no_pull_eva_success_criterion`).

## 1. Ordre de lectura

1. Aquest document.
2. **`docs/imatges/PENDENTS-IMATGES.md`** — l'inventari complet: estat per ranura, les 4 peces amb què necessita
   cadascuna, 6 pendents que no són cap peça, 4 bloqueigs de fons, 9 preguntes obertes i l'ordre recomanat. **És el
   document de referència del que queda.**
3. DECISION-LOG, les 6 entrades d'avui: `2026-09-07 (tarda-3)` (pas 2, decisions D0-D14 per tipus de figura),
   `(tarda-4)` (peça 0, la mesura), `(tarda-5)` (peça 1, plantilla), `(vespre)` (peça 2, lector de fotos), `(nit)`
   (peça 3, retall del tall), `(nit, 2)` i `(nit, 3)` (el senyal de la caixa de mocadors).
4. `docs/imatges/INVENTARI-I-VERITAT-2026-09-07.md` §6 (què fa l'Eva per tipus) i §4 (per figura). Els 7 fulls
   `docs/imatges/fulls/*.jpg` si cal mirar.
5. Memòries: `project_imatges_peca3_2026-09-07`, `..._peca2_...`, `..._peca1_...`, `..._peca0_...`, `..._pas2_...`,
   `..._pas1_...`, `feedback_measure_baseline_before_coding`.

## 2. On som (run de referència `2026-09-07-m341-peca3`)

| ranura de l'Eva | M | C | X | ND | |
|---|--:|--:|--:|--:|---|
| `foto_materials` | 7 | 0 | 0 | 2 | ✅ |
| `fig_tall` | 3 | 3 | 1 | 0 | ✅ |
| `foto_sondeig` | 2 | 0 | 1 | 0 | ✅ (1 empat) |
| `foto_vista` | 2 | 0 | 1 | 1 | ✅ (1 empat) |
| `foto_dpsh` | 3 | 0 | 4 | 0 | ✅ (4 empats) |
| `fig_situacio` | 0 | 0 | 7 | 1 | ⏳ peça 5 |
| `fig_assaigs` | 0 | 0 | 3 | 3 | ⏳ **peça 4** |
| `fig_geologic` | 0 | 0 | 4 | 3 | ⏳ peça 6 |
| `fig_projecte` | 0 | 0 | 2 | 3 | ⏳ peça 7 |
| **total** | **17** | **3** | **23** | **13** | **47 %** |

Escalars 74 %, taules 82 %, grup `fix` 74 M · 25 X: **cap peça d'imatges els ha mogut** (llevat d'una cel·la de
narrativa a la peça 2), i el `fix` només el mourà la peça 7.

## 3. Ordre recomanat (i per què)

1. **Peça 4 — figura dels assaigs.** 6 forats, és la figura del capítol 2.2 i **la font existeix als 7 projectes**:
   l'annex «plànol de situació» de l'Eva (`PDF/ANNEXES/*plànol de situació.pdf`, `pl situació.pdf`, `.FH11`), i a
   Castellar, Rubí i Vilanova també com a PNG a `ALTRES` (`m8.png`, `F2 UBI PUNTS.png`, `F2 PUNTS.png`).
2. **Retall de PNG** (bbox de píxels no blancs). És petit i el necessiten la peça 5 i el tall de Rubí, que avui agafa
   `F5 TALL.png` sense retallar i per això queda en X.
3. **Peça 6 — mapa geològic.** 3 dels 7 es resolen amb el PNG `*MGEOL*` sense tocar res més; els altres, amb les UTM
   (vegeu §4).
4. **Peça 5 — situació.** 8 forats, la més llarga; depèn del retall de PNG i, en part, de les UTM.
5. **Peça 7 — figures del projecte + bloc de figures variable a la plantilla.** És **l'única que mou la numeració**
   (10 de les 25 X del grup `fix` són «nombre de figures del projecte»).
6. Pendents de plantilla: foto de materials per punt, i el text fix de Rubí «unitat NMgo» que s'imprimeix a tots els
   projectes dins el bucle de nivells (mateixa família de risc que el pastís, ja tret).

## 4. Dues indicacions del Josep (2026-09-07, nit) — llegir abans de la peça 5 i la 6

### 4.1 Les coordenades: derivar-les de la referència cadastral que ja llegim

El Josep: «hem resolt molt bé trobar la parcel·la anteriorment, així que podem descarregar la foto que calgui quan ja
som a ICGC». **Té raó i està a tocar.** Avui la cadena va al revés (UTM → Cadastre) i per això 3 projectes queden sense
capes ICGC. Però la lectura ja dona la referència:

| projecte | `cadastral_refs` llegit | es pot derivar UTM? |
|---|---|---|
| vilanova | `8606709CG9280N` | **sí** — 14 caràcters, directe |
| anciles | `6184504BH9158N0000SS` | **sí** — `rc14 = ref[:14]` |
| rubi | `Polígon 6, Parcel·la 105-B` | rústica sense RC: cal `cadastre_reader.resolve_portal` amb l'adreça llegida |

La peça que falta ja existeix: **`automation/cadastre_adjacents.get_parcel_geometry_utm(rc14)`** retorna el polígon de
la parcel·la en EPSG:25831 (WFS INSPIRE del Cadastre). Amb el **centroide del polígon** ja tens `utm_x`/`utm_y` i
`_download_icgc_images` funciona sense tocar res més. Fer-ho com una font de coordenades nova, per darrere de
`COORDENADES.txt` i de la geocodificació (`geocode_coordinates.py` és **via B de producció: es pot llegir, no tocar**),
i marcar-la com a derivada perquè es vegi al wizard. Desbloqueja el geològic de Vilanova i Anciles i l'opció ICGC de la
situació.

### 4.2 La imatge de l'ICGC que l'Eva ja va aprovar: reproduir-la EXACTAMENT

El Josep: «el pipeline determinista crea una imatge des de ICGC molt concreta que estaria bé crear exactament igual
(l'Eva la va aprovar)». **És `automation/icgc_geology.get_geological_map_with_terrain`** i el generador ja la crida
(`image_manager.py` ~926, clau `geological_composite_*` a la cau). La recepta exacta, per no desviar-se'n:

- base topogràfica ICGC + capa `unitats-geologiques-50000` **transparent**, composades amb `alpha_composite`
  (no `blend`: conserva les etiquetes de les unitats), **opacitat de la capa geològica 0,65**;
- `buffer_m = 700`, `width = 800`, `height = 600`, `SRS = EPSG:25831`;
- **punt vermell al centre**: `ellipse` de radi 10 px, farcit `red`, contorn `darkred` de 2 px.

És exactament el tipus de figura que l'Eva té a Linyola, Bell-lloc i Alcoletge. **No canviar cap d'aquests paràmetres**
a la peça 6: el forat d'aquells 3 projectes no és la recepta, és que no tenim UTM. Per a Rubí, Castellar i Vilanova
l'Eva fa servir el seu PNG amb llegenda composta (per això el PNG va primer), i Anciles és Aragó (IGME).

## 5. Com es treballa cada peça (el ritme que hem seguit tot el dia)

1. **Baseline amb el codi quiet.** Comparar sempre contra `2026-09-07-m341-peca3` (`feedback_measure_baseline_before_coding`).
2. Canviar **una sola cosa** i mesurar-la sola.
3. `diff` de les cel·les d'escalars per projecte + secció «MATEIXA FONT» de l'agregat + taules. Si es mou res fora de
   la ranura que toques, entendre per què abans de continuar.
4. Pèrdues honestes al `docs/REGISTRE-PERDUES-MESURA.md`; entrada al DECISION-LOG amb el «per què» i les alternatives.
5. Suite sencera: 31 vermells amb els **mateixos noms** que `suite-vermells-esperats.txt` (`feedback_compare_test_names_not_counts`).

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"; git branch --show-current; git status --short; git log --oneline -3
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_341.py 2026-09-<dd>-m341-<motiu>
M=docs/wizard-headless/mesures/runs; A=2026-09-07-m341-peca3; B=2026-09-<dd>-m341-<motiu>
for s in castellar rubi bell-lloc linyola alcoletge vilanova anciles; do diff <(grep '^- ' $M/$A/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') <(grep '^- ' $M/$B/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') | grep '^[<>]' | sed "s/^/$s: /"; done
sed -n '/^## Imatges (viaA): MATEIXA/,/^Llindars/p' $M/$B/_AGREGAT-341.md | grep '^|'
# re-puntuar imatges sense regenerar: PYTHONPATH=$PWD .venv/bin/python docs/wizard-headless/mesures/imatges_font.py --remeasure $M/$B
# lector de fotos (per lots: el segon pla talla als 10 min): docs/wizard-headless/mesures/llegir_fotos_corpus.py <run> --projects a,b
PYTHONPATH=$PWD .venv/bin/python -m pytest tests -q --no-header -p no:cacheprovider > /tmp/suite.txt 2>&1; diff <(grep '^FAILED' /tmp/suite.txt | sed 's/ - .*//' | sort) <(grep -v '^#' docs/wizard-headless/mesures/suite-vermells-esperats.txt | sort)
```

## 6. Eines que ja tens (no les refacis)

| Eina | Què fa | On |
|---|---|---|
| Mesura per figura | M/C/X/ND «mateixa font que l'Eva», assignació un a un, sobrants, `--remeasure` | `docs/wizard-headless/mesures/imatges_font.py` |
| Lector de fotos | inventari + annex de l'Eva + exemplars leave-one-out + `claude -p` amb skill | `automation/imatges/lector_fotos.py` + `.claude/commands/g3dt-llegir-fotos.md` |
| Retall de dibuix (PDF vectorial) | nucli d'estrats → finestra → creixement fins al blanc | `automation/imatges/retall.py` |
| Veritat dels signats | 63 figures/fotos amb peu, ranura i procedència | `docs/imatges/veritat/` + `~/g3dt-e2e/imatges/veritat/` |
| Aparellament calibrat | phash + NCC multiescala + PSR | `docs/imatges/scripts/match.py` (pas 1) |

## 7. Què NO fer

- No dibuixar mai els punts d'assaig nosaltres (decisió D3 del pas 2): la font és el dibuix que l'Eva ja té.
- No tocar `automation/geocode_coordinates.py` (via B de producció) ni els paràmetres de
  `get_geological_map_with_terrain` (§4.2).
- No reintroduir `_render_situation_plan_left` (el retall del 38 % esquerre; no coincideix amb cap signat).
- No canviar el **nombre de taules** de la plantilla sense passar abans `refresh_eva_narrativa.py --all-keys` en sec:
  l'extractor de referència aparella les taules per ordre (gotcha de la peça 1).
- El `.docx` s'edita amb lxml, mai amb expressions regulars. A PyMuPDF, `box |= r` ignora els rectangles degenerats.
- No enviar cap pregunta a l'Eva sense el Josep.

## 8. Tasques obertes

- [ ] **Peça 4** (assaigs), i després l'ordre de §3.
- [ ] UTM des de la referència cadastral (§4.1) — desbloqueja la 6 i part de la 5.
- [ ] Preguntes 30-38 i 19a a l'Eva (la 39 ja té resposta: caixa blava de mocadors = sondeig).
- [ ] Bloc 5 del PLA (castellà) continua a la cua, fora d'imatges.
