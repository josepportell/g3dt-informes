# FOR NEW YOU — 2026-08-23 20:15 — Lectura d'or 8/8 feta (0 ERR reexecutable); toca headless, DWG, Fase 4a i UI de candidats

**Escrit:** 2026-08-23, 20:15 · **Per a:** la sessió que continua la via A amb els pendents.
**Recap de la sessió:** s'ha executat la lectura d'or dels 8 projectes amb el procediment del futur skill de producció
(`.claude/commands/g3dt-llegir-projecte.md`, v0.1→v0.4 durant la lectura; 0 $ d'API): 163 JSON per font + 8 `_decisions.json`
amb comparació contra l'Eva a `docs/golden-read/`. Resultat (ANALISI §11): **120 cel·les = 80 OK / 17 CAND / 10 NT / 11 n/a /
2 ERR**; OK 80,8 % ✓, CAND 17,2 % ✓, ERR 2 ✗ → tots dos convertits en regla al skill (reexecució esperada: 0). El Josep ha
aportat el "Pas 0" (context del document abans de llegir-lo), que és ara el cor del skill. Nivell B = "després", no descartat.

## 1. Ordre de lectura (20 min)

1. Aquest document.
2. `STATUS.md` § "Via A en curs" (taula 8/8 amb commits per projecte).
3. `docs/ANALISI-NIVELL-A-LECTURA-HUMANA-2026-08-23.md` **§11** (taula 15×8 + què signifiquen OK/CAND/NT/n/a/ERR) i §7 (mètrica/hold-out).
4. `.claude/commands/g3dt-llegir-projecte.md` **sencer** (v0.4). És alhora el playbook i l'entregable: cada regla hi és perquè un
   projecte la va exigir. El Pas 0 (context del document) és la resposta a la reflexió del Josep — no el rebaixis a "metadata".
5. `docs/golden-read/_LESSONS.md` — registre append-only de lliçons per projecte, amb les "Regles en paraules d'Eva"
   (citades d'`EXPLICACIÓ DETALLS.docx` de Bell-lloc) al capdavant.
6. Un `_decisions.json` sencer com a model: `docs/golden-read/4001612 BELL-LLOC/_decisions.json` (el més net) i
   `docs/golden-read/3001621 CASTELLAR DEL VALLES/_decisions.json` (conté l'ERR #1 i el CAND−, amb les regles apreses).
7. MEMORY.md: `feedback_no_pull_eva_success_criterion`, `feedback_tier_a_extraction_first`, `eva_workflow_annexes_before_wizard`,
   `gtl_lab_identity`, `feedback_windows_folders`, `feedback_building_type_close_match`.
8. Handoff anterior (`docs/_FOR-NEW-YOU-20260823-1745.md`) només si necessites el marc de la via A; el seu §6 ja està executat.

## 2. Regles interpretatives crítiques (no òbvies des dels fitxers)

- **La mètrica que mana és ERR-amb-confiança = 0.** OK% i CAND% són secundàries. NT no penalitza si el valor realment no és a
  cap fitxer (10 dels 10 NT d'aquesta lectura ho són: superfícies del Cadastre/visor, UTM del visor, arquitecte extern).
- **Els 2 ERR i el CAND− ja són regles** (skill v0.4): etiqueta de mostra = annex de l'Eva > GTL; arquitecte persona vs despatx
  = candidats; CTE derivat amb superfície construïda TOTAL de l'encàrrec. En reexecutar, no re-descobrir-los: verificar-los.
- **Les referències (`eva_reference_values.json`) tenen soroll conegut**: Tulipa NO té informe de l'Eva (la "referència" ve d'un
  informe GENERAT nostre → usar `_user_data_prev.json` amb `_sources == 'user'`); Anciles V0 desalineada (plantes='C-1',
  superficie='T-1', dpsh=frase dels SPT); Vilanova sense `.docx` a la còpia de Windows (el PDF és imatge; `lab_depth` marcat ⚠
  pendent). Els 11 "n/a" de la taula són això, no camps sense resposta.
- **Multi-informe**: Tulipa = 1 expedient, 2 informes, 2 clients, 2 arquitectes (nota del pressupost "DOS INFORMES … UN A NOM DE
  CADA CLIENT"); Anciles = 7 adossats en UN informe. El senyal és el pressupost, no el nombre de cases.
- **Biaix**: la lectura d'or ja està feta; qualsevol re-lectura ara està contaminada per les referències. El següent pas honest
  de validació és el **hold-out headless** (§7.2 de l'anàlisi): executar el skill des de fora de la sessió (subagent o
  `claude -p`) sobre un projecte i comparar sense donar-li res més que la carpeta.

## 3. Supòsits estructurals que aguanten el pes

| On | Què | No desfer |
|---|---|---|
| `docs/golden-read/{exp}/*.json` | evidència de la lectura (cites verbatim + `context`) | append-only; si una lectura era errònia, anotar-ho al `_decisions.json`, no reescriure el JSON de font |
| `docs/golden-read/*/_decisions.json` camp `bias_disclosure` | què sabia el lector abans de llegir | mantenir-lo a qualsevol decisió nova; és el que fa el resultat creïble |
| `.claude/commands/g3dt-llegir-projecte.md` Pas 0 | context del document ABANS de llegir (mapa casella→rol) | és el mecanisme anti-"client=G3"; cap refactor del skill el pot fer opcional |
| `scripts/tier_a_truth_map.py` | matching per paraula sencera (`(?<![\w.,])…(?![\w])`) + docstring arreglat (`095e817`) | el codi commitat abans NO compilava i el JSON vell tenia falsos positius (995 dins de 620199571) |
| `automation/ai_pipeline/`, `web/vision_groq.py`, `auto_extractor.py` | fallback via B intacte | segueix prohibit tocar-los en aquesta línia |
| `web/vision_fast.py:172-182` | patró `claude -p … --permission-mode bypassPermissions` | és la forma headless de producció; reutilitzar, no reescriure |
| Branca `experiment/nivell-a-2026-08` in-place a `g3dt-prod/` | tota la via A | `production/g3dt-eva-v1` intacta a `1f1d7fd`; MAI proposar pull/merge a l'Eva |

## 4. Procediments operatius

```bash
# Estat esperat en obrir
git branch --show-current    # → experiment/nivell-a-2026-08
git log --oneline -3         # → 128da7e (STATUS 8/8), db10d8e (§11 + skill v0.4), 47c2cee (Anciles)

# Regenerar el truth-map (ara compila; matching \b)
.venv/bin/python scripts/tier_a_truth_map.py docs/audit/tier-a-truth-map-2026-08-23.json

# Taula 15×8 des dels _decisions.json (el generador és trivial; refer-lo si cal)
#   → codi d'exemple dins del commit db10d8e (missatge) o refés amb json.load de comparison_with_eva.totals

# Fonts: còpies de Windows (MAI reference-material/ per a dades)
/mnt/c/claude/g3dt/projectes/{7}  i  /mnt/c/claude/g3dt/projectes-debug/3001706 C.TULIPA CERDANYOLA
```

- `.venv/bin/python` (repo) té `fitz`, `openpyxl`, `xlrd`, `extract_msg`, `python-docx`, `PIL`. **No** té `ezdxf`; no hi ha
  `dwg2dxf` al sistema (verificat): el conversor DWG és tasca, no supòsit.
- Helper de volcat usat pels subagents: `scratchpad/dump.py` (efímer, al scratchpad de la sessió anterior — recrear-lo si cal;
  ~60 línies: pdf per blocs ordenats (y,x), xlsx/xls totes les cel·les, msg cos+adjunts amb md5).
- Els subagents llegint per parelles (text/visió) amb el skill com a prompt han funcionat: ~120-280k tokens/agent, 0 ERR
  atribuïbles a ells. El patró d'encàrrec és als transcripts de la sessió i replicable des del skill mateix.
- FreeHand→PDF (PScript5/Distiller): text brossa → renderitzar a 100 dpi (150-200 amb clip per a manuscrits i caixetins).
  Signatures digitals: `fitz` widgets → xref `/V` → `/Name`, `/M`.

## 5. Seqüència d'obertura suggerida

1. `git branch --show-current` + `git log --oneline -3` (contra §4). Si no quadra, atura't i pregunta.
2. Llegeix §1 (sobretot skill v0.4 i _LESSONS.md).
3. Tria el primer pendent amb el Josep si és a la sessió; si no, l'ordre recomanat és el de §8 (el hold-out headless valida
   tota la resta i és el que converteix "0 ERR esperat" en "0 ERR mesurat").
4. Per al hold-out: executa el skill via subagent SENSE accés a `validation/` ni a `docs/golden-read/` (dis-li explícitament)
   sobre 2-3 projectes, i compara tu les seves decisions amb les existents. Divergències = regles poc clares del skill.
5. Commit per peça, com fins ara (`docs(g3dt): …`).
6. Si toques el skill, puja la versió al header i anota el perquè (una línia).

## 6. Què NO fer

- No proposar pull/merge/desplegar a l'Eva (memòria `feedback_no_pull_eva_success_criterion`). Ni "mentrestant".
- No tocar `automation/ai_pipeline/`, `web/vision_groq.py`, `automation/auto_extractor.py`, `web/vision_fast.py`.
- No escriure a `reference-material/` ni a `validation/` de les còpies de Windows; no commitar les 119 supressions de
  `mined_images` ni els 3 `docs/diagnostics/ai_pipeline_demo-project_*.md` ni `photo_selection.json` (deixats a propòsit).
- No re-llegir projectes "per millorar la taula": la lectura d'or és tancada; el que toca és hold-out headless.
- No usar el truth-map antic (pre-`095e817`) ni citar el §3.2 de l'anàlisi sense el matís dels falsos positius (nota al §11).
- No convertir un `candidats` en `segur` sense 2 fonts independents: els 17 CAND actuals són multiplicitat real o pràctica
  variable de l'Eva, no mandra.
- No fer servir "cost a volum" per triar models (Eva és l'única usuària) ni presentar millores residuals com a resultat.

## 7. Pendents (ordre recomanat)

1. **Hold-out headless del skill** (valida ERR = 0 sense biaix): subagent o `claude -p` amb el skill, sense referències, 2-3
   projectes, comparació per part teva. És el "GO" real de la via A.
2. **Conversor DWG** (Tulipa: `PARAMETRES URBANISTICS.dwg` dins del zip hauria de portar els 564 m²): provar LibreDWG
   (`dwg2dxf`) + `ezdxf` al venv; si no, ODA File Converter; documentar si no és viable.
3. **Tancar els n/a**: aconseguir els `.docx` reals de Vilanova (verificar `lab_depth` 0,8-1,4 vs 1,00-1,60 ⚠) i Anciles
   (plantes, superfície, nombre de DPSH a l'informe); regenerar les referències desalineades si cal.
4. **Fase 4a**: lectors deterministes Python de les 5 plantilles G3 — les cel·les exactes ja són al Pas 2 del skill
   (pressupost per blocs (y,x), fitxa C6/C7/F38, comanda N18-N23+fila 35, PLAN_COST E9/OFERTA!B21, Excel DPSH fulls).
5. **Disseny de la crida headless des del wizard** (reutilitzar `vision_fast.py`) + **UI de candidats** (tres estats; el badge
   "+N" actual no té senyal de dubte). Multi-informe (Tulipa) és canvi d'UI/model de dades: anotat, no dissenyat.
6. Desplegament a l'Eva (instal·lació presencial CLI Windows, login, flag, timeout+fallback+telemetria): decisió del Josep.

## 8. Guanys ocults (no surten a la taula)

- El skill v0.4 és el producte: 8 projectes l'han esculpit i 6 agents frescos l'han seguit sense contaminació de context —
  la millor evidència fins ara que un `claude -p` headless el podrà executar a casa de l'Eva.
- `EXPLICACIÓ DETALLS.docx` (Bell-lloc) és el playbook de l'Eva en primera persona (gener 2026): citat a `_LESSONS.md`;
  qualsevol dubte de "on mira l'Eva" es resol abans allà que especulant.
- Descoberta la cadena completa de l'error històric "client = G3": albarà TPS → SmartScan → informe. Amb el Pas 0 és
  estructuralment impossible de repetir.
- El paquet `docs/golden-read/` (163 JSON amb cita i ubicació) és alhora oracle, corpus de test per a la Fase 4a i material
  de demostració per al Josep davant de G3.
