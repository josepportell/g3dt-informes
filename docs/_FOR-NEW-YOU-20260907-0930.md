# FOR NEW YOU — 2026-09-07 (matí, 09:30) — BLOC 1 del PLA FET i mesurat (P5 · P6 · CTE · rang · data · variant). SENSE commit. Següent: bloc 2 (cua d'A: format)

**Escrit:** 2026-09-07, 09:30. Substitueix `_FOR-NEW-YOU-20260907-2230.md` (conserva'l: hi ha el bloc 1 peça per peça tal com es va
plantejar). Branca `experiment/nivell-a-2026-08`, in-place a `g3dt-prod/` (estat esperat). **Commitejat en 4 (GO del Josep 09:35): `6d2d79b` (P5) ·
`eb1c2a4` (P6) · `375b7c9` (CTE, rang, data, variant) · docs/tests/runs; arbre net** (només `photo_selection.json` de Linyola sense versionar). `production/g3dt-eva-v1` @ `c46bc69` sense push.

## En una frase

Els 4 projectes sense sondeig calculen ara sobre la geometria del tall llegit (P5) i un sol classificador de sòl (P6); CTE T-1, rang
per taules, data de signatura i variant d'assentament fets: **calc 66 → 76 %, total 67 → 71 %, taules 79 → 81 %** (`viaA`, 7 projectes),
A i narrativa intactes. Tot a DECISION-LOG 2026-09-07 (decisions A-G, cel·les guanyades i perdudes).

## Estat exacte

- Runs de referència NOUS: `2026-09-07-m341-bloc1b` i `2026-09-07-informe-bloc1b` (els `-peca3` són l'anterior; `bloc1a` = primera
  passada amb el crash de la Secció 3, conservat).
- Suite: 31 vermells amb els mateixos NOMS que `suite-vermells-esperats.txt` / 2324 verds / 5 omesos.
- Preguntes 22-25 a l'Eva afegides a `docs/PREGUNTES-EVA-PENDENTS.md`.
- Pèrdues acceptades del bloc 1 (4 + 4 moviments X→X) a `docs/REGISTRE-PERDUES-MESURA.md` (append-only, per revisar amb l'Eva; cada bloc hi afegeix les seves).

## Què NO fer

- No tocar la narrativa ni la plantilla per pujar el número.
- No decidir P4 (N20 global o del tram) ni `sulfate_level_name` (preguntes 6 i 13).
- No inventar contactes de nivell a Vilanova/Anciles (el tall no els imprimeix; el segmentador hi mana i és honest).
- No proposar pull/merge a l'Eva; no pujar `production/g3dt-eva-v1`.

## Bloc 2 (el que faràs ara), del PLA §Bloc 2

Cua del grup A, 33 X de les quals ≈ 20 són FORMAT: `superficie_parcela` «1.284» (milers), `plantes` amb `format_floor_notation` sobre
el text llegit, `client` amb «SR./SRA.», `building_type_lower` amb article, `spt_lithology` en forma curta, `municipality_proper` del
padró a la impressió, `cota_referencia` d'Anciles des de l'annex DPSH. Els que depenen de l'Eva (arquitecte 1.7, N30, cota relativa,
superfícies de Vilanova) esperen. Referència: `2026-09-07-m341-bloc1b/<slug>/viaA/_compare_341.txt` (grup A).

## Procediments

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"; git branch --show-current; git status --short | grep -v runs/
PYTHONPATH=$PWD .venv/bin/python -m pytest tests/test_bloc1_calc_taules.py tests/test_geotech_criteria.py tests/test_bearing_layer_rule.py tests/test_terzaghi_caps.py tests/test_cte_classifier.py -q --no-header -p no:cacheprovider
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_341.py 2026-09-<dd>-m341-<motiu>
M=docs/wizard-headless/mesures/runs; for s in castellar rubi bell-lloc linyola alcoletge vilanova anciles; do diff <(grep '^- ' $M/2026-09-07-m341-bloc1b/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') <(grep '^- ' $M/2026-09-<dd>-m341-<motiu>/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') | grep '^[<>]' | sed "s/^/$s: /"; done
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_informe.py 2026-09-<dd>-informe-<motiu>
for d in $(cd $M/2026-09-07-informe-bloc1b && find . -name '_compare_informe.txt'); do diff -q $M/2026-09-07-informe-bloc1b/$d $M/2026-09-<dd>-informe-<motiu>/$d >/dev/null || { echo "DIFF $d"; diff $M/2026-09-07-informe-bloc1b/$d $M/2026-09-<dd>-informe-<motiu>/$d | grep '^[<>]' | head -6; }; done
PYTHONPATH=$PWD .venv/bin/python -m pytest tests -q --no-header -p no:cacheprovider > /tmp/suite.txt 2>&1; diff <(grep '^FAILED' /tmp/suite.txt | sed 's/ - .*//' | sort) <(grep -v '^#' docs/wizard-headless/mesures/suite-vermells-esperats.txt | sort); rm -f docs/diagnostics/ai_pipeline_demo-project_$(date +%Y%m%d).md
```

Gotcha del `grep`: el hook `rtk` reescriu `grep` i pot fallar en pipelines: `rtk proxy grep …`. Els `.doc` signats es llegeixen amb
`soffice --headless --convert-to txt:Text --outdir <dir> <fitxer.doc>` (fet avui per verificar les taules in situ).

## Seqüència d'obertura

1. `git branch --show-current` (una línia al Josep), `git status`, `git log --oneline -5` (docs del bloc 1 al capdamunt, després `375b7c9`, `eb1c2a4`, `6d2d79b`).
2. DECISION-LOG 2026-09-07 (decisions A-G i «cel·les que es perden») i el PLA §Bloc 2.
3. Bloc 2 peça per peça, mesura després de cada una contra `-bloc1b`, DECISION-LOG per bloc.
