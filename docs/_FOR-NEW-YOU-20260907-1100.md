# FOR NEW YOU — 2026-09-07 (suffix 1100) — Bloc 4 del PLA FET i mesurat (numeració i imatges), SENSE commit: pendent del GO del Josep

**Escrit:** 2026-09-07 (vespre), en tancar el bloc 4. Substitueix `_FOR-NEW-YOU-20260907-1015.md` (que obria el bloc 4); conserva'l.
Branca `experiment/nivell-a-2026-08`, in-place a `g3dt-prod/` (estat esperat; `production/g3dt-eva-v1` @ `c46bc69` sense push, mai
proposar pull/merge a l'Eva). **Arbre BRUT a consciència**: tot el bloc 4 està fet i mesurat però no commitejat (el Josep dona el GO
als commits al final de cada bloc). `git status`: `automation/reference_extractor.py`, `automation/image_manager.py`,
`docs/wizard-headless/mesures/mesura_341.py`, `docs/wizard-headless/mesures/refresh_eva_narrativa.py`, els 7
`reference-material/*/validation/eva_reference_values.json`, `tests/test_bloc4_numeracio.py` (nou), docs (DECISION-LOG, REGISTRE,
PREGUNTES, PLA, STATUS, sessió, aquest handoff, memòria) i els runs `2026-09-07-m341-bloc4-num`, `2026-09-07-m341-bloc4b` (amb
`_IMATGES.md` + `imatges/*.jpg`), `2026-09-07-informe-bloc4b`. `reference-material/4001607 LINYOLA/validation/photo_selection.json`
sense versionar ja hi era abans.

## Què s'ha fet (detall: DECISION-LOG 2026-09-07 (vespre), decisions A-G)

- **Numeració** (22 forats `*_num`, mai mesurats): `extract_numbering_variables` a `reference_extractor.py` (pas 5b): per TIPUS
  (Figura / Fotografia / Taula / capçalera), text sense número, capçaleres ratio ≥ 0,70, peus ratio ≥ 0,5 **i** contenció de
  paraules ≥ 0,6, assignació global un-a-un, empat = blanc només amb números diferents, sense àncora. 99 veritats noves als 7
  signats (`--keys all --apply --label "2026-09-07 (bloc 4, numeració)"`), només `_num` van moure al dry-run.
- **M341**: grup `fix` = text exacte, buit del generador amb veritat = X; test de presència d'imatges (`image_presence`, taula
  «Imatges» a l'agregat). **Resultat: 74 M · 25 X → 75 %; cap altra cel·la moguda** (`-bloc3b` → `-bloc4-num` → `-bloc4b` diff buit
  fora de `fix`; 12 informes idèntics a `-bloc2b`).
- **Les 25 X, cap és error de comptar** (registre de pèrdues, bloc 4): 10 nombre de figures del projecte (plantilla fixa 3, signats
  2/3/4), 5 taules de Linyola (pregunta 23), 5 expansivitat (`include_expansivity` mai derivat; pregunta 28), 2 vistes generals de
  Rubí (tria de l'Eva), 1 empentes d'Anciles (`has_basement` del wizard; pregunta 29), 2 incoherències del signat (Castellar salta
  la Fotografia 3; Bell-lloc «2.4.3» ×2).
- **Imatges**: full de control 7 × 11 a `runs/2026-09-07-m341-bloc4b/_IMATGES.md` (+ `imatges/<projecte>.jpg`). **Bug greu trobat**:
  la cau global d'imatges usava el nom del PDF com a clau → el tall de correlació de Bell-lloc sortia a 5 projectes més, el plànol
  A.01 de Bell-lloc a Alcoletge, el retall de situació de Linyola a Anciles (verificat per md5). Arreglat: `ImageManager._cache_name`
  (`<prefix>_<nom>_<md5 del PDF>.jpg`, 7 punts); 0 duplicats a `-bloc4b`. **`production/g3dt-eva-v1` té el mateix codi** (6 punts
  amb `.stem`): decisió del Josep quan i com hi arriba.
- Tests: `tests/test_bloc4_numeracio.py` (20). Suite: 31 vermells amb els mateixos noms / 2451 verds esperats.

## Referència nova a batre

`docs/wizard-headless/mesures/runs/2026-09-07-m341-bloc4b` (M341 viaA: **308 · 43 · 123 · 33 → 74 %**; A 78, calc 77, narr 61,
**fix 75**, resta 87; taules 292 · 99 · 88 → 82 %) i `2026-09-07-informe-bloc4b` (12 informes, idèntics a `-bloc2b`).

## Obert (per a qui vingui)

1. **Decisions del Josep**: (a) commits del bloc 4 (proposta en 4: `reference_extractor.py` + tests · `mesura_341.py` +
   `refresh_eva_narrativa.py` · 7 veritats · `image_manager.py`; després docs i runs); (b) bloc de figures del projecte a la
   plantilla (2 / 3 / 4 figures: 10 cel·les); (c) foto de materials repetida per nivell amb el mateix número (plantilla p254-262:
   `{{ photo_materials_image }}` dins del bucle de nivells; els signats en porten UNA); (d) portar `_cache_name` a producció.
2. **Preguntes a l'Eva**: 23 (Linyola «3, 4 i 5»: 6 cel·les), 28 (expansivitat: criteri i posició: 5), 29 (empentes i soterrani: 1).
3. **Imatges pendents (8)**: UTM a la via A per a Rubí / Vilanova / Anciles (ortofoto + geològic) i rol `architect_plan` a Castellar /
   Vilanova. **Imatges errònies (4)**: Alcoletge «màquina DPSH» = full de camp; Anciles «màquina del sondeig» = testimonis i «plànol»
   = portada d'`IV_PLANOS.pdf`; Rubí «plànol» = foto d'un paper. **Retall (1)**: Linyola situació en tira (A4 apaïsat, altra
   maquetació). Tot a `image_manager.py` (tria de fotos, pàgina del plànol, `_render_situation_plan_left`).
4. Bloc 5 (castellà): els peus «Fotografía» / «Tabla» de Vilanova i Anciles queden en blanc (contenció CA/ES); les capçaleres ja
   s'extreuen.

## Procediments (els de sempre + els nous)

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"; git branch --show-current; git status --short | grep -v runs/; git log --oneline -4
PYTHONPATH=$PWD .venv/bin/python -m pytest tests/test_bloc4_numeracio.py tests/test_bloc3_extractor.py tests/test_reference_extractor.py -q --no-header -p no:cacheprovider
PYTHONPATH=$PWD .venv/bin/python docs/wizard-headless/mesures/refresh_eva_narrativa.py --all-keys        # dry-run; avui: 0 diferències
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_341.py 2026-09-<dd>-m341-<motiu>
M=docs/wizard-headless/mesures/runs; for s in castellar rubi bell-lloc linyola alcoletge vilanova anciles; do diff <(grep '^- ' $M/2026-09-07-m341-bloc4b/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') <(grep '^- ' $M/2026-09-<dd>-m341-<motiu>/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') | grep '^[<>]' | sed "s/^/$s: /"; done
grep -E "^\| \*\*Total|^\| \`viaA\` \| (A|calc|narr|resta|fix) " $M/2026-09-<dd>-m341-<motiu>/_AGREGAT-341.md; grep -A 10 "^## Imatges" $M/2026-09-<dd>-m341-<motiu>/_AGREGAT-341.md
# duplicats d'imatges entre projectes (md5 dels word/media/*, saltant les 11 estàtiques de la plantilla): ha de dir «cap»
PYTHONPATH=$PWD .venv/bin/python -m pytest tests -q --no-header -p no:cacheprovider > /tmp/suite.txt 2>&1; diff <(grep '^FAILED' /tmp/suite.txt | sed 's/ - .*//' | sort) <(grep -v '^#' docs/wizard-headless/mesures/suite-vermells-esperats.txt | sort); rm -f docs/diagnostics/ai_pipeline_demo-project_$(date +%Y%m%d).md
```

Gotchas: `rtk` reescriu `grep` / `git show` (→ `rtk proxy …`); `str()` d'un `InlineImage` viu peta (mira `type(v).__name__`); la
cau antiga (`tall_tall.jpg`, `planol_A.01.jpg`…) queda a `~/g3dt-prod-cache/images/` sense ús; els signats es llegeixen amb
`soffice --headless --convert-to txt:Text`.
