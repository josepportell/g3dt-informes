# FOR NEW YOU — 2026-09-05 23:15 — Bloc 1: peces 1.4 (derivats) i 1.5 (L1/L3) fetes, skill v1.9 validat; següent 1.6 T2 (decisió sobre la passada LLM de conflictes)

**Escrit:** 2026-09-05, 23:15. Per a la sessió següent (1.6 T2 i, després, 1.7 amb l'Eva i el bloc 2). Substitueix
`_FOR-NEW-YOU-20260906.md` com a punt d'entrada; conserva'l: al capdamunt té les §Actualització (nit 4 → 8) i a sota el mètode
de la nit (test + reconsolidació dels 7 a cost 0 + llista de TOTES les cel·les que canvien) i les trampes, que continuen vigents.
Branca `experiment/nivell-a-2026-08`, in-place a `g3dt-prod/` (estat esperat). **RES commitejat des de `f399baf`** (handoff del
06): 13 fitxers modificats + 30 no seguits (les `_reconsolida-2026-09-05-{d14,l3,l1,v19}/` ×7 i dos runs parcials).

**En una frase:** la nit ha tancat 1.4 (cinc derivats geomètrics de `soil_levels`) i 1.5 (telèfon fora del nom, cap N30 a una
mostra alterada, i l'SPT del full manuscrit amb re-lectura d'un document), més una esmena (la «R» no s'infereix, era impresa
a tres documents → skill v1.9, validat re-llegint l'annex DPSH); **taules 139/21/6/31/0 → 144 OK / 28 CAND / 6 ALERTA / 19
blancs / 0 ERR**, escalars intactes a 118/23/5/1/0 (els tres llindars), 2,72 USD de subscripció en dues lectures.

## Ordre de lectura (15 min)

1. Aquest document.
2. `STATUS.md`: capçalera (nit, 8) i **§«Pendents de revisió»** (dos ítems del Josep amb rutes exactes: or d'Alcoletge
   `soil_levels[1].a`; Linyola `mostra_del_nivell` material vs interval → pregunta 13 a l'Eva).
3. `docs/_FOR-NEW-YOU-20260906.md` §Actualització (nit 4, 5, 6, 8) i §Regles d'interpretació / §Trampes / §Procediments.
4. `docs/DECISION-LOG.md`, entrades 2026-09-05 (nit, 4) → (nit, 8): regles, alternatives rebutjades, marxes enrere.
5. `docs/wizard-headless/mesures/runs/2026-09-03-mesura-8/_AGREGAT-8.md` §nit 4 → §nit 8; `_DIAGNOSTICS-INDEX.md` §Estat dels
   fixes — nit (files 1.4, L3, L1, v1.9, i la fila ⏳ «T2 (decisió), R4 + persona/despatx»).
6. Per a T2: `docs/DECISION-LOG.md` 2026-09-05 (tarda) §decisió 7 («T2 sense heurística de multi-expedient») i §Limitacions
   («T2 no elimina el cost fix»); `_AGREGAT-8.md` §Runner i operació (D4, D6, T1, T2); `automation/lectura/runner.py`
   l.170-200 (`_load_config`: `G3DT_LECTURA_CONSOLIDA` = auto | python | llm) i l.830-880 (branca `auto`);
   `tests/test_lectura_runner.py` l.765-880 (Fase 12: tests dels tres modes).
7. Memòria: `project_mesura8_fila0b_fixes_2026-09-05` (actualitzada fins a nit 8).

## Peça 1.6 T2 — què és i què ja se sap (mesurat aquesta nit a cost 0)

**Què és.** El runner consolida amb Python (Fase 12) i, en mode `auto` (defecte), crida el `claude -p --consolida
--only-fields` NOMÉS per als camps amb conflicte A-vs-A (dues fonts d'autoritat que discrepen), fins a un topall de 8. El
handoff del 05 (fila 1.6) demanava: «mesurar, als 7 runs, si els valors que ha aplicat (`_consolida_only.json`) són millors
que els candidats Python contra el signat. Si no: `G3DT_LECTURA_CONSOLIDA=python` per defecte».

**Inventari (`{slug}/_reconsolida-2026-09-05-v19/_decisions.json` → `conflicts`; `{slug}/_consolida_only.json` = resposta LLM
cachejada del run mare):**

| Projecte | Conflictes ara | Resposta LLM cachejada | LLM vs Python (estat, valor, candidats) |
|---|---|---|---|
| Castellar | 4: `lab_sample_id`, `street_address`, `utm_x`, `utm_y` | 3 (sense `street_address`: conflicte posterior a la passada) | **idèntics** els 3 |
| Bell-lloc | 1: `street_address` | 1 | idèntic |
| Rubí | 1: `lab_sample_id` | 1 | idèntic |
| Vilanova | 1: `street_address` | 1 (1r intent rc=1 als 292 s; el 2n va bé) | idèntic |
| Linyola, Alcoletge, Anciles | 0 | — | — |
| Tulipa (sense veritat) | 6 (3 escalars + 3 de taula) | 3 escalars aplicats, 3 de taula sense resposta | idèntics els 3 |

**Conclusió empírica:** en els 8 runs del corpus, la passada LLM **no ha canviat cap valor** respecte de la decisió Python
(9 de 9 cel·les respostes són iguals cel·la a cel·la; les que no respon queden com Python). La pregunta «quin dels dos és
millor contra el signat» no té objecte: són el mateix valor. El que sí costa: **200-290 s i 14-23 torns per projecte, 0,94-1,08
USD** (Tulipa 523 s, 30 torns, 1,95 USD), i un cas de rc=1 (Vilanova) que va obligar a repetir (T1: el CLI no emet res fins al
final; les penjades no es detecten).

**Decisió que cal prendre (del Josep, amb aquesta evidència):** defecte `python` (cap crida LLM; els conflictes queden
`candidats` amb tots els candidats visibles, que és exactament el que l'LLM tornava). `auto`/`llm` continuen disponibles per
variable d'entorn per a mesures futures. Efecte per a l'Eva: 3-5 min menys per projecte i un punt de fallada menys.

**Si el Josep diu sí, on toca:** (a) `runner._load_config` l.175 (`"auto"` → `"python"`) i el missatge d'avís de mode invàlid
l.176-177 («fent servir auto»); (b) `tests/test_lectura_runner.py` l.765+: els tests fixen el mode amb `monkeypatch.setenv`,
mira si n'hi ha cap que provi el DEFECTE (grep `G3DT_LECTURA_CONSOLIDA` i `_load_config()` sense env) i afegeix-ne un;
(c) docs: `docs/DISSENY-WIZARD-HEADLESS-CANDIDATS-2026-08-24.md` (taula de modes), capçalera v1.5 del skill (frase «deixa per a
una crida LLM curta … NOMÉS els camps amb conflicte real» → «opcional, `auto`»), `_DIAGNOSTICS-INDEX.md` fila T2, DECISION-LOG,
STATUS, PLA fila 0b. **Mesura:** reconsolidació dels 7 (`reconsolida_mesura.py` fusiona la `_consolida_only.json` si existeix:
per construcció, res canvia) + `tests/test_lectura_runner.py` + suite a fitxer amb diff de noms. Cost 0.

**Trampa:** `reconsolida_mesura.py` sempre fusiona la `_consolida_only.json` cachejada (l.55-57) — les mesures de la nit ja
la porten dins (`llm_only_fields: True` a 6 cel·les). Si canvies el defecte, aquesta fusió NO és el que faria el runner en
`python`: per a la mesura «com quedaria en `python`», comenta la fusió o passa un sub sense ella i comprova que cap veredicte
canvia (ho hauria de ser: mateixos valors).

## Regles d'interpretació (no òbvies)

- **El comparador mesura contra l'or de lectura, no contra el signat.** Dels **6 ALERTA de taules**: 5 són «OK per veritat»
  (Rubí cota P-1/P-3 i `soil_levels[2].de`, Vilanova `id` ×2) i 1 és formal — Alcoletge `soil_levels[1].a`: l'or diu
  `no_trobat`, els altres 4 ors i el signat diuen «fins al fons» (STATUS §Pendents, 1). No els comptis com a errors.
- **CAND «disjunts» amb contingut bo:** els `mostra_del_nivell` derivats són booleans i l'or hi té textos («possible»,
  «Si — material»); les `a`/`de` per punt vs agrupades (Alcoletge). El comparador no els casa; no «ho resolguis» canviant el
  format del derivat ni afegint perdó al comparador.
- **Els 19 blancs de taula que queden són fora de l'abast del consolidador:** 8 cotes d'Anciles (I1, lectures a
  `runs/2026-09-05-i1-anciles/`, no al run mare) i 11 lectures gràfiques del tall que cap lector emet (Vilanova i Anciles
  transicions inclinades + `mostra`, Rubí franja superficial, Bell-lloc base de la cobertura).
- **Llegir, no inferir (esmena nit 7):** la «R» de l'N30 de Linyola era impresa a 3 documents; el lector 1.6 la va deixar a la
  cita amb el valor buit per «sempre registre». Abans d'inventar una derivació, mira si el valor ja és en alguna lectura
  (`grep` als `{doc}.json` del run). La regla «≥ 50 cops → R» del consolidador és una convenció de transcripció de rescat,
  només si el lector no dona cap valor.
- **El lector és un productor cec també en les claus** (`value_candidates` per a `n30`): el consolidador llegeix per forma;
  una clau nova torna a deixar la cel·la en blanc en silenci. Rastre de dialectes només per a superfície (limitació apuntada).
- **Una lectura = una mostra d'un lector variable.** El mateix document, mateix model i esforç: 1.6 no emetia la fila SPT del
  manuscrit, 1.8 sí; 1.9 llegeix la columna de colors «Nivells» de l'annex DPSH que 1.6 ignorava. Cap «validació» d'una
  lectura és definitiva; la de debò arriba amb el proper projecte nou.
- **Cost i comptabilitat:** el runner usa la sessió de claude.ai (`G3DT_LECTURA_AUTH=login` per defecte; la clau API del
  `.env` es filtra perquè no té crèdit). El `cost_usd` de `_telemetry.jsonl` és el que el CLI reporta, consumeix quota de
  subscripció. Per document (Sonnet 5, xhigh): correu 0,6-0,7 · PDF senzill 0,8-1,1 · annex de diverses pàgines 1,2-1,5 ·
  manuscrit PENETROS 1,6-2,1. Projecte sencer ≈ 18 USD; els 7 ≈ 125. **Dir sempre el cost abans de llançar una lectura.**
- **`authority_for` mana; `FIELD_AUTHORITY_CONF` 0,5 no s'abaixa; `field_date` = primer dia** (vegeu handoff del 06).

## Invariants de codi dels quals depèn la mesura (no desfer)

- `automation/lectura/consolidate.py` → `_derive_soil_levels` es crida a `consolidate_python` DESPRÉS de
  `_cota_relative_system` i `_depths_from_msnm`: els derivats necessiten les `a` en fondària i per punt (R2 les converteix).
  Ordre intern D1 → D2 → D3 → D4 → D5.
- D1 (`_derive_first_level_top`): `segur` «0,00» només si la primera fila NO és cobertura (`_is_cover_row` via `_COVER_RE`);
  el cas amb cobertura el governen E2/E2b (Pas 3b). Puja a segur una lectura ≤ 0,05 m; una lectura no superficial no es toca.
- D3 (`_derive_last_level_base`): el text `_FONS_PREFIX` = «fins al fons d'investigació (…)» és la forma canònica (= or de
  Linyola); `_depth_nums` i `_cell_depths` l'ignoren a posta. **(nit 8)** si TOTES les bases llegides de l'últim nivell són
  fondàries de rebuig/sondeig (±5 cm), el text va PRIMER (Linyola, Bell-lloc, Castellar); si el fons és més avall, s'afegeix.
- D4 (`_derive_sample_level`): omple `no_trobat`; a `candidats` només afegeix el booleà derivat si no hi és (afirmació d'un
  document no-A contradita per la geometria); mai toca un `segur`. Sostre/base «al punt» prefereixen clàusules amb el punt
  (`_cell_depths` parteix per «;»); gruix nul = interval inexistent (el 0,00 de l'annex sota una cobertura, E2).
- `_ma_sample_has_no_n30`: només si TOTES les etiquetes de la fila són MA. Castellar («SPT-1» annex vs «MA1» GTL, pregunta
  12) ha de conservar el «R».
- `_strip_phone_tail`: només telèfon AL FINAL i amb ≥ 2 lletres davant; camps `_PERSON_LIKE_FIELDS`.
- Branca `n30` de `_cell_signals`: alies de claus (`value_candidates`, `valor_candidats`, `candidats_valor`, `n30_candidats`) i
  «≥ 50 → R» només sense cap candidat escrit.
- Carpetes `_anterior-skill-1.6/` dins dels runs parcials: `load_corpus` només mira `out_dir.glob("*.json")` (no recursiu). Un
  JSON de lectura vella al primer nivell comptaria com a document.
- `_preext/` dels runs NOUS (l1, v19) s'ha esborrat abans de versionar (37 MB, regenerable). Els de `2026-09-03-mesura-8/*`
  SÍ estan seguits (642 fitxers): no els toquis.
- Cadena de referències de reconsolidació: … → `-f1` → `-d14` (1.4) → `-l3` (L3) → `-l1` (L1) → **`-v19` (actual)**.
- La suite regenera `docs/diagnostics/ai_pipeline_demo-project_20260905.md` (timestamp): `git checkout --` després.
- Skill `g3dt-llegir-projecte.md` **v1.9**: v1.8 (el full d'assaig SPT p.3 emet SEMPRE la fila amb `registre`) + v1.9 (N30
  imprès sense registre = candidat «sense registre»). Cache per md5: editar el skill no invalida res.

## Procediments

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"     # el cd dins d'una comanda canvia el cwd de la sessió: repeteix-lo
git branch --show-current                                            # experiment/nivell-a-2026-08
# tests dels mòduls tocats (5 s)
PYTHONPATH=$PWD .venv/bin/python -m pytest tests/test_lectura_consolidate.py tests/test_lectura_service.py tests/test_field_dates_text.py tests/test_lectura_runner.py -q --no-header -p no:cacheprovider
# línia base (ha de donar 118/23/5/1/0 i 144/28/6/19/0)
PYTHONPATH=$PWD .venv/bin/python docs/wizard-headless/mesures/agrega_mesura.py --sub _reconsolida-2026-09-05-v19
# reconsolidació dels 7 a cost 0 + TOTES les cel·les que canvien
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/reconsolida_mesura.py _reconsolida-2026-09-06-t2 _reconsolida-2026-09-05-v19
M=docs/wizard-headless/mesures/runs/2026-09-03-mesura-8; for s in castellar bell-lloc rubi linyola alcoletge vilanova anciles; do for k in escalars taules; do diff <(grep -v '^TOTALS' $M/$s/_reconsolida-2026-09-05-v19/_compare_$k.txt) <(grep -v '^TOTALS' $M/$s/_reconsolida-2026-09-06-t2/_compare_$k.txt) | grep '^[<>]' | sed "s/^/$s $k: /"; done; done
grep -o '[0-9]* conflictes A-vs-A' $M/*/_reconsolida-2026-09-06-t2/_decisions.json      # 4/1/1/0/0/1/0 (castellar/bell-lloc/rubi/linyola/alcoletge/vilanova/anciles)
# suite sencera (3 min) a fitxer, i diff de NOMS vermells (31 esperats)
PYTHONPATH=$PWD .venv/bin/python -m pytest tests -q --no-header -p no:cacheprovider > /tmp/suite.txt 2>&1; diff <(grep '^FAILED' /tmp/suite.txt | sed 's/ - .*//' | sort) <(grep -v '^#' docs/wizard-headless/mesures/suite-vermells-esperats.txt | sort)
git checkout -- docs/diagnostics/ai_pipeline_demo-project_20260905.md
```

**Re-lectura parcial d'UN document (recepta de la nit, només si el Josep ho autoritza amb el cost dit abans):** copia
`runs/2026-09-03-mesura-8/{slug}/` a `runs/<data>-<motiu>/` amb `rsync -a` excloent `_reconsolida-*`, `_compare_*`,
`_decisions.json`, `_elapsed.txt`, `_consolida_cache.json`, `_DIAGNOSTIC.md`, `_NOTES.md`, `_telemetry.jsonl`, `_events.jsonl`,
`_preext` i el `{doc}.json` que vols re-llegir; escriu `_NOTES.md`; llança `run_lectura(project, out_dir=…)` (vegeu
`runs/2026-09-05-l1-linyola/_NOTES.md`, comanda completa) amb `G3DT_LECTURA_CONSOLIDA=python`, `nohup … &` i vigila el log
(`"cached": false`, `DONE`, `Traceback`); comprova `_telemetry.jsonl` (cost), el `{doc}.json` nou, i consolida la carpeta amb
`consolidate_python` abans de copiar la lectura al run mare (la vella a `_anterior-skill-<v>/`) i reconsolidar els 7.

Dades: `~/g3dt-e2e/projectes/<NNNNNNN NOM>/`. Cache HTTP: `/home/josep/g3dt-prod-cache` (no netejar). Lectures cachejades =
`{doc}.json` per md5 dins de cada carpeta de run. `rtk` retalla sortides llargues: redirigeix a fitxer.

## Seqüència d'obertura suggerida

1. `git branch --show-current` + `git status --short | wc -l` (43 entrades esperades). **Res commitejat des de `f399baf`:**
   abans de res, pregunta al Josep si commiteja, i proposa-li tres commits: (1) 1.4 derivats (`consolidate.py` bloc 1.4,
   `test_D14_*`, docs nit 4), (2) 1.5 L3+L1+v1.9 (`consolidate.py` L3/n30, `test_L3_*`/`test_L1_*`, skill, runs parcials,
   `linyola/penetros.json` i `pdf_annexes_4001607_dpsh.json` nous, docs nit 5-8), (3) handoffs. O un de sol si ho prefereix.
2. Reprodueix la línia base v19 amb `agrega_mesura.py` (118/23/5/1/0; 144/28/6/19/0). Si no coincideix, atura't.
3. Llegeix §Peça 1.6 T2 d'aquest doc i confirma l'inventari amb una comanda (conflictes v19 vs `_consolida_only.json`).
4. Exposa al Josep la decisió (defecte `python`) amb l'evidència «9 de 9 iguals, 200-290 s i ~1 USD per projecte per res».
5. Si diu sí: runner l.175 + test del defecte + docs; reconsolidació (res canvia) + `test_lectura_runner.py` + suite a fitxer.
6. Docs amb el patró de la nit (DECISION-LOG entrada nova, `_AGREGAT-8.md` §, `_DIAGNOSTICS-INDEX.md` fila T2, STATUS, PLA fila
   0b, sessió del dia, handoff). Commit només si el Josep ho diu.
7. Després: 1.7 (R4 CTE imprès, persona/despatx — necessita l'Eva: `docs/PREGUNTES-EVA-PENDENTS.md`, files 11-13 noves) i
   bloc 2 (P0, P2a, P2b, M341 amb línia base abans de codificar).

## Què NO fer

- No re-llegir documents per mesurar un canvi del consolidador: reconsolida (cost 0). Cap lectura sense dir-ne el cost.
- No tocar l'or (`docs/golden-read-taules/*/_tables_decisions.json`): el pendent d'Alcoletge és decisió del Josep.
- No afegir al comparador regles que perdonin el que mesura; no canviar el format dels derivats per igualar un or concret.
- No proposar pull/merge a l'Eva (memòria `feedback_no_pull_eva_success_criterion`). No tocar `geocode_coordinates.py` (via B).
- No omplir cap derivat sense font «(derivat: …) ← …» ni pujar-lo a segur (excepció única: D1, definició geomètrica).
- No deixar JSON de lectures velles al primer nivell d'una carpeta de run (subcarpeta `_anterior-skill-<v>/`).
- No versionar `_preext/` de runs nous; no esborrar els `_preext/` seguits de la mesura-8.
- No parlar en codis al Josep («D3», «T2», «L1»): tradueix cada codi a què passa, on, què costa; rutes absolutes.
- No confondre les «R» del penetròmetre (Excel DPSH B80, «Rebuig a la cota de…») amb l'N30 de l'SPT: dos assaigs.

## Tasques obertes

- **1.6 T2** (aquest handoff §Peça 1.6): decisió del Josep + implementació + mesura. Cost 0.
- **1.7** (amb l'Eva): R4 CTE imprès (9 cel·les `cte_*`), persona/despatx (`architect_name` Linyola). Preguntes 11-13 noves a
  `docs/PREGUNTES-EVA-PENDENTS.md` (cota P-2 Rubí; MA-1/SPT-1 Castellar; nivell de la mostra per fondària o material).
- **Pendents de revisió del Josep** (STATUS §): or d'Alcoletge `[1].a`; Linyola `mostra_del_nivell` material vs interval.
- **Peces futures apuntades, no planificades:** banda de color «Nivells» de l'annex DPSH com a font explícita de la
  transició per punt (l'or d'Alcoletge ho fa; Vilanova podria guanyar-hi `[0].a`/`[1].de` — caldria re-llegir el seu annex,
  ≈ 1 USD); «N=R» del tall cap a `n30` (`n30_candidat_tall`, comprovar al proper run); rastre de dialectes de claus per a `n30`
  a `notes_estructurals`; «material vs interval» per a `mostra_del_nivell` (comparar litologies).
- **Bloc 2** (P0 columna N, P2a col·lapse de nivells, P2b fondària de sabata, M341 mesura completa amb línia base primer) i
  **bloc 3** (S1 multi-casa: capa intermèdia a la branca, després branca nova). Ordre del Josep: `_FOR-NEW-YOU-20260905.md` §final.
- **Commit** de tot el que hi ha (decisió del Josep; vegeu obertura 1).

## Guanys que no surten a cap taula

- Els 5 derivats i els 2 fixes de 1.5 són **cost 0 per sempre**: cap crida nova per projecte; només Python sobre el
  `_decisions.json`. Els 31 → 19 blancs de taula són el que el consolidador podia fer sol; la resta és lectura.
- L'esmena (nit 7) ha canviat una manera de pensar: abans de derivar, `grep` a les lectures. La «R» era a la cita d'un
  document que ja teníem. El skill v1.9 ho recull i ja s'ha vist funcionar (1 lectura, 1,09 USD).
- El lector 1.9 llegeix la banda de color «Nivells» de l'annex DPSH: una font de transicions per punt que el corpus no
  explotava. D3/D4 ja la digereixen sense regressions; convertir-la en font explícita és feina petita i mesurable.
- T2 ja té l'evidència feta: la passada LLM no ha mogut cap valor en 8 runs. La sessió següent només ha de decidir i apagar-la.
