# FOR NEW YOU — 2026-08-25 20:25 — Pre-extracció v2 ADOPTADA (26 min), Fase 10 feta, effort fixat, 3 models mesurats (Fable 20,5 / Opus 4.8 23,6 / Sonnet 26,2); següent: Fase 12 (Python-first + guards) abans de la Fase 11

**Escrit:** 2026-08-25, 20:30 · **Per a:** la sessió següent (mateix vespre o 2026-08-26) que arrenca la Fase 12.
**Recap de la sessió:** del handoff del matí ("Fase 10 + experiment pre-extracció") s'ha passat a: Fase 10 construïda (`8d3b0ce`), pre-extracció v1
mesurada i rebutjada (`0dfff32`, colors d'Excel absents), v2 mesurada i **adoptada a producció** (`5fdaa97` + `e87da6c`, skill v1.4), descoberta
que tots els runs corrien a effort `xhigh` heretat → runner fixa `--effort` (`1ea3a28`), i tres models comparats sobre el mateix pipeline
(llibre: 7 runs). Decisions del Josep: Fable @xhigh quan el pla ho permeti, preparats per Opus 4.8 @high; objectiu real = **les 341 variables
de l'informe**, no només el nivell A. Commits del dia sobre `da995b3`: `0dfff32 8d3b0ce 96f9c61 5fdaa97 1ea3a28 0876873 f661d41 e87da6c` (+ el d'aquest handoff).

## 1. Ordre de lectura (20 min)

1. Aquest document.
2. `STATUS.md` — blocs «🎯 Objectiu real (Josep, 2026-08-25)» i «Wizard headless + UI de candidats» (paràgrafs 2026-08-25 matí i nit, i «Decisions Josep (20:25)»).
3. `docs/wizard-headless/mesures/LEDGER.md` — 7 runs; i `runs/*/meta.json` → `quality_judgement.notes` (el judici línia a línia viu aquí, no al xat).
4. `docs/wizard-headless/fase8-e2e/_RESULTATS.md` — addendums 2026-08-25 matí (v1), vespre (v2 + effort) i nit (taula final de tres models + lectura per tipus de document).
5. `docs/DECISION-LOG.md` — entrades «2026-08-25» i «2026-08-25 (vespre)».
6. `docs/DISSENY-ANNEX-TRES-BOTONS-JOBS-NOTIFICACIONS-2026-08-24.md` §7.2 (consolidació Python-first) i §10 fila 12 — és la feina següent.
7. `.claude/commands/g3dt-llegir-projecte.md` v1.4: subsecció «Pre-extracció (mode --only)» dins d'Arguments, i Pas 5/5b (el consolidador que la Fase 12 ha de substituir en part).
8. `docs/_FOR-NEW-YOU-20260825.md` §3-§4 (regles i supòsits de les Fases 0-9, tots vigents) i §7.
9. MEMORY.md: `project_via_a_claude_code_reader`, `reference_claude_p_inherits_effort_xhigh`, `project_eva_subscription_model_tier`, `feedback_full_report_341_variables_goal`.

## 2. Marc fixat pel Josep (2026-08-25) — no re-litigar

- **Objectiu real: l'informe complet, 341 variables** (`DIAGNOSTIC-PROD-2026-08-23` §3). Nivell A és el pas 1; després latència, grup B (càlculs) i la resta.
  Que el wizard n'ensenyi un subconjunt no limita l'abast. Mai declarar "qualitat resolta" amb només el nivell A.
- **Model:** Fable @xhigh quan la subscripció ho permeti; preparats per **Opus 4.8 @high** (`G3DT_LECTURA_MODEL=claude-opus-4-8 G3DT_LECTURA_EFFORT=high`).
  El defecte codificat del runner segueix `sonnet` fins que G3 triï pla. `--model opus` és **Opus 5**, no 4.8.
- **Pre-extracció v2 adoptada**: `G3DT_LECTURA_PREEXT` ON per defecte, skill de producció v1.4, sense còpia `-preext`.
- **Taula prod-vs-via-A per a l'Eva: ajornada** (cal unificar veritat, camps i semàntica; sessió del matí).
- Vigents del 24-08: qualitat primer (Pas 0/3/verificació intocables), només subscripció, ≤ 1 projecte/dia, tres botons, totes les mesures al llibre.

## 3. Regles interpretatives crítiques

- **El consolidador LLM (`--consolida`) és el punt feble comú a tots els models.** 8 consolidacions en 2 dies: 1 en dialecte pla de taules (Sonnet, cel·les
  `"cota_inici": "-4 m"` en lloc de `{estat, value, candidates}` → comparador 23 ABSENT), 2 amb senyals emesos perduts (`num_floors` = PB+1 emès pel correu
  del pressupost i descartat: Sonnet ×1, Opus 4.8 ×1 + `cte_edificacio`), 1 candidat inventat (Opus: `sondeig cota '-4,20 m'` amb font "derivat" i cita
  "-4 m"; ve de l'exemple del skill "Eva: -4,20"). Fable n'ha estat net (n=1). Conseqüència: **Fase 12 abans que Fase 11**.
- **Per jutjar la lectura, separa-la de la consolidació.** Si una consolidació surt estranya, rellança-la sola (13 docs en cache → només `--consolida`, ~8-10 min;
  procediment §5) abans de culpar la lectura. `merge_degradat` sobre els perdoc dels 3 runs va donar lectura equivalent (3 OK / 11-13 CAUTELA).
- **n=1 per model.** La variància del consolidador entre runs (13-14 OK d'escalars) és de la mida de les diferències entre models. El que és robust:
  paret (Fable −22 %), neteja de taules (Fable 23/0), i que els tres tenen 0 erroni-amb-confiança de fons.
- **Erroni-amb-confiança de fons = 0 segueix manant.** Tots els ERR dels 7 runs són format: dates `24/10/2025` vs `2025-10-24`, `-4` vs `-4,0`, `1,0 - 1,2 m`
  vs `-1,00 a -1,20 m`, variants de carrer. Cas límit anotat: `nivell_freatic` `'No indicat'` vs or `'No detectat'` (mateix fet; regla de vocabulari pendent).
- **Effort:** el CLI accepta `--effort low|medium|high|xhigh|max`; el Josep té `CLAUDE_EFFORT=xhigh` + `effortLevel: xhigh` als settings; fins a `1ea3a28`
  els fills ho heretaven. Ara el runner fixa `--effort` (defecte `xhigh`) i treu `CLAUDE_EFFORT` del fill. Els 5 runs previs porten `effort: "xhigh (heretat)"`
  al `meta.json`. Files d'effort inferior (Sonnet@high, @medium) NO fetes: són palanques pendents, cadascuna amb comparador d'or.
- **Per què Fable és més ràpid:** no per tok/s (74 vs 79 de Sonnet) sinó per un terç menys de turns (10,1 vs 14,8) i la meitat de tokens d'entrada.
  Guanya en documents de molts passos (fils de correu, pressupostos de 7 p, manuscrits de 5 p); perd en documents d'un cop d'ull (`tall.pdf`, correu curt,
  `DPSH.xls`). Opus 4.8: pocs turns però més tokens i 71 tok/s → suma ≈ Sonnet; el més ràpid a Excel/correus, el més lent als annexos gràfics d'1 pàgina.
- **Pre-extracció: què hi ha a `validation/lectura/_preext/{safe_name}/`:** `page-N.txt`, `page-N.png` (150 dpi), meitats `-left/right` o `-top/bottom`
  (220 dpi) **només per a pàgines amb `text_ok=false`**, `sheet-i.csv` + `sheet-i.cells.txt` (`REF<TAB>valor`) + `sheet-i.colors.txt` (fons/font de color;
  buit = cap color), `body.txt` + `attachments/` (dedup md5 → `already_in_folder`), `image.png` (≤ 1600 px), `meta.json` (`text_ok`, `halves_pages`,
  `text_suspect_reason`, `colored_cells`, `error`). 72 MB per projecte (Castellar). `text_ok` = ≥ 20 alfanumèrics i ràtio de caràcters normals ≥ 0,75;
  el producer Distiller només informa (el mateix producer dona text net i brossa).

## 4. Supòsits estructurals que aguanten el pes (nous; els dels handoffs anteriors segueixen vigents)

| On | Què | No desfer |
|---|---|---|
| `runner.py::_load_config` | `preext` (`_env_bool`, defecte True), `skill` (defecte `g3dt-llegir-projecte`, `G3DT_LECTURA_SKILL` mana), `effort` (validat, defecte `xhigh`) | els tests de `test_lectura_runner.py` fixen argv `… --model M --effort E --output-format json` (flags DESPRÉS del prompt: el mock parseja `argv[1]`) |
| `runner.py::_child_env` | treu SEMPRE `CLAUDE_EFFORT` (a més de les claus API en mode login) | sense això l'effort del pare mana i les mesures no són transferibles |
| `runner.py::run_lectura` | `augment_inventory` en try/except just abans de `lectura_inventari`; event `lectura_preext {n_docs,n_errors,elapsed_s}` o `{error}` | la pre-extracció mai pot fer caure la lectura |
| `preext.py` | idempotent per `source_md5` (meta.json = marca de fet); cap escriptura dins `project_path`; excepcions per document → `meta.error` | `run.sh` esborra `validation/lectura/` sencer (inclou `_preext/`) → cada run és fred |
| `preext.py` `.xls` colors | fons blanc explícit `(255,255,255)` = sense color (`_XLS_BG_DEFAULT_RGB`); `.xlsx` font `theme 1` = defecte | la plantilla DPSH pinta ~450/560 cel·les de blanc sòlid; sense el filtre, 542 "colors" |
| `scripts/write_doc_json.py` | única via d'escriptura del skill en mode `--only`: valida, nom canònic (`safe_doc_name`), atòmic; `authority_for` sense `tier_a` = WARNING, no error | un error aquí = el document no s'escriu i el runner reintenta (`_MAX_ATTEMPTS`) |
| `jobs.py::Job.emit` | escriu `_job.json` a disc ABANS de repartir als subscriptors; `subscribe()` fa replay de `history` sota lock | evita `_job.json` endarrerit després de l'event terminal, i forats/duplicats en attach |
| `jobs.py::JobRegistry.list_jobs` | `interrupted` = estat no terminal i `pid != os.getpid()`; **mai `os.kill(pid, 0)`** | a Windows `os.kill` amb senyal arbitrari MATA el procés; el wizard és un sol procés |
| `jobs.py` mapa d'events | només `lectura_decisions_marker`/`consolidacio_inici`/`consolidacio_fallback` → `consolidating`; l'event propi `decisions` (payload) NO | mapejar-lo faria retrocedir `merging` → `consolidating` just abans de `ready` |
| `web/lectura_service.py` | `run_lectura_job(emit)` = cos del pipeline; `get_lectura_streaming(attach=)` = subscriptor; `_jobs_root()` = `wizard_service._REF_DIR` llegit dinàmicament | els tests reassignen `_REF_DIR` amb monkeypatch; en mode xarxa el workspace aplana al nom fulla → escaneig de 1r nivell és correcte |
| `contract.py` | una cel·la de taula PLANA (string) passa la validació (`_canonicalize_row_cells` només toca dicts amb `estat`) | és el forat que va deixar passar el dialecte pla; la Fase 12 hi posa l'embolcall determinista |

## 5. Procediments operatius

```bash
git branch --show-current        # → experiment/nivell-a-2026-08 (in-place a g3dt-prod/)
git log --oneline -10            # → e87da6c f661d41 0876873 1ea3a28 5fdaa97 96f9c61 8d3b0ce 0dfff32 da995b3 …
# Suite de lectura (30 s, 131 passed / 2 skipped: xlwt absent + xls colors) i completa (2,5 min; línia base 32 failed, cap a lectura/preext/jobs)
.venv/bin/python -m pytest tests/test_lectura_runner.py tests/test_lectura_preext.py tests/test_lectura_service.py tests/test_lectura_jobs.py tests/test_lectura_contract.py tests/test_lectura_inventory.py tests/test_utf8_read_text.py -q -p no:cacheprovider
.venv/bin/python -m pytest tests/ -q -p no:cacheprovider     # mai decidir amb `| tail -1`; la suite genera docs/diagnostics/ai_pipeline_demo-*.md (no versionar)

# Run complet de Castellar sol (esborra validation/lectura/, pre-extracció inclosa) — 20-26 min segons model
~/g3dt-e2e/fase9/run.sh 3 <etiqueta>                                              # sonnet @xhigh, preext ON (defectes)
G3DT_LECTURA_MODEL=fable ~/g3dt-e2e/fase9/run.sh 3 <etiqueta>                     # Fable
G3DT_LECTURA_MODEL=claude-opus-4-8 G3DT_LECTURA_EFFORT=high ~/g3dt-e2e/fase9/run.sh 3 <etiqueta>
G3DT_LECTURA_PREEXT=0 ~/g3dt-e2e/fase9/run.sh 3 <etiqueta>                        # sense pre-extracció (v1.3 de fet)
# Llançar: `setsid nohup … > /dev/null 2>&1 &` + Monitor sobre run.log (patró 'lectura_doc \{|lectura_doc_error|decisions|TOTAL_ELAPSED|Traceback|DONE')

# NOMÉS la consolidació (13 docs en cache; segona mostra del consolidador, ~8-10 min): esborra _decisions.json i corre sense force
rm "$HOME/g3dt-e2e/projectes/3001621 CASTELLAR DEL VALLES/validation/lectura/_decisions.json"
PYTHONPATH=. .venv/bin/python -c "from pathlib import Path; from automation.lectura.runner import run_lectura; run_lectura(Path('$HOME/g3dt-e2e/projectes/3001621 CASTELLAR DEL VALLES'), on_event=lambda n,p: print(n, str(p)[:120], flush=True), force=False)"
# (el driver amb force=False de la sessió és a l'scratchpad; run.sh SEMPRE fa force=True)

# Ingerir un run al llibre (carpeta + meta.json + perdoc/ + ledger.py); copia meta.json d'un run anterior i edita label/model/effort/changes/quality_judgement
D=docs/wizard-headless/mesures/runs/<etiqueta>; mkdir -p $D/perdoc
cp ~/g3dt-e2e/fase9/<etiqueta>/{_telemetry.jsonl,_decisions.json,escalars.txt,taules.txt,run.log} $D/
cp "/tmp/g3dt-lectura-3001621 CASTELLAR DEL VALLES-consolida.log" $D/consolida.log
cp "$HOME/g3dt-e2e/projectes/3001621 CASTELLAR DEL VALLES/validation/lectura/"*.json $D/perdoc/
python3 docs/wizard-headless/mesures/ledger.py

# Pre-extracció sola (12 s, per inspeccionar el que veu el model), a un tmp
PYTHONPATH=. .venv/bin/python -c "from pathlib import Path; from automation.lectura.inventory import write_inventory; from automation.lectura.preext import augment_inventory; import tempfile; o=Path(tempfile.mkdtemp()); p=Path('$HOME/g3dt-e2e/projectes/3001621 CASTELLAR DEL VALLES'); augment_inventory(p, write_inventory(p,o)); print(o)"
# Zoom manual: .venv/bin/python scripts/render_clip.py PDF --page N --clip 0.1,0.3,0.6,0.5 --dpi 400 --out /tmp/clip.png

# Comprovar l'effort/model reals d'una crida: telemetria porta `model`, `effort`, `skill`, `preext`
python3 -c "import json;[print(json.loads(l)['mode'],round(json.loads(l)['elapsed_s']),json.loads(l).get('num_turns'),json.loads(l).get('model'),json.loads(l).get('effort')) for l in open('PATH/_telemetry.jsonl')]"
```

- Artefactes de la sessió fora del repo: `~/g3dt-e2e/fase9/{preext-c3,preext-v2-c3,preext-v2-c3-consolida2,fable-preext-v2-c3,opus48-high-preext-v2-c3}/`.
- `run.sh` registra `preext=` i `skill=` a la línia START; **no** registra `effort` (afegir-ho si es fan files d'effort).
- `pkill -f PATRÓ` es mata a si mateix si el patró surt a la seva línia: `"[c]laude -p /g3dt"`. `sleep` en primer pla sol estar bloquejat: `until` en `run_in_background` o Monitor.
- Els agents Sonnet (code-implementer) van usar `git stash` DUES vegades malgrat la prohibició; al brief cal la prohibició **amb el motiu** (stash compartit entre sessions vives) i dir-los que la línia base ja és coneguda. Sense el motiu, ho tornen a fer.
- El session log de l'agent: un implementer hi va enganxar un bloc "HANDOFF SUMMARY" amb frontmatter; revisa `.claude/sessions/` després de cada delegació.

## 6. Seqüència d'obertura suggerida

1. `git branch --show-current`, `git log --oneline -10`, suite de lectura (131).
2. Llegeix l'annex §7.2 i el Pas 5/5b del skill v1.4; llegeix `runs/2026-08-25-preext-v2-c3/{_decisions.json,consolida2/_decisions.json}` per veure les dues sortides del mateix consolidador.
3. **Fase 12, brief tancat a Sonnet** (decidir amb el Josep si la fa Sonnet): `automation/lectura/consolidate.py` (Python-first): (a) cap `tier_a` emès es perd —
   cada entrada esdevé candidat del seu `concept_id` amb `font`+`quote`; (b) `_g3_templates.json` = autoritat A; (c) regles deterministes: duplicats per md5,
   independència de fonts, `segur` només amb ≥ 2 fonts independents o plantilla G3 ≥ 0,9, `n30`/`litologia` mai `segur`, fusió `spt_ma_tests` per punt+fondària,
   embolcall de cel·les planes `{estat: estat_bloc, value, candidates:[{value, font:"(adaptat)"}]}` al contracte, candidats derivats només amb font documental;
   (d) la crida `--consolida` LLM només per als camps en conflicte real (`--only-fields`, skill v1.5) o cap. Acceptació: comparador d'or escalars + taules a
   Castellar i Bell-lloc amb 0 erroni-amb-confiança de fons i cap cel·la per sota de `consolida2`/Fable; fila nova al llibre (temps de consolidació: objectiu < 60 s).
4. Regla de vocabulari al skill (Pas 3b): `nivell_freatic` absent amb columna N.F. buida → `No detectat` (no `No indicat`). Normalitzadors del comparador per a
   dates i signes (els ERR de format deixen de fer soroll).
5. Fase 11 (delta-sync) després; Fases 13-15 (auto_result, UI tres botons, notify).
6. Preguntes al Josep quan toqui: pla de subscripció (Fable o Opus 4.8), política de neteja de `_preext/` (72 MB/projecte), decisions §12 de l'annex.

## 7. Què NO fer

- No proposar pull/merge/desplegar a l'Eva (memòria `feedback_no_pull_eva_success_criterion`).
- No retallar el protocol del skill (Pas 0/3/3b/verificació). La pre-extracció canvia *com* s'accedeix al document, no *què* es mira.
- No canviar el defecte de model a Fable al codi fins que el pla estigui decidit; no córrer Fable "perquè sí" (pes ×3,7 sobre el pla).
- No jutjar un model o una palanca amb una sola consolidació estranya sense rellançar la consolidació sola.
- No usar `--model opus` pensant que és 4.8 (és Opus 5); l'id és `claude-opus-4-8`.
- No confiar en cap mesura de temps feta sense `--effort` explícit (les 5 primeres files del llibre són `xhigh` heretat; ja anotat).
- No editar `LEDGER.md` a mà; no reescriure `docs/golden-read*`; no tocar la via B ni afluixar `contract.py` excepte per l'embolcall determinista de la Fase 12.
- No córrer dues lectures del mateix projecte alhora (la Fase 10 posa lock només dins del servidor web; `run.sh` no en té).
- No escriure a `/mnt/c`; no llançar E2E amb `G3DT_LECTURA_AUTH=api_key`; no `stream-json` al runner.
- No deixar que un implementer toqui git ni el session log; no incloure `docs/diagnostics/ai_pipeline_demo-*.md` ni `photo_selection.json` als commits.

## 8. Tasques obertes (ordre)

1. **Fase 12** — consolidació Python-first + guards + embolcall de cel·les planes + candidats derivats amb font (§6.3). Objectiu doble: qualitat (cap senyal perdut, cap invenció) i temps (−8-10 min per run i per botó 3b).
2. Skill v1.5: vocabulari `No detectat`; normalitzadors de format al comparador d'or.
3. Fase 11 (delta-sync) → 13 (`auto_result` a disc + residus Groq) → 14 (UI tres botons + taula) → 15 (notify) → 16 (E2E tres botons) → 17 (Windows).
4. Files d'effort (Sonnet@high, @medium) i, quan hi hagi pla, Fable/Opus 4.8 sobre Bell-lloc (2n projecte) — totes amb comparador d'or.
5. Política de neteja de `validation/lectura/_preext/` (72 MB/projecte) abans de Windows; `run.sh` amb `effort=` a START.
6. Pendents anteriors intactes: forat 1 (fonts Python al consolidador — la Fase 12 el pot tancar), Fase 8b, Tier B, preguntes a l'Eva (criteri N30, sulfats Anciles, `sondeig_tests[0].cota` relativa vs absoluta), multi-informe Tulipa.
7. **Objectiu 341 variables**: després del nivell A i la latència, grup B (càlculs) i la resta — mantenir un registre d'abast per variable (A / B / narrativa / lab / geocode) perquè res quedi "més o menys".

## 9. Guanys ocults

- **El llibre ja permet decidir model, effort i palanques amb dades i no amb opinions**: 7 files comparables (mateix projecte, mateix comparador, effort ara explícit), i el procediment per aïllar el consolidador de la lectura.
- **La pre-extracció fa el model més barat i més fiable alhora**: −37 %/doc, −31 % de tokens, i el model ja no s'inventa `fitz` cada cop (les `reading_notes` mostren el protocol intacte i el zoom amb `render_clip.py` on cal).
- **La Fase 10 converteix la lectura en un job**: estat a disc, un sol job viu per projecte, attach amb replay — la base perquè l'Eva tanqui la pestanya.
- **S'ha localitzat el punt feble real**: no és el model ni la lectura, és la consolidació LLM (dialecte pla, senyals perduts, candidat inventat). La Fase 12 té ara un cas d'ús empíric, no teòric.
- **Preparats per al pla econòmic**: Opus 4.8 @high dona 0 erroni-amb-confiança de fons; amb la Fase 12 és una opció real, no un pla B a cegues.
