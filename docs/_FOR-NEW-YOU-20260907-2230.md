# FOR NEW YOU — 2026-09-07 (nit del 06, 22:30) — Peça 3 COMMITEJADA (d7d02e9 · 10423c4 · 67fcd12). Hi ha PLA de tot el que queda (`docs/PLA-QUE-QUEDA-DESPRES-DE-A-B-I-NARRATIVA-2026-09-07.md`). Següent: BLOC 1 = càlcul i taules dels 4 projectes sense sondeig (P5, P6, CTE, rang de taules, data de signatura)

**Escrit:** 2026-09-06, 22:30. Substitueix `_FOR-NEW-YOU-20260907-2115.md` (conserva'l: hi ha les regles de la peça 3). Branca
`experiment/nivell-a-2026-08`, in-place a `g3dt-prod/` (estat esperat). Arbre net excepte el pla, aquest handoff i les dues línies de
STATUS/sessió (sense commit: el Josep decideix). `production/g3dt-eva-v1` @ `c46bc69` sense push.

## En una frase

Les dades A (lectura, 74 %), B/càlcul (66 %) i la narrativa (61 %) ja estan modelades i mesurades; el Josep ha preguntat si queda
algun altre tipus i la resposta és el pla: cap tipus nou, però (1) la cua d'A i calc (33 + 32 cel·les X), (2) les taules de Vilanova,
Alcoletge i Anciles, (3) un bloc mai mesurat (numeració, imatges) i (4) el castellà (decisió: sí, més endavant). **Ordre suggerit i
acceptat implícitament pel Josep en demanar el handoff: bloc 1 (P5/P6/CTE) → bloc 2 (cua d'A) → bloc 3 (extractor) → bloc 4 (numeració
i imatges) → castellà quan digui.**

## Bloc 1, peça per peça (el que faràs primer)

Referència de mesura: `2026-09-07-m341-peca3` (M341) i `2026-09-07-informe-peca3` (11 taules). Diagnòstic complet: DECISION-LOG
2026-09-06 (vespre), «Troballes de M341» 1-3.

1. **P5 — geometria de nivells des de la lectura quan no hi ha sondeig.** Avui `sondeig_layers` (i amb ell el nivell portant, el
   règim i les potències de la taula sísmica) surt del segmentador DPSH quan no hi ha sondeig: Linyola col·lapsa a un «Llims» 0-3,0
   (φ 38 / c 0 / E 450; signat 30 / 1,0 / >800). La taula `soil_levels` llegida (de/a per nivell, litologia; «fins al fons» = capa
   oberta) ja arriba al generador dins `user_data['lectura_tables']` i només s'usa per sobreescriure descripcions (Fase 8b).
   On: `automation/report_generator.py` (bloc `sondeig_layers`, `_bearing_soil_type`, càlcul de la sísmica),
   `automation/lectura/tables_report.build_report_tables`. Comprova amb `geomech_phi`/`geomech_cohesion` de Linyola, Alcoletge,
   Vilanova i la taula `sismica` (gruixos 0,80↔2,20 Vilanova, 1,00↔1,40 Alcoletge, 2,20↔4,00 Anciles).
2. **P6 — un sol classificador de sòl.** `cte_geomech.detect_soil_type` diu «grava» a «Arcilla limosa y arenosa con algunas gravas»
   (per «grav») i «granular» a «Rebliment antròpic»; `geotech_criteria._classify` ho fa bé (rebliment / argila / transicional /
   granular). Fes que `detect_soil_type` delegui a `_classify`, i que el topall Qa 3,5 (`terzaghi_calculator.calculate_qa`) dispari
   per a tot `GRANULAR_TYPES`, no només `'granular'` literal. Comprova `qa_value` Rubí 3,00 → 3,50, Alcoletge 3,00 → 3,50, Vilanova
   1,00 → 2,50 (aquest també necessita c 0,05 de P5) i la taula `geotecnica` de Vilanova (9 X) i Anciles (5 X).
3. **CTE sòl i edificació.** `cte_classifier.classify_soil` posa T-2 si N20 mitjà < 20 i T-3 amb rebliment; l'Eva escriu **T-1 a
   6/6 signats**: T-1 per defecte (T-2/T-3 només si l'Eva ho canvia al wizard). `classify_building`: Castellar C-0↔C-1 (les plantes
   llegides «Pb+1» han de comptar 2) i Alcoletge C-1↔C-0 (plantes buides → defecte C-1; el signat diu C-0 amb PB: agafa les plantes
   del pressupost/comanda o pregunta). Comprova `cte_sol` (1 · 0 · 5 → 6 · 0 · 0) i la taula `cte_edificacio`.
4. **Rang de taules in situ.** `_compute_numbering` compta TESTS (3 DPSH → «3, 4 i 5»); l'Eva compta TAULES: DPSH (1) + sondeig (1 si
   n'hi ha) + SPT (1) → «3 i 4» sense sondeig, «3, 4 i 5» amb sondeig, 7/7 signats. Deu línies. Comprova `table_dpsh_range` de Rubí,
   Alcoletge, Vilanova, Anciles.
5. **Data de signatura.** `data_signatura_text` és 0/7: el generador posa avui i l'Eva signa quan signa. Camp del wizard («Data de
   signatura», defecte avui, `WIZARD_FIELDS` + `review.html` + `report_generator`). Trivial; treu 7 X del grup «resta».
6. **Assentament, variant de frase.** Bell-lloc «inferiors a» vs el nostre «iguals o inferiors a»: candidat a `settlement_criteria`.

Després de cada peça: `mesura_341.py` amb nom nou + `diff` per projecte contra `-peca3`, i `mesura_informe.py` contra
`informe-peca3` (les 11 taules idèntiques o millor cel·la a cel·la). Suite a fitxer i `diff` de NOMS dels vermells.

## Què NO fer al bloc 1

- No tocar la narrativa ni la plantilla per pujar el número: el bloc 1 és càlcul i taules.
- No decidir P4 (N20 global o del tram) ni `sulfate_level_name` (nivell de la mostra): esperen les preguntes 6 i 13 a l'Eva.
- No re-extreure veritats senceres (bloc 3 va després, amb la seva disciplina de `diff`).
- No proposar pull/merge a l'Eva; no pujar `production/g3dt-eva-v1`.

## Procediments

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"; git branch --show-current; git status --short | grep -v runs/
# dirigits del càlcul (segons)
PYTHONPATH=$PWD .venv/bin/python -m pytest tests/test_geotech_criteria.py tests/test_bearing_layer_rule.py tests/test_terzaghi_caps.py tests/test_cte_classifier.py tests/test_settlement_criteria.py -q --no-header -p no:cacheprovider
# M341 (7 projectes, ~2-5 min) i diff contra la referència viva
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_341.py 2026-09-<dd>-m341-<motiu>
M=docs/wizard-headless/mesures/runs; for s in castellar rubi bell-lloc linyola alcoletge vilanova anciles; do diff <(grep '^- ' $M/2026-09-07-m341-peca3/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') <(grep '^- ' $M/2026-09-<dd>-m341-<motiu>/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') | grep '^[<>]' | sed "s/^/$s: /"; done
# 11 taules (3 projectes × 4 variants, ~1 min)
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_informe.py 2026-09-<dd>-informe-<motiu>
for d in $(cd $M/2026-09-07-informe-peca3 && find . -name '_compare_informe.txt'); do diff -q $M/2026-09-07-informe-peca3/$d $M/2026-09-<dd>-informe-<motiu>/$d >/dev/null || { echo "DIFF $d"; diff $M/2026-09-07-informe-peca3/$d $M/2026-09-<dd>-informe-<motiu>/$d | grep '^[<>]' | head -6; }; done
# suite (3 min) a fitxer, diff de NOMS
PYTHONPATH=$PWD .venv/bin/python -m pytest tests -q --no-header -p no:cacheprovider > /tmp/suite.txt 2>&1; diff <(grep '^FAILED' /tmp/suite.txt | sed 's/ - .*//' | sort) <(grep -v '^#' docs/wizard-headless/mesures/suite-vermells-esperats.txt | sort); rm -f docs/diagnostics/ai_pipeline_demo-project_$(date +%Y%m%d).md
```

Gotcha del `grep`: el hook `rtk` reescriu `grep` amb opcions i pot fallar en pipelines: `rtk proxy grep …`.

## Seqüència d'obertura

1. `git branch --show-current` (una línia al Josep), `git status`, `git log --oneline -4` (67fcd12 al capdamunt).
2. El pla sencer (`docs/PLA-QUE-QUEDA-DESPRES-DE-A-B-I-NARRATIVA-2026-09-07.md`) i DECISION-LOG 2026-09-06 (vespre) «Troballes de M341».
3. Línia base: re-executa M341 sense tocar res només si dubtes que el `-peca3` sigui reproduïble (memòria `feedback_measure_baseline_before_coding`).
4. Bloc 1 en l'ordre de dalt (P5 i P6 junts: comparteixen Vilanova i Linyola), mesura després de cada peça, DECISION-LOG per bloc.
