# FOR NEW YOU — 17-09-2026 (vespre): dues avaries silencioses tancades, dues decisions grosses obertes

**Escrit:** 2026-09-17, vespre. **Per a:** la sessió següent, amb la finestra de context sencera.
**Recap d'una frase:** la prova punta a punta amb Rubí va bé (27 min, 12/12, zero errors amb confiança), pel camí van caure dues avaries **silencioses** (el rellotge de la lectura i la coma decimal de les UTM), i van aparèixer **dues coses més grosses que ningú ha tocat encara** — la contradicció «solar pla» vs. secció de vessant dins l'informe signat, i els 18 de 64 camps que el sistema decideix i presenta com a resolts.

**La visita a l'Eva ja NO és el 18-09.** No respon els correus; objectiu **26-09-2026, sense confirmar**. No hi ha pressa de vigília: es pot treballar amb calma.

## 1. Ordre de lectura

1. Aquest document.
2. `STATUS.md`, primer paràgraf.
3. `docs/DECISION-LOG.md`, entrades **2026-09-17 (2)** i **(3)** — el «per què» de cada decisió amb les alternatives rebutjades.
4. **`docs/troballes/TROBALLA-SOLAR-PLA-VS-VESSANT-2026-09-17.md`** i **`docs/troballes/TROBALLA-UTM-COMA-DECIMAL-2026-09-17.md`** — les dues decisions obertes viuen aquí. Són el moll de la sessió.
5. `docs/_FOR-NEW-YOU-20260917-1400.md` §3 i §4 (regles d'interpretació i invariants del 17-09 al matí: segueixen vigents).
6. `MEMORY.md` del projecte, sobretot `feedback_wizard_ui_rules_josep`, `feedback_tier_a_extraction_first`, `feedback_no_pull_eva_success_criterion`, `feedback_measure_baseline_before_coding`, `feedback_no_rm_test_state_you_did_not_create`.
7. `docs/wizard-headless/mesures/runs/2026-09-17-wsl-rubi-e2e/` (`meta.json`, `_troballes.md`, `escalars.txt`, `taules.txt`) — la mesura sencera que ho va destapar tot.

## 2. Estat de les branques (verificat 2026-09-17 vespre)

| on | branca | HEAD | notes |
|---|---|---|---|
| `clients/g3dt-prod/` (worktree de treball) | `experiment/nivell-a-2026-08` | `649b2b4` | pujat a origin |
| `clients/g3dt-release/` (worktree) | `release/2026-09` | (merge de `649b2b4`) | pujat a origin |
| `~/g3dt-release-wsl-test/app` (clon net) | `release/2026-09` | `37c8c93` | **el servidor està ATURAT** (el sistema el va matar per falta de memòria). Li falta el merge de les UTM |
| `production/g3dt-eva-v1` | — | `123b4f2` | **intacta**, no rep res fins que el Josep ho decideixi |

Commits d'aquesta sessió: `bb10fb6` (rellotge), `649b2b4` (UTM + les dues troballes documentades).

## 3. EL PATRÓ DE FONS (llegeix això abans de res)

Les tres avaries del 17-09 **són el mateix error**:

- El rellotge deia «< 1 min restants» perquè un zero que volia dir *«encara no ho sé»* es tractava com un zero real.
- L'informe diu «es tracta d'un solar pla» perquè un pendent **desconegut** es tracta com a 0 %.
- Les UTM desapareixien perquè un valor que no cap en un camp es torna buit **sense queixar-se**.

**El sistema converteix «no ho sé» en una afirmació concreta, i ho fa en silenci.**

Això no és retòrica: és el criteri de disseny per a les dues decisions obertes. Quan hi treballis, la pregunta bona no és «quin valor hi posem» sinó **«com distingim "no ho sé" de "ho sé i és això"»**. La correcció del rellotge (`bb10fb6`) n'és l'exemple executable: la guarda va sobre `basis` (buida = mai calculat), **no** sobre el número, i es va rebutjar explícitament derivar-la de `docs_total > 0` perquè zero és un resultat legítim. Reprodueix aquest raonament.

## 4. DECISIÓ OBERTA 1 — l'informe es contradiu a si mateix

**Doc:** `docs/troballes/TROBALLA-SOLAR-PLA-VS-VESSANT-2026-09-17.md` (cadena verificada pas a pas).

Al `.docx` generat avui per a Rubí conviuen:
- **3.3.1 Hidrogeologia:** «Com que es tracta d'**un solar pla**, no s'han detectat marques…»
- **Índex 4.5:** «**ESTABILITAT DE VESSANT**» (i 4.4 «EMPENTES DE TERRES»)

Pendent real segons ICGC: **21,6 %**.

**Cadena:** Rubí no té `COORDENADES.txt` → quan es calculen els prefills no hi ha UTM → `automation/auto_extractor.py:311` (`_phase3_slope(utm_x, utm_y, …)`) no s'executa → `slope_percent` queda buit → `web/wizard_service.py:558` fa `slope_val = float(slope_pct) if slope_pct else 0.0` i `site_condition_sentence(0.0, …)` escriu la frase del solar pla amb font `'computed (slope 0%)'`. Les UTM arriben **després**, per la via de reserva dels adjacents (`wizard_service.py:333`, `source: geocode:adjacents_fallback`). En generar, `automation/report_generator.py:1195-1205` ja té UTM, crida `get_slope()`, obté 21,6 % i activa les seccions de vessant. **Ningú recalcula la frase.**

**Per què no s'havia vist abans:** a Alcoletge la coma decimal feia perdre les UTM, o sigui que `get_slope()` no s'arribava a cridar mai. **Un defecte n'amagava l'altre.** Ara que les UTM estan arreglades, aquest surt a la llum en més projectes.

**Camins possibles** (cap decidit): consultar el pendent quan ja hi ha UTM encara que arribin tard; que `site_condition` quedi **per revisar** si no hi ha `slope_percent` en comptes de resoldre's com a «pla»; que el generador recalculi o avisi si acaba sabent un pendent que contradiu el supòsit. Repassa si hi ha **altres frases narratives** decidides amb un valor per defecte que després es contradiu amb dades obtingudes més tard (`automation/narrative_criteria.py`).

## 5. DECISIÓ OBERTA 2 — 18 de 64 camps decidits pel sistema i presentats com a resolts

Mesurat sobre els prefills reals de Rubí (`GET /api/prefills/{projecte}`):

- **64 camps** de cara a l'Eva (exclosos els interns amb prefix `_`).
- **18** arriben amb valor generat / plantilla / per defecte amb font «automàtica».
- **21** compten a «Queden N camps a revisar».
- **Cap dels 18 hi compta.**

**Derivacions legítimes** (no toquis): `num_dpsh_tests`, `table_dpsh_range`, `num_site_photos`, `utm_x`/`utm_y` (geocode).
**Escriuen prosa a l'informe:** `site_condition`, `site_description`, `access_description`, `settlement_sentence`, `access_street`, `lab_tests_text`.
**Defectes estàndard sobre l'edifici:** `foundation_depth_m=0,3`, `has_basement=False`, `has_retaining_walls=False`, `num_soil_levels=1`, `sulfate_level_name='1er nivell'`, `data_signatura` (avui).

**El fet que ho canvia tot, i que va fer aturar la primera implementació:**
la capa «Observacions de camp» (`#evaPreQuestions`, `templates/validation/review.html:2706`) que s'obre sola a cada obertura **NO és redundant**, encara que els seus tres camps (`site_description`, `access_description`, `is_anthropized`) **ja existeixin al formulari** (`review.html:2906-2911` i `3044-3054`).

Cadena verificada: `_wizardBarPendingBadges()` (`review.html:7601-7616`) només compta `.source-badge.revisar` i `.estat-badge.candidats`/`.no_trobat`; la classe `revisar` només s'assigna quan `_wizardSourceLabel()` rep el literal `'revisar'` (`review.html:6316`); el backend sempre els omple amb una font calculada (`site_description_sentence()` → `computed (...)`, `wizard_service.py:529-544`; `access_description` → `'plantilla generada'`, 486-521; `is_anthropized` → `'ICGC ortho+visió'`, 469-483), i cap mapeja a `revisar`. `PRE_QUESTION_FIELDS`/`shouldShowPreQuestions()` (`review.html:5134-5138`) és **l'únic lloc del codi** que tracta «font ≠ user» com a senyal de «cal revisar» per a aquests tres camps.

**Conclusió:** si es treu la capa sense res més, tres camps s'enviarien a l'informe amb text generat que l'Eva no ha vist mai, i el comptador diria «tots revisats». **Treure-la era empitjorar-ho.**

Alhora, la capa **incompleix la regla 2 del Josep** (memòria `feedback_wizard_ui_rules_josep`): «cap panell flotant que aparegui sol». O sigui que la solució ha de fer **les dues coses**: que els camps siguin visibles com a pendents, i llavors treure el panell.

**Opcions plantejades al Josep, cap triada:** (a) frontend, per a aquests 3 camps `source !== 'user'` es pinta `revisar`; (b) backend, marcar `source: 'revisar'` sempre que el valor sigui heurística/plantilla i no lectura real — més correcte de fons però més radi (cal mirar que el comptador no es dispari); (c) deixar la capa però mostrar-la un sol cop. El Josep va demanar **investigar primer**, i la investigació és aquesta secció.

## 6. Invariants estructurals (no desfer)

- **`automation/lectura/jobs.py`, `Job._docs_total_known`**: la guarda va **dins la branca `else` d'`estimate_remaining()`**, NO a l'arrel de `_recompute_estimate()`. El mode de reserva `projecte` no emet mai `lectura_inici` (`docs_total` es queda a 0 tota la vida del job) però sí crida `lector_imatges_inici/fi`, que arriben amb `state == IMATGES` i una branca pròpia; una guarda a l'arrel els hauria deixat sense estimació. Verificat reproduint-ho.
- **No derivis `_docs_total_known` de `docs_total > 0`**: després de `lectura_inici`, `docs_total == 0` és un resultat **legítim** (`n_claude=0`). Confondre «sabut i és zero» amb «encara no sabut» era el bug.
- **`automation/lectura/job_text.py`**: `estimate = _round_estimate(remaining) if basis else None`. `basis` buida = mai calculat. No hi posis condicions sobre el número.
- **Hi ha DUES `_split_utm`**: `contract.py:371` només adapta fixtures d'or als tests (`adapt_legacy`, usada per `mesura_informe.py` i `compare_consolida.py`); **la via real de producció és `consolidate.py:1082`**. Totes dues deleguen a `normalize_utm_xy_decimal`. Si en toques una, toca l'altra.
- **`normalize_utm_xy_decimal` és específica d'X/Y UTM**: el llindar `_UTM_XY_MIN_PLAUSIBLE` (10.000) no val per a Z ni decimals petits (`"198.9"` → `"1989"`). No la reutilitzis.
- **`_updateUtmWarning` (`review.html:6461-6474`) assigna `textContent` SEMPRE**, buit inclòs. Els camps UTM són nodes DOM **estàtics** compartits per tots els projectes: si només l'assignes quan hi ha problema, l'avís d'un projecte es queda visible sobre el següent.
- **`job_text._reading_found_nothing()`**: `total > 0 and failed >= total`. Mai `done` ni `errors` (dues variants ja van caure a revisió).
- **`automation/geocode_coordinates.py`** és via B de producció: es llegeix, mai es modifica.
- **`web/wizard_service.py::_values_equal`** i «un valor buit del formulari no promou mai a canvi»: decisió explícita del Josep amb cost conegut. No ho «arreglis» sense parlar-ho.

## 7. Regles d'interpretació (no òbvies)

- **La suite té 31 vermells esperats** (`docs/wizard-headless/mesures/suite-vermells-esperats.txt`). Compara'ls **per NOM**, en els dos sentits. Extreu-los amb `awk '/^_+ .* _+$/'`; `grep '^FAILED'` trunca els parametritzats llargs.
- **`test_cancellation_mid_flight_kills_live_process` és inestable des d'abans i NO és dels 31** (~30 % de fallades amb les 8 CPU saturades). Mira `uptime` abans de dir-ne res.
- **Les ALERTA del comparador NO són errors per defecte.** A Rubí n'hi va haver 5 i **cap era un error de lectura**: són l'artefacte «prod més confiat que l'or». Obre-les una per una abans de comptar-les com a problema. Exemple: a `architect_name` el sistema **no** va caure al parany que l'or mateix anota, va proposar un candidat amb cita literal i nota explicativa.
- **El test de paritat JS/Python de les UTM SE SALTA EN SILENCI sense Node** (verificat: 14 SKIPPED, exit 0). En una màquina sense Node es pot trencar la paritat i la suite surt verda. **Decisió pendent del Josep**: fer-lo fallar o deixar-ho documentat. A més, la taula de casos està **duplicada a mà** entre el fitxer Python i el JS, lligada només per un comentari.
- **L'estimació del botó és conservadora a posta** (compta documents sense md5). Que trigui menys és el comportament esperat: Rubí «uns 35 min» → 27 min reals.

## 8. Procediments

**Suite (10 min de marge, comparar noms):**
```bash
cd /home/josep/projects/claudecode-job/clients/g3dt-prod
timeout 600 .venv/bin/python -m pytest tests -q -p no:cacheprovider > /tmp/suite.txt 2>&1; tail -1 /tmp/suite.txt
awk '/^_+ .* _+$/' /tmp/suite.txt | sort > /tmp/vermells.txt
diff <(sort /tmp/vermells.txt) <(grep -v '^#' docs/wizard-headless/mesures/suite-vermells-esperats.txt | sort)
```

**Servidor del clon WSL** (ara ATURAT; cal el HOME net, i `run_in_background`, **mai** `&` dins d'una cadena amb assignacions):
```bash
HOME=/home/josep/g3dt-release-wsl-test/home \
  bash -c 'cd /home/josep/g3dt-release-wsl-test/app && exec .venv/bin/python -m uvicorn web.server:app --host 127.0.0.1 --port 8766'
```
Verifica sempre el HOME real del procés de python (no el del bash embolcall):
`tr '\0' '\n' < /proc/<pid>/environ | grep ^HOME=`

**Credencials abans de qualsevol run** (memòria `reference_oauth_credentials_copied_between_homes`): copia-les **just abans** i verifica-ho amb una crida real, no només amb `auth status`:
```bash
cp -p ~/.claude/.credentials.json /home/josep/g3dt-release-wsl-test/home/.claude/
HOME=/home/josep/g3dt-release-wsl-test/home claude -p "Respon només: OK" --output-format json
# ha de dir is_error: false. `claude login` NO existeix: és `claude auth login`.
```

**Comparar la lectura amb l'or:**
```bash
D="/home/josep/g3dt-release-wsl-test/workspace/<PROJECTE>/validation/lectura/_decisions.json"
.venv/bin/python docs/wizard-headless/fase0-acceptacio/compare_consolida.py escalars "$D" "<PROJECTE>"
.venv/bin/python docs/wizard-headless/fase0-acceptacio/compare_consolida.py taules  "$D" "<PROJECTE>"
```
El tercer argument és **el nom del projecte**, no una ruta (l'or viu a `docs/golden-read/<PROJECTE>/`).

**Cau:** la lectura cacheja per document i per md5 a `validation/lectura/` del workspace. Repetir un projecte ja llegit és quasi instantani (bo per a UX, inútil per a mesurar temps). Per mesurar el rellotge des del segon zero cal un projecte **que el sistema no hagi vist mai**.

**Memòria de la màquina:** 7,4 Gi en total. Avui el sistema va matar el servidor del clon per falta de memòria amb la suite + subagents + lectura alhora. Si has de fer proves llargues, no ho encavalquis.

## 9. Seqüència d'obertura suggerida

1. `git branch --show-current` a `g3dt-prod/` i comunica-ho al Josep en una línia (directiva del CLAUDE.md del projecte).
2. Comprova els quatre HEAD de §2 i que `production/g3dt-eva-v1` segueix a `123b4f2`.
3. Llegeix `STATUS.md` i les dues troballes de `docs/troballes/`.
4. **Pregunta al Josep per quina de les dues decisions vol començar** i amb quina opció. Les dues estan investigades i documentades; el que falta és la seva tria, no més anàlisi.
5. Abans de tocar codi, mesura la línia base (memòria `feedback_measure_baseline_before_coding`).
6. Segueix el patró implementer → reviewer → tester (`~/T4/agents-teams/IMPLEMENTER-REVIEWER-TESTER-PATTERN.md`): el main orquestra i **no arregla codi ell mateix**; les troballes tornen a l'implementer; no saltis al tester amb un «looks good» vague. Avui aquest bucle va tombar dues solucions que semblaven bones i va destapar que la via de producció de les UTM era un fitxer diferent del que jo creia.

## 10. Què NO fer

- **No proposar mai un `pull` o un `merge` a l'Eva** (memòria `feedback_no_pull_eva_success_criterion`).
- **No avançar `production/g3dt-eva-v1`** ni fer `--force`/`reset` de res pujat.
- **No treure la capa «Observacions de camp» sense resoldre abans el comptador**: seria empitjorar-ho (§5).
- **No fer servir `window.confirm`/`alert`** a la UI: bloqueja Playwright i és lleig per a l'Eva; la porta suau és inline.
- **No committejar** `schemas/formats/learned/*` (es reescriuen a cada «Generar») ni `docs/diagnostics/*` (els genera la suite).
- **No esborrar `.playwright-mcp/`** ni cap estat de proves que no hagis creat tu en aquesta sessió (memòria `feedback_no_rm_test_state_you_did_not_create`). El projecte de prova `9999999 PROVA-AUTH-EFICIENTS` és del Josep: **pregunta abans d'esborrar-lo**.
- **`git checkout -- <fitxer>` per desfer una prova temporal s'endú també la feina no committejada.** Va passar ahir i va tenir sort que fos reproduïble. Fes un commit de treball abans de qualsevol prova destructiva. I **mai `git stash` pelat**: la pila és compartida entre worktrees.
- **Mai `pkill -f` amb un patró que també casi amb la teva pròpia ordre.** Mata per PID tret de `ps -eo pid,args`.

## 11. Tasques obertes

- [ ] **Decisió 1:** contradicció «solar pla» vs. vessant (§4). Investigada, no tocada.
- [ ] **Decisió 2:** els 18 de 64 camps i la capa «Observacions de camp» (§5). Investigada, no tocada.
- [ ] Test de paritat JS/Python que se salta sense Node: fer-lo fallar o documentar-ho (§7).
- [ ] Actualitzar el clon WSL amb el merge de les UTM i tornar a arrencar el servidor quan calgui.
- [ ] Icona «Tornar a entrar a Claude» a l'escriptori de l'Eva: únic pendent **manual** del dia de la visita (`scripts/G3DT-Tornar-a-entrar-a-Claude.bat` existeix i la seva lògica està verificada, però el `.bat` mai s'ha executat).
- [ ] Decidir si es conserva el projecte de prova `9999999 PROVA-AUTH-EFICIENTS`.
- [ ] Defectes menors que queden de la llista original: «Figura - Projecte 2» repeteix la raó de la figura 1; el comptador de la barra no baixa fins al desat.
- [ ] `docs/GUIA-EVA-WIZARD.md` desfasada (descriu la via B): substituir-la per `docs/COM-FUNCIONA-EVA-2026-09.html`.
- [ ] Seguiments no bloquejants de la suite: test d'integració de reintent; `test_cancellation_mid_flight_kills_live_process`.

## 12. Guanys que no surten a cap mètrica

- **El sistema va passar la prova que importava.** Un projecte que no havia vist mai, de punta a punta, sense claus API, en 27 minuts i sense un sol valor erroni dit amb confiança. Això és el que calia saber abans de posar-ho a mans de l'Eva.
- **Dues avaries silencioses menys.** Cap de les dues feia fallar res de manera visible: el rellotge només minava la confiança, i la coma decimal es menjava una coordenada i deixava l'informe sense la consulta geològica sense que ningú s'assabentés. Les avaries que criden es troben soles; aquestes no.
- **Un defecte n'amagava un altre.** Arreglar les UTM és el que va fer visible la contradicció del solar pla: a Alcoletge mai s'arribava a consultar el pendent. Val la pena recordar-ho: quan es tapa un forat, cal tornar a mirar.
- **El bucle de revisió va guanyar-se el sou tres vegades:** va tombar dues guardes que semblaven bones (una fallava en projectes petits, l'altra donava alarma falsa en lectures perfectes), va descobrir que la via de producció de les UTM era un fitxer diferent del que deia la troballa original, i va detectar que la rèplica JS i el Python **ja havien divergit** amb hores de vida.
- **El patró de fons val més que els tres arranjaments** (§3). Si la sessió nova només s'endú una cosa d'aquest document, que sigui aquesta.

*Fi. Handoff 2026-09-17 vespre: dues avaries silencioses tancades, dues decisions grosses damunt la taula, i el fil que les uneix.*
