# FOR NEW YOU — 2026-08-23 22:00 — Via A: hold-out ERR=0, DWG, n/a i Fase 4a tancats; toca wizard headless + UI de candidats

**Escrit:** 2026-08-23, 22:00 · **Per a:** la sessió que continua la via A.
**Recap de la sessió:** els 4 primers pendents del handoff de les 20:15 estan TANCATS, en aquest ordre: (1) hold-out headless
del skill → **ERR = 0 mesurat** (3 projectes, 3 agents frescos; skill v0.4→v0.5); (2) conversor DWG viable (LibreDWG compilat,
`scripts/dwg_text_dump.py`; skill v0.6) i **misteri del 564 resolt: era el Cadastre, no el DWG**; (3) n/a de Vilanova/Anciles
tancats amb els PDF dels informes (tenen capa de text!) → totals lectura d'or **81 OK / 19 CAND / 10 NT / 8 n/a / 2 ERR**;
(4) **Fase 4a feta**: `automation/g3_templates.py` (lectors deterministes de les 5 plantilles G3, validats 8/8, 9 tests).

## 1. Ordre de lectura (15 min)

1. Aquest document.
2. `STATUS.md` § "Via A en curs" — l'estat sencer amb els 4 tancaments d'avui i el pendent.
3. `docs/holdout-headless/_RESULTATS.md` — el veredicte del hold-out (taula per camp, caveat de leakage, feedback→v0.5).
4. `.claude/commands/g3dt-llegir-projecte.md` **v0.6** — el producte. Header amb el changelog v0.4→v0.6.
5. `docs/DECISION-LOG.md` entrada 2026-08-23 (Fase 4a) — per què `g3_templates.py` és com és.
6. `docs/DWG-CONVERSOR-2026-08-23.md` — recepta d'instal·lació + què porta de veritat cada DWG.
7. `docs/ANALISI-NIVELL-A-LECTURA-HUMANA-2026-08-23.md` §11 + **§11.1** (addendum amb els totals vigents).
8. MEMORY: `project_via_a_claude_code_reader` (cronologia nit 1-5), `feedback_no_pull_eva_success_criterion`,
   `feedback_tier_a_extraction_first`.

## 2. Regles interpretatives crítiques

- **La mètrica que mana segueix sent ERR-amb-confiança = 0, i ara està MESURADA**: el hold-out (3 agents frescos, skill a
  cegues, sense referències) va donar 0 ERR i només divergències prudents (segur→candidats). OK% = 79,4 % (sota el 80):
  és el preu correcte — les cel·les que van baixar eren multiplicitat real, no mandra.
- **Caveat de leakage (honestedat obligada)**: el skill anomena projectes del corpus (BUNYESC/Linyola, Castellar, Anciles,
  Tulipa) en regles concretes. El hold-out valida EXECUTABILITAT headless i re-execució amb ERR=0 — NO generalització.
  Generalitzar només es mesura amb carpetes noves de l'Eva. No venguis el hold-out com a més del que és.
- **Les referències (`eva_reference_values.json`) han caigut 2 cops**: Tulipa (informe generat nostre) i Vilanova
  (lab_depth "-1,00/-1,60" era slot desalineat; l'informe real diu 0,80-1,40). Regla: davant conflicte
  carpeta-unànime vs referència, sospita primer de la referència. Els informes reals en PDF (`PDF/`, `PDF_V0/`)
  TENEN capa de text — "el PDF és imatge" només val per a les pàgines d'annex.
- **El 564 de Tulipa NO és a cap fitxer de la carpeta**: és `areaValue` del WFS INSPIRE del Cadastre per a la RC
  3445105DF2934E0001GG, i la RC es llegeix del caixetí del TOP.dwg. Lectura (RC del DWG) vs derivació (WFS) són capes
  separades: el skill llegeix, Python deriva. No barregis els dos rols.
- **El DWG de paràmetres urbanístics porta superfícies de PLANEJAMENT** (358,75/491,24 — divisió proposada), que NO
  competeixen amb la superfície cadastral. Etiqueta-les sempre com a planejament.

## 3. Supòsits estructurals que aguanten el pes

| On | Què | No desfer |
|---|---|---|
| `automation/g3_templates.py` | Fase 4a: detecció per CONTINGUT (mai nom), CLIENT: emès a 0,3 amb nota "sol·licitant", bloc G3 = `NOT_client_name` | l'anti-"client=G3" és a la font; cap consumidor l'ha de re-derivar |
| `tests/test_utf8_read_text.py` (guard F1) | prohibeix `read_text`/`write_text` sense `encoding=` a `automation/` i `web/` | va caçar el bug cp1252 al mòdul nou d'avui; escriu SEMPRE amb `encoding="utf-8"` |
| `~/.local/bin/dwg2dxf` (LibreDWG 0.13.3 compilat) | conversor DWG d'aquest WSL; el font era al scratchpad (efímer) | recepta de rebuild a `docs/DWG-CONVERSOR-2026-08-23.md` §1; a Windows d'Eva NO existeix |
| `docs/golden-read/*/_decisions.json` + `comparison_addendum_2026-08-23` | correccions per addendum datat, mai reescrivint | append-only; el §11.1 de l'ANALISI és qui porta els totals vigents |
| `docs/holdout-headless/` | sortides dels 3 agents del hold-out (59 JSON + 3 decisions) | evidència del GO; no "millorar-los" a posteriori |
| Skill v0.6 header | changelog per versió amb el perquè | si el toques: puja versió + una línia de motiu |
| `automation/ai_pipeline/`, `web/vision_groq.py`, `auto_extractor.py`, `web/vision_fast.py` | fallback via B intacte | segueix PROHIBIT tocar-los en aquesta línia |
| Branca `experiment/nivell-a-2026-08` in-place a `g3dt-prod/` | tota la via A | `production/g3dt-eva-v1` intacta; MAI pull/merge a l'Eva |

## 4. Procediments operatius

```bash
# Estat esperat en obrir
git branch --show-current   # → experiment/nivell-a-2026-08
git log --oneline -4        # → 95d42f7, f007910 (Fase 4a), 68495ad, 2a1dd54 (n/a)

# Fase 4a sobre un projecte (0 $, ~2-5 s)
.venv/bin/python -m automation.g3_templates "/mnt/c/claude/g3dt/projectes/4001612 BELL-LLOC" [--out FILE]

# Text d'un DWG
.venv/bin/python scripts/dwg_text_dump.py FITXER.dwg [--json OUT]

# Superfície cadastral des d'una RC (el que fa "Eva" per a superficie_parcela)
curl -s "https://ovc.catastro.meh.es/INSPIRE/wfsCP.aspx?service=WFS&version=2.0.0&request=GetFeature&STOREDQUERIE_ID=GetParcel&refcat={RC14}" | grep areaValue

# Tests del mòdul nou / suite completa (timeout 10 min; línia base: 32 failed IDÈNTIQUES / 1088 passed / 3 skipped)
.venv/bin/python -m pytest tests/test_g3_templates.py -q
.venv/bin/python -m pytest tests/ -q
```

- `.venv` és **uv, sense pip**: instal·lar amb `uv pip install -p .venv/bin/python PAQUET`. Té `ezdxf` 1.4.4 des d'avui.
- Fonts de dades: `/mnt/c/claude/g3dt/projectes/{7}` i `projectes-debug/3001706 C.TULIPA CERDANYOLA` (MAI
  `reference-material/` per a dades; els tests sí que usen reference-material com a fixtures — és l'excepció pactada).
- Els informes reals de l'Eva, quan calguin: `{projecte}/PDF/{exp}_informe.pdf` o `PDF_V0/` (text llegible amb fitz).
- Patró d'agents frescos per a hold-outs futurs: 1 agent/projecte amb el skill com a únic playbook + prohibicions
  explícites (validation/, golden-read/, informes, memòria); el prompt exacte és als transcripts d'avui i reproduïble
  des de `docs/holdout-headless/_RESULTATS.md` §1.

## 5. Seqüència d'obertura suggerida

1. `git branch --show-current` + `git log --oneline -4` (contra §4). Si no quadra, atura't i pregunta.
2. Llegeix §1 (sobretot `_RESULTATS.md` i el skill v0.6).
3. El pendent gros és UN disseny amb dues cares: **crida headless des del wizard + UI de candidats**. Abans de codificar,
   escriu el pla: seqüència SSE (g3_templates < 5 s → camps segurs; `claude -p` amb el skill → candidats amb cita),
  reutilitzant el patró de `web/vision_fast.py:172-182` (`claude -p … --permission-mode bypassPermissions`) SENSE tocar
   el fitxer; i els 3 estats de la UI (segur / candidats+popup amb cites / no_trobat+on s'ha buscat). El badge "+N"
   actual no té senyal de dubte — això és el que la UI ha de resoldre.
4. Multi-informe (Tulipa) és canvi de model de dades del wizard: anotat, NO dissenyat. Si l'abordes, decisió explícita
   amb el Josep primer.
5. Commit per peça (`docs(g3dt): …` / `feat(g3dt): …`), com fins ara.
6. Si toques el skill: versió +0.1 al header i una línia de perquè.

## 6. Què NO fer

- No proposar pull/merge/desplegar a l'Eva (memòria `feedback_no_pull_eva_success_criterion`). Ni "mentrestant".
- No tocar `automation/ai_pipeline/`, `web/vision_groq.py`, `auto_extractor.py`, `web/vision_fast.py` (reutilitzar
  patrons llegint-los, sí; editar-los, no).
- No re-executar la lectura d'or ni "polir" `docs/golden-read/` o `docs/holdout-headless/`: són evidència tancada.
  Correccions = addendum datat.
- No presentar el hold-out com a validació de generalització (leakage, §2). No convertir CAND en segur sense 2 fonts.
- No escriure a `reference-material/` ni a les còpies de Windows; no commitar les 119 supressions de `mined_images`
  ni `docs/diagnostics/ai_pipeline_demo-project_*.md` ni `photo_selection.json` (deixats a propòsit).
- No usar "cost a volum" per triar models (Eva és l'única usuària); no presentar millores residuals com a resultat.
- No `write_text`/`read_text` sense `encoding="utf-8"` — el guard F1 et suspendrà la suite (i té raó).

## 7. Pendents (ordre recomanat)

1. **Disseny crida headless wizard + UI de candidats** (§5.3) — l'últim tram tècnic de la via A. Inclou: com el wizard
   invoca `claude -p` amb el skill per document (`--only`), timeout+fallback+telemetria, i com la UI mostra els 3 estats.
2. **Validació amb projectes nous de l'Eva** (quan n'hi hagi): el primer test real de generalització del skill.
3. **Desplegament a casa l'Eva** (decisió Josep): CLI Windows + login + flag; DWG a Windows = build LibreDWG Windows o
   conversió en servidor (pendent de decidir); presencial.
4. Menor: el candidat `municipality` de PLAN_COST E9 usa regex `EG {tipus} {municipi}` (conf 0,6) — si un títol no
   estàndard apareix, baixar confiança, no sobre-enginyar.

## 8. Guanys ocults (no surten a cap mètrica)

- **El skill és executable per agents sense context**: 3 agents frescos el van seguir de cap a cua amb 0 ERR — la millor
  evidència que `claude -p` a casa de l'Eva funcionarà. El feedback dels agents (10 punts) ja està absorbit a v0.5.
- **La cadena TOP.dwg → RC → WFS Cadastre** tanca l'últim forat de `superficie_parcela` sense geocodificar ni visió.
- **`g3_templates.py` talla l'error "client=G3" a la font** i dona els camps segurs en < 5 s — és la meitat "instantània"
  del wizard futur (SSE: primer Python, després Claude).
- **El guard F1 ha demostrat que funciona**: va caçar el mateix bug cp1252 que va matar Can Mir Rubí, aquest cop abans
  de sortir del dev. El sistema de qualitat s'aguanta sol.
