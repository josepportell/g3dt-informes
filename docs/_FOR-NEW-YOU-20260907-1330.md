# FOR NEW YOU — 2026-09-07 (suffix 1330) — IMATGES: pas 1 FET (veritat, inventari, aparellament, fulls, document). Ara toca el PAS 2: revisió figura a figura amb el Josep

**Escrit:** 2026-09-07 (tarda), en acabar el pas 1 del pla de `_FOR-NEW-YOU-20260907-1130.md` (llegeix-ne § 3 «El pla» per al marc dels 3 passos).
**Per a:** la sessió que farà el pas 2 (revisió amb el Josep, sense codi) i prepararà el pas 3 (disseny per tipus de figura).
**Recap d'una frase:** els 7 signats porten les imatges amb el seu peu; les hem extret, hem inventariat els 291 candidats de cada projecte (FH11
inclosos), les hem aparellat per hash i les hem mirat una a una; el resultat és `docs/imatges/INVENTARI-I-VERITAT-2026-09-07.md` (700 línies) amb
7 fulls a tres columnes, i diu on som (fotos iguals 12/23, figures 0/…) i què posa l'Eva a cada ranura. Cap canvi a `automation/` ni a la plantilla.

Branca `experiment/nivell-a-2026-08`, in-place a `g3dt-prod/` (estat esperat). Últim commit `ef86db2` (bloc 4). **`docs/imatges/`, `STATUS.md` i la
sessió del dia NO estan commitejats** (GO del Josep pendent; aquest handoff sí que es commiteja sol). `production/g3dt-eva-v1` @ `c46bc69`, sense push.
Mai proposar pull/merge a l'Eva (memòria `feedback_no_pull_eva_success_criterion`).

## 1. Ordre de lectura (abans de tocar res)

1. Aquest document.
2. `docs/imatges/INVENTARI-I-VERITAT-2026-09-07.md`: **§0** (10 punts), **§6** (què fa l'Eva per tipus, 10 subseccions), **§7** (taula A/B/C i preguntes
   candidates 30-36), **§8** (direcció del pas 3). §4 i §5 són la referència per figura i per fitxer: consulta-les, no cal llegir-les senceres.
3. Els 7 fulls `docs/imatges/fulls/<projecte>.jpg` (Read, un per un): columna 1 = l'Eva, 2 = nosaltres (bloc 4b `viaA`), 3-5 = candidats amb hash. És el
   material amb què es fa el pas 2; el Josep els ha de tenir davant.
4. `STATUS.md` (entrada «2026-09-07 (tarda-2)») i `.claude/sessions/2026-09-07-session.md` («Tarda (sessió 2)»).
5. Memòria: `report_figure_sources` (REESCRITA: el «plànol sense punts» era només Bell-lloc) i `project_imatges_pas1_2026-09-07`.
6. `_FOR-NEW-YOU-20260907-1130.md` § 3 (els 3 passos) i § 5 (què no fer) i `_FOR-NEW-YOU-20260907-1100.md` § Obert (decisions del bloc 4 encara obertes:
   bloc de figures 2/3/4, foto de materials per nivell, `_cache_name` a producció).
7. `docs/PREGUNTES-EVA-PENDENTS.md` (última: 29; les 30-36 del document són candidates, no s'hi han afegit).
8. `docs/imatges/notes-visuals-2026-09-07.md` (esborrany de la lectura visual, per si cal la versió curta).

## 2. Regles d'interpretació (no es veuen al codi)

- **La veritat és el signat, no la plantilla.** La plantilla `g3dt-jinja-template.docx` ÉS el signat de Rubí: les 11 imatges que porta són de Rubí; només
  3 es referencien (logo, segell G3 473×210, i el **pastís de granulometria de Rubí** dins `{%p if show_granulometric %}`, peu «Gràfic 1…»). Si algú activa
  `show_granulometric`, un altre projecte porta la granulometria de Rubí (50,3/31,5/18,2): risc de fabricació, encara a la plantilla.
- **Ranures de l'Eva ≠ ranures de la plantilla.** Ella posa: `situació` (1 o 2 imatges, dos mapes compostos) + `projecte` (0-2, «Font: Projecte») +
  `assaigs` (amb punts, 6/7) → 2/2/3/3/2/3/4 figures abans de la cullera. La plantilla té `cadastre` + `aèria` + `plànol` fixes. **`fig_aerea` no existeix
  a cap signat** (l'orto va dins situació o dins assaigs). Bell-lloc és l'únic sense figura d'assaigs ni punts.
- **Lectura de les columnes de hash (§4):** `ph` ≤ 10 = mateixa imatge sencera (fotos, PNG d'ALTRES); `ncc` és la correlació ponderada per la singularitat
  del pic (PSR); **fort** = ncc ≥ 0,7 i PSR ≥ 6; parelles no relacionades donen 0,34-0,47. El «veredicte hash» és l'opinió del número; **la columna
  «lectura visual (Claude)» és la que val**. Punt feble: dibuixos CAD de línia fina (planta de Bell-lloc, tipologies d'Anciles, planta de Linyola) queden
  a 0,3-0,5 encara que a ull la font sigui inequívoca.
- **«Igual a l'Eva» a la columna nosaltres** = phash ≤ 12 entre la nostra imatge (bloc 4b) i la del signat de la mateixa ranura. Vàlid per a fotos; per a
  figures compostes «≠ Eva» vol dir composició diferent, no necessàriament font diferent (el tall: mateixa font 7/7, retall diferent 7/7).
- **Fonts que NO són al projecte** (i per tant no es poden «trobar»): captures ICGC quan no queden a ALTRES, Google Earth (Rubí Foto 1), Sede del Catastro
  (Anciles Fig 1), IGME (Anciles geològic), figures de llibre (Castellar Fig 6-7). Zero fabrication: es diu «cap font», no s'inventa.
- **Artefactes nostres al corpus:** a Castellar, `FOTOGRAFIES/maquina_dpsh*.jpg`, `detall_materials*.jpg`, `vista_general_1.jpg`, `maquina_sondeig.jpg`
  són còpies renombrades (març) de P1-P4/S1; `M1.png` porta el rol `figure_geological_map` erroni (és el topogràfic). No ensenyen res de l'Eva.
- **Numeració de figures al docx:** Vilanova repeteix un rId (Fotografía 2 dues vegades) i «Fotografía 1» dos cops; Castellar salta la Fotografia 3. Són
  incoherències del signat, ja comptades al bloc 4.

## 3. Supòsits estructurals dels quals depèn el pas 1 (no desfer)

- `docs/imatges/veritat/<slug>/index.json`: una entrada per imatge del signat en ordre de document, amb `slot` (classificador per paraules clau del peu,
  CA/ES), `static_of` (només si phash ≤ 6 contra un media **referenciat** per la plantilla: logo, segell, pastís; la cullera NO és referenciada, la insereix
  el generador, i es marca `fig_spt_cullera`), `caption`/`after`/`heading`. Les imatges a mida real són fora del repo (`~/g3dt-e2e/imatges/veritat/`,
  regenerables). El `.wmf` de Castellar té còpia `.png` (soffice).
- `docs/imatges/scripts/*.py` porten **camins absoluts del scratchpad d'aquesta sessió** (`/tmp/claude-1000/…/a25f4f53…/scratchpad`), que pot no existir.
  Per reexecutar: canvia la constant `S` a un scratchpad nou i refés la cadena de §5 (tot determinista, sense API). `match.py`: no toquis la
  penalització PSR (`eff = ncc * min(1, psr/6)`), el `psr = 99` quan la plantilla és tota la imatge, l'escala nativa 1:1, ni l'`alpha_composite` sobre
  blanc (els PNG d'ALTRES són RGBA): cadascun arregla un fals negatiu o positiu vist.
- `build_tables.py` genera les taules base de §4/§5; la columna «lectura visual» i el mapa curat de §5 els vaig escriure a mà dins el document (no hi ha
  script): **el document és la font**. Si es regenera, cal tornar a encaixar el text.
- `ImageManager._cache_name` (bloc 4, `4afcc80`) continua sent l'únic canvi de codi de l'àrea d'imatges; producció no el té.

## 4. Procediments

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"; git branch --show-current; git status --short | grep -v runs/; git log --oneline -3
# veure els fulls (Read): docs/imatges/fulls/{castellar,rubi,linyola,bell-lloc,alcoletge,vilanova,anciles}.jpg
# regenerar-ho tot (≈ 15 min): signats → docx (soffice --headless --convert-to docx), scripts/truth_extract.py, scripts/fh11_convert.sh,
#   scripts/inventory.py, scripts/match.py (7 processos, ≈ 7 min), scripts/ours_extract.py, scripts/build_sheets.py, scripts/build_tables.py
# aturar match.py: NO `pkill -f match.py` des d'una ordre que esmenti el fitxer (mata la pròpia shell): kill $(cat $S/match.pid) i pkill -P.
# veritat d'una figura a mida real: ~/g3dt-e2e/imatges/veritat/<slug>/NN_<ranura>_<peu>.<ext>
# FH11 → PDF (3-14 s): soffice --headless --convert-to pdf --outdir OUT "X.FH11"   (render fidel; text refluït; diverses pàgines aplanades en una)
```

- Els generats del bloc 4b (`~/g3dt-e2e/informes-mesura/2026-09-07-m341-bloc4b/<slug>_viaA.docx`) són la columna «nosaltres»; si es regenera un run,
  `ours_extract.py` i `nostres/fonts.json` s'han de refer.
- `rtk` reescriu `grep`/`git show`/`git diff`: `rtk proxy …` en pipelines. Mai `str()` d'un `InlineImage` viu. `unzip` no hi és (zipfile).

## 5. Seqüència d'obertura (pas 2)

1. `git branch --show-current` (una línia al Josep), `git status` (has de veure `docs/imatges/` sense commitejar llevat que ell hagi donat el GO).
2. Llegir §0, §6, §7, §8 del document; obrir els 7 fulls.
3. Amb el Josep, **tipus per tipus** (§6.1 → §6.10), decidir A/B/C amb l'evidència del full: situació, aèria (eliminar ranura?), assaigs (annex de l'Eva
   retallat vs. punts per UTM), projecte (Claude tria), vistes, DPSH/sondeig, materials (plantilla: una), geològic (PNG → ICGC → IGME), tall (retall).
   Apuntar cada decisió amb el seu «per què» i les alternatives rebutjades.
4. Decisions de plantilla que arrosseguem del bloc 4: nombre de figures variable (2/3/4), `fig_aerea`, foto de materials per nivell, pastís de Rubí.
5. Preguntes a l'Eva: de les candidates 30-36 (§7), les que el Josep aprovi → afegir-les a `docs/PREGUNTES-EVA-PENDENTS.md` (numerades a partir de 30, mateix
   format que la 29). No enviar res a l'Eva.
6. Escriure l'entrada del DECISION-LOG «2026-09-07/08 — imatges: pas 2 (decisions per tipus)» amb les seccions de sempre (Context, Decisions, Limitacions,
   GO/NO-GO, Següents passos) i actualitzar el bloc «imatges» del PLA (`docs/PLA-QUE-QUEDA-DESPRES-DE-A-B-I-NARRATIVA-2026-09-07.md`).
7. Només llavors, el pas 3: pla per peces mesurables en l'ordre de §8.5 (fotos → tall → assaigs → situació → geològic → projecte → plantilla), amb la
   mesura «mateixa font que l'Eva» a M341 i baseline abans de tocar codi (`feedback_measure_baseline_before_coding`).

## 6. Què NO fer

- Cap codi a `automation/` ni a la plantilla abans de tancar el pas 2 amb el Josep. El pas 1 val precisament perquè és observació.
- No reintroduir el retall del 38 % esquerre de l'annex (`_render_situation_plan_left`) com a «figura de situació»: no coincideix amb cap signat.
- No fiar-se del «veredicte hash» contra la lectura visual, ni citar la NCC a escales < 0,2 (falsos positius).
- No tractar la memòria antiga «plànol SENSE punts» com a regla: ja és reescrita; era Bell-lloc.
- No proposar-li res a l'Eva sense el Josep, i mai un pull.
- No `pkill -f <nom de fitxer>` en una ordre que contingui el mateix nom (mata la shell de l'ordre; ha passat dues vegades).
- No commitejar `docs/imatges/` sense GO; no versionar les imatges a mida real (2-7 MB per projecte: fora del repo, a `~/g3dt-e2e/imatges/`).
- No tocar `reference-material/4001607 LINYOLA/validation/photo_selection.json` (sense versionar, hi era abans d'aquesta sessió; no és nostre).

## 7. Tasques obertes

- [ ] GO del Josep i commit de `docs/imatges/` + `STATUS.md` + sessió (proposta: 1 commit «docs: imatges pas 1 — inventari, veritat i aparellament»).
- [ ] Pas 2: decisions A/B/C per als 9 tipus + 4 decisions de plantilla (§5 punts 3-4 d'aquí) → DECISION-LOG + PLA.
- [ ] Preguntes 30-36 a `PREGUNTES-EVA-PENDENTS.md` (les aprovades).
- [ ] Pas 3: disseny del lector d'imatges (skill G3DT) + llibreria d'exemplars (`docs/imatges/veritat/` + descripció textual per exemplar; `--exclude`
  per al leave-one-out) + eines de render/retall/composició; comprovar abans què fa ja l'MCP plànols amb retalls de plànols.
- [ ] Treure de la plantilla el pastís de Rubí (o substituir-lo per un gràfic generat de les dades del projecte) i els 8 media morts (8,9 MB).
- [ ] Pendents del bloc 4 (handoff 1100 § Obert): bloc de figures 2/3/4, foto de materials per nivell, `_cache_name` a producció, bloc 5 (castellà).
- [ ] M341: cel·la per figura «mateixa font que l'Eva» (phash ≤ 10 o NCC ≥ 0,7) en lloc de presència; el codi de `match.py` es pot reaprofitar.

## 8. Guanys que no surten a cap mesura

- **Els signats són la veritat de les imatges**, i ara està extreta, indexada i mirada: 63 figures/fotos amb peu, ranura i procedència. És la llibreria
  d'exemplars que el Josep va demanar (falta la descripció textual per exemplar).
- **Els FH11 s'han usat per primera vegada com a font**: els renders de `plànol de situació` contenen la figura d'assaigs de l'Eva (Alcoletge 0,72,
  Vilanova 0,98, Anciles 0,70) i els mapes de la de situació; existeixen abans del wizard.
- **L'annex de fotografies de l'Eva** (`*_fotografies.pdf`/`.FH11`) apareix com a segon candidat de gairebé totes les fotos del signat: és la seva selecció
  i pot guiar la tria (o directament ser-ne la font).
- **Tres bugs latents documentats sense tocar-los**: pastís de Rubí a la plantilla, 8,9 MB morts per informe, rol `figure_geological_map` a `M1.png`.
- **Kit d'aparellament calibrat** (phash + NCC fina + PSR + escala nativa): serveix per a la mesura per figura del pas 3 i per a qualsevol «d'on surt
  aquesta imatge».
