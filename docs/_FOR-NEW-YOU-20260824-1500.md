# FOR NEW YOU — 2026-08-24 15:00 — Via A: taules tancades (113/35/1, skill v0.9); toca Tier B per nivell + wizard headless amb UI de candidats

**Escrit:** 2026-08-24, 15:00 · **Per a:** la sessió que ataca els DOS pendents grossos de la via A.
**Recap de la sessió:** el skill `g3dt-llegir-projecte` va aprendre a llegir les TAULES de l'informe (v0.7 regles Pas 3b →
v0.8 anatomia de l'annex de sondeig → v0.9 lliçons de la lectura d'or) i la **lectura d'or de taules 8/8 està feta i
tancada**: 8 agents cecs, 6 projectes comparats amb informes signats → **113 OK / 35 CAND / 1 ERR sobre 150 cel·les
(OK+CAND-encertat 98,7 %)**. L'ERR (cota de sondeig) ja és regla. Commits: `62f1345` (v0.7) → `beef982` (v0.8) →
`c2aa8a2` (lectura) → `c4bced5` (v0.9) → `d89c8ea` (STATUS).

## 1. Ordre de lectura (15 min)

1. Aquest document.
2. `STATUS.md` § "Via A en curs" — estat sencer amb la lectura de taules tancada.
3. `docs/golden-read-taules/_RESULTATS.md` — el veredicte complet (totals, l'ERR, regles noves, caveats).
4. `.claude/commands/g3dt-llegir-projecte.md` **v0.9** — el producte; Pas 3b és la part nova.
5. `docs/DECISION-LOG.md` entrada 2026-08-24 — el perquè de cada decisió de taules.
6. `docs/_FOR-NEW-YOU-20260823-2200.md` §5.3 — el disseny previ del wizard headless (encara vigent, ara s'hi suma `tables`).
7. Per a Tier B: `docs/METODOLOGIA-EVA.md` + memòries `eva_methodology_calculations`, `implementation_status_calcs`,
   `project_crespo_tabla_11_2`.

## 2. Els DOS pendents grossos (l'encàrrec d'aquesta sessió)

### Pendent A — Tier B per nivell: K, coef. C sísmic, γ/c/φ/E

Les 4 taules de l'informe que el skill NO llegeix per disseny (permeabilitat, sísmica, geotècnica, qualificació de
sulfats) es DERIVEN dels inputs que el skill ja dona bé (litologies, fondàries de transició, Nb/registres, N30).

- **El dataset de validació ja existeix**: `docs/golden-read-taules/_eva_truth/*.json` porta les graelles EXACTES
  d'aquestes 4 taules per als 6 projectes amb informe (grids `permeabilitat`, `sismica`, `geotecnica`, `sulfats`).
  És l'or contra el qual calibrar qualsevol mapping material→K, nivell→Tipus terreny/C, Nb→γ/c/φ/E.
- **Fonts del criteri d'Eva**: `docs/METODOLOGIA-EVA.md` (Crespo per c/φ — àncora φ=28° per transicionals, Tabla 11.2
  p.175; Rodríguez Ortiz bicapa; Schmertmann E; taula de valors típics a la memòria `eva_methodology_calculations`).
  Calculadores existents: `automation/terzaghi_calculator.py` i les rutes de `report_generator.py` (`_calc_*`).
- **Regla d'enfocament**: els 41 MISMATCH de càlcul del diagnòstic són JUDICI d'Eva (E=650 per graves carbonatades, Qa
  per sobre del càlcul) — el wizard té overrides. L'objectiu de Tier B NO és clavar el 100 %: és proposar candidats/defaults
  per material amb la font del criteri, i deixar l'override. No presentar-ho mai com a "càlcul exacte".
- Detall que ajuda: la sísmica d'Eva usa com a gruix del nivell únic la profunditat del punt més fondo (Rubí: 4,55 = P-1)
  i marca amb `*` el gruix del nivell obert ("es poden atribuir potencies superiors").
- Sulfats: la QUALIFICACIÓ (No Agressius / Qa segons CE-21) és derivació del valor GTL; la FILA (quin nivell) ja la llegeix
  el skill (`mostra_del_nivell`).

### Pendent B — Crida headless des del wizard + UI de candidats

L'últim tram tècnic de la via A. El disseny previ (handoff 23/2200 §5.3) segueix vigent; ara més ric:

- **Seqüència SSE en 2 temps**: (1) `automation/g3_templates.py` < 5 s → camps segurs instantanis; (2) `claude -p` amb el
  skill v0.9 per document (`--only FITXER`, una crida per document = forma headless de producció) → candidats amb cita
  + bloc `tables`. Reutilitzar el patró de `web/vision_fast.py:172-182` (`claude -p … --permission-mode bypassPermissions`)
  SENSE editar aquest fitxer.
- **3 estats a la UI**: segur / candidats (popup amb valors, font i cita — el badge "+N" actual no té senyal de dubte) /
  no_trobat (mostrant `sources_checked` + proposta). Ara també per a FILES de taula: n30 amb registre i sumes candidates,
  litologies amb 2-3 redaccions, NF humitat|aigua.
- **Sortida del skill que la UI consumeix**: `_decisions.json` (Pas 5) — 15 escalars + bloc `tables` amb els mateixos
  3 estats per fila. Els JSON reals dels 8 projectes són a `docs/golden-read-taules/*/` i `docs/golden-read/*/` — useu-los
  com a fixtures del disseny (dades reals, no inventades).
- Timeout + fallback + telemetria per crida; si `claude -p` falla, el wizard té els camps de g3_templates i la via B vella.
- **Multi-informe (Tulipa)**: canvi de model de dades del wizard — NO dissenyat; decisió explícita amb el Josep abans.

Ordre recomanat si cal triar: primer el DISSENY del Pendent B (document, sense codi) perquè fixa el contracte de dades;
Tier A ja alimenta; Tier B pot anar en paral·lel o després perquè només afegeix files derivades al mateix contracte.

## 3. Regles interpretatives crítiques

- **La mètrica que mana: erroni-amb-confiança = 0.** La lectura de taules en va tenir 1 (cota sondeig Castellar), ja
  convertit en regla v0.9 (la cota de la taula segueix el SISTEMA de cotes del projecte; sistema mixt → candidats).
- **n30 MAI segur**: el criteri de suma d'Eva és INESTABLE (Rubí/Alcoletge/Anciles = trams centrals del registre;
  Bell-lloc informe 54 ≠ tall 58 ≠ centrals 62). PREGUNTA OBERTA per a l'Eva — no la resolguis unilateralment.
- **Litologia mai segura per a la cadena literal**: l'Eva re-redacta, i fins i tot les dues còpies del mateix annex
  discrepen (Tulipa). Annex de sondeig «Descripció dels materials» primer, tall segon.
- **Leakage**: la lectura d'or (escalars i taules) valida executabilitat i re-execució, NO generalització. No la venguis
  com a més. El test real = carpetes noves de l'Eva.
- Les referències (`eva_reference_values.json`) han fallat 2 cops; davant conflicte carpeta-unànime vs referència,
  sospita primer de la referència. La veritat d'Anciles és un V0 (placeholders); Vilanova NO té cos d'informe (n/a).
- Formats d'Eva inestables entre informes ("1/--" vs "1/0" vs "1/0/0"; suma vs components de superfície; capçalera
  "Humitat (m)"): el LECTOR emet dades i matisos; el GENERADOR formata.

## 4. Supòsits estructurals que aguanten el pes

| On | Què | No desfer |
|---|---|---|
| `scripts/compare_tables_vs_eva.py` + `docs/golden-read-taules/_eva_truth/` | arnès de validació de taules (empremta de capçalera + clau de fila) | és l'eina de re-validació del wizard futur; criteris consistents amb el comparador escalar |
| `docs/golden-read-taules/` (8 carpetes + `_comparison.json` + `_RESULTATS.md`) | evidència TANCADA de la lectura d'or de taules | append-only; correccions = addendum datat, mai reescriure |
| Skill v0.9 header | changelog per versió amb motiu | si el toques: +0.1 i una línia de perquè |
| `automation/g3_templates.py` | camps segurs < 5 s; anti-"client=G3" a la font | la meitat instantània del wizard futur |
| `tests/test_utf8_read_text.py` (guard F1) | prohibeix read/write_text sense `encoding=` | escriu SEMPRE `encoding="utf-8"` |
| `automation/ai_pipeline/`, `web/vision_groq.py`, `auto_extractor.py`, `web/vision_fast.py` | fallback via B intacte | PROHIBIT editar-los (llegir-ne patrons, sí) |
| Branca `experiment/nivell-a-2026-08` in-place a `g3dt-prod/` | tota la via A | `production/g3dt-eva-v1` intacta; MAI pull/merge a l'Eva |

## 5. Procediments operatius

```bash
# Estat esperat en obrir
git branch --show-current   # → experiment/nivell-a-2026-08
git log --oneline -6        # → d89c8ea … 62f1345 (cadena de taules)

# Arnès de taules: comparar un docx generat (futur wizard) amb la veritat d'Eva
.venv/bin/python scripts/compare_tables_vs_eva.py GENERAT.docx docs/golden-read-taules/_eva_truth/bell-lloc.json --tag NOM
# (accepta .docx o .json de veritat; Alcoletge .doc es converteix amb: soffice --headless --convert-to docx --outdir DEST FITXER)

# Camps segurs deterministes (0 $, ~2-5 s)
.venv/bin/python -m automation.g3_templates "/mnt/c/claude/g3dt/projectes/4001612 BELL-LLOC"

# Suite (timeout 10 min; línia base: 32 failed IDÈNTIQUES / 1088 passed / 3 skipped)
.venv/bin/python -m pytest tests/ -q
```

- `.venv` és uv, sense pip: `uv pip install -p .venv/bin/python PAQUET`.
- Dades: `/mnt/c/claude/g3dt/projectes/{7}` + `projectes-debug/3001706 C.TULIPA CERDANYOLA` (MAI `reference-material/`
  per a dades; els tests sí que l'usen com a fixtures — excepció pactada). MAI escriure a /mnt/c.
- Informes reals per consulta: `{projecte}/{exp}_informe.docx` (5), Anciles `PDF_V0/…_V0.pdf` (capa de text), Vilanova
  NO en té amb cos de taules.
- Patró d'agents cecs reproduïble: prompts del 24 als transcripts; prohibicions = informes/generated/validation/
  golden-read*/audit/memòria; escriure sortides a `docs/…/{exp}/`.

## 6. Seqüència d'obertura suggerida

1. `git branch --show-current` + `git log --oneline -6` (contra §5). Si no quadra, atura't i pregunta.
2. Llegeix `_RESULTATS.md` i el skill v0.9 (Pas 3b sencer).
3. Confirma amb el Josep per quin pendent es comença (recomanat: disseny del B, que fixa el contracte).
4. Pendent B: escriu el document de disseny (SSE 2 temps, contracte `_decisions.json` + `tables`, 3 estats UI, timeout/
   fallback/telemetria) ABANS de cap línia de codi; valida'l amb el Josep.
5. Pendent A: comença construint la matriu material→paràmetres des de `_eva_truth/` (6 projectes × 4 taules derivades)
   i creua-la amb METODOLOGIA-EVA; el lliurable és un mòdul nou (p. ex. `automation/tier_b_levels.py`) + tests, sense
   tocar `report_generator.py` fins que el mapping estigui validat contra l'or.
6. Commit per peça, com sempre (`feat(g3dt): …` / `docs(g3dt): …`).

## 7. Què NO fer

- No proposar pull/merge/desplegar a l'Eva (memòria `feedback_no_pull_eva_success_criterion`). Ni "mentrestant".
- No editar els 4 fitxers del fallback via B (§4). No re-executar ni "polir" `docs/golden-read*` — addendums datats.
- No resoldre el criteri N30 tu sol: candidats amb registre fins que l'Eva contesti.
- No convertir CAND en segur sense 2 fonts independents; no presentar Tier B com a càlcul exacte (és candidats + override).
- No usar "cost a volum" per triar models; no presentar millores residuals com a resultat.
- No `read_text`/`write_text` sense `encoding="utf-8"` (el guard F1 suspèn la suite).
- No commitar les supressions pendents de `reference-material/**/mined_images` ni `docs/diagnostics/ai_pipeline_demo-*`
  ni `photo_selection.json` (deixats a propòsit a l'arbre).

## 8. Tasques obertes (ordre)

1. **Pendent B**: disseny crida headless + UI de candidats (amb `tables`) → implementació.
2. **Pendent A**: Tier B per nivell (K, C, γ/c/φ/E) validat contra `_eva_truth/`.
3. Pregunta a l'Eva (quan toqui): criteri de suma N30; sulfats d'Anciles (cap GTL a la carpeta).
4. Validació amb projectes NOUS de l'Eva (primer test de generalització).
5. Desplegament a casa l'Eva (decisió Josep; presencial).
6. Multi-informe Tulipa al wizard (decisió de model de dades amb el Josep).

## 9. Guanys ocults

- L'arnès `compare_tables_vs_eva.py` + `_eva_truth/` converteix "les taules surten bé?" en una ordre d'una línia — val
  per al wizard, per a regressions i per a projectes nous.
- Les deteccions espontànies dels agents cecs (full de camp de Linyola dins del PENETROS de Bell-lloc; p.2 de l'annex de
  Castellar que era de Bell-lloc) mostren que el Pas 0 del skill fa de xarxa de seguretat documental — un argument de
  venda del sistema que cap mètrica recull.
- 23/23 profunditats DPSH exactes amb una regla de 2 línies (peu "Rebuig a"): la lliçó repetida de la via A és que la
  precisió surt de saber ON mira l'Eva, no de models més grossos.
