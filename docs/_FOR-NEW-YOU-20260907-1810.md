# FOR NEW YOU — 2026-09-07 (vespre del 06, 18:10) — Pendents de lectura TANCATS, P2b UI, ASSENTAMENT per criteri, M341 v1 (58 % / 78 %); COMMITEJAT en 5 (GO Josep 18:20); següent (decisió Josep): ANÀLISI DE LA NARRATIVA en dos grups (adjacents + solar / resta), després preguntes 13/14/15, P5, P6

**Escrit:** 2026-09-06, 18:10. Substitueix `_FOR-NEW-YOU-20260907-1705.md` com a punt d'entrada (conserva'l: les regles
d'interpretació dels dos harnesses, les 4 variants i els invariants del bloc 2 continuen vigents i no es repeteixen). Branca
`experiment/nivell-a-2026-08`, in-place a `g3dt-prod/` (estat esperat), **arbre net després dels 5 commits** (vegeu `git log --oneline -6`).

**En una frase:** en una tarda (diumenge, l'Eva descansa) s'han tancat els dos pendents de lectura, s'ha fet visible el nivell
portant al wizard, l'assentament ha passat de fórmula a criteri (frase genèrica en roca/cohesiu, Es amb candidats, plantilla amb
variable), i per primer cop hi ha una mesura COMPLETA reproduïble dels 7 projectes generats només amb la lectura: 58 % dels
escalars i narrativa, 78 % de les taules — i dues peces noves que la mesura ha destapat (P5, P6).

## Ordre de lectura (15 min)

1. Aquest document.
2. `STATUS.md` capçalera (vespre) i §«✅ Pendents de revisió» (tancats, com).
3. `docs/DECISION-LOG.md` entrada **2026-09-06 (vespre)**: 5 decisions (A-E), validació amb les taules, 6 troballes de M341,
   limitacions, següents passos. És l'única explicació completa.
4. `docs/wizard-headless/mesures/runs/2026-09-06-m341/_NOTES.md` (referència viva de M341) i `_AGREGAT-341.md` §«Per VARIABLE».
5. `automation/settlement_criteria.py` (docstring = els 5 criteris) i `docs/PREGUNTES-EVA-PENDENTS.md` files 13, 14, 15.
6. Memòria: `project_m341_and_settlement_2026-09-06`.

## Estat del repositori: COMMITEJAT en 5 (GO del Josep, 2026-09-06 18:20) — la partició que es va fer

| # | Commit | Fitxers |
|---|---|---|
| 1 | lectura: or d'Alcoletge + D4b + comparador v5 | `automation/lectura/consolidate.py`, `docs/wizard-headless/fase0-acceptacio/compare_consolida.py`, `docs/golden-read-taules/4001670 ALCOLETGE/_tables_decisions.json`, `tests/test_lectura_consolidate.py`, `tests/test_compare_consolida.py`, `runs/2026-09-03-mesura-8/*/_reconsolida-2026-09-06-pend/` |
| 2 | P2b UI | hunks `bearing_note`/`_calc_bearing` de `web/wizard_service.py`, hunks `calc-bearing`/`calc-regime`/`calcNoteKeys` de `review.html`, `tests/test_wizard_bearing_note.py` |
| 3 | assentament per criteri | `automation/settlement_criteria.py`, `terzaghi_calculator.py`, `report_generator.py`, hunks `bearing_level_number` de `report_data.py`, resta de `wizard_service.py` i `review.html`, `templates/g3dt-jinja-template.docx`, `mesura_informe.py`, `PREGUNTES` (15), `tests/test_settlement_criteria.py`, runs `-repro`, `-assent`, `-final` |
| 4 | M341 v1 + guardes | `mesura_341.py`, guarda `depth_to_m None` de `report_data.py`, `spt_n_column.py` (`_TALLY_RE`), `scripts/compare_prefills_vs_eva.py`, `tests/test_soil_levels_open_bottom.py`, `tests/test_spt_n_column.py`, run `2026-09-06-m341` |
| 5 | docs | DECISION-LOG, STATUS, PLA, sessió, aquest handoff |

`wizard_service.py`, `review.html` i `report_data.py` portaven hunks de DUES peces: es van partir per hunks (`rtk proxy git diff` →
patch per hunk → `git apply --cached`) i cada commit intermedi es va verificar en un worktree temporal amb els tests dirigits.

## Regles noves (no òbvies llegint el codi)

- **`{{ settlement_sentence }}` és l'única variable d'assentament de la plantilla.** `context['settlement']` (el número) es manté
  per al wizard, però la plantilla ja no el llegeix. Si mai veus «iguals o inferiors a 2.80 cm» a un informe de roca, algú ha
  tornat a posar la frase fixa.
- **L'assentament es calcula al `build_context`, després de P0** (necessita l'N SPT del nivell portant), i sobreescriu el del
  càlcul inicial de `generate()` (2,5 × Nb global). `ReportData.bearing_level_number` diu quin nivell de l'informe és el portant.
- **`Es_settlement` dels `user_data` d'abril NO és de l'Eva** (76 / 104 / 56 = 2,5 × Nb global desat pel wizard): les variants
  `calc`/`t2`/`viab` del harness el treuen; només `8b` el conserva.
- **D4b mira la PRIMERA litologia del text** («Lutita gresosa» → lutita) i només actua si UN nivell té la mateixa classe i la
  geometria el dona «fora». Dos nivells de la mateixa classe → l'interval mana sol.
- **M341 mesura «el que el sistema faria sol»** (`viaA`), amb UNA assumpció per projecte (`DF_SIGNAT`). Els grups (A / calc / narr
  / fix / resta) són per nom de variable (`GROUPS`, v1). Compara runs amb `diff` dels `_compare_341.txt`, mai titulars.
- **La suite té 31 vermells esperats**; compara NOMS amb `suite-vermells-esperats.txt`. Els 7 de `test_ai_pipeline_ranking_api`
  hi són.

## Procediments

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"
git branch --show-current; git status --short | grep -v runs/                 # experiment/nivell-a-2026-08, BRUT (vegeu partició)
# dirigits (4 s): 650 verds
PYTHONPATH=$PWD .venv/bin/python -m pytest tests/test_settlement_criteria.py tests/test_soil_levels_open_bottom.py tests/test_wizard_bearing_note.py tests/test_spt_n_column.py tests/test_geotech_criteria.py tests/test_bearing_layer_rule.py tests/test_bicapa.py tests/test_bicapa_wire.py tests/test_soil_levels_grouping.py tests/test_crespo_granular.py tests/test_crespo_hunt.py tests/test_lectura_tables_report.py tests/test_adjacent_wiring.py tests/test_refresh_prefills_wiring.py tests/test_lectura_consolidate.py tests/test_compare_consolida.py tests/test_ai_pipeline_calculator_pass.py -q --no-header -p no:cacheprovider
# informe (3 generables, 1 min): referència viva `2026-09-06-informe-final`
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_informe.py 2026-09-<dd>-informe-<motiu>
M=docs/wizard-headless/mesures/runs; for s in castellar rubi bell-lloc; do for v in 8b calc t2 viab; do diff <(grep -v '^\*\*' $M/2026-09-06-informe-final/$s/$v/_compare_informe.txt) <(grep -v '^\*\*' $M/2026-09-<dd>-informe-<motiu>/$s/$v/_compare_informe.txt) | grep '^[<>]' | sed "s/^/$s $v: /"; done; done
# M341 (7 projectes, 5 min): referència viva `2026-09-06-m341`
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_341.py 2026-09-<dd>-m341-<motiu>
for s in castellar rubi bell-lloc linyola alcoletge vilanova anciles; do diff <(grep '^- ' $M/2026-09-06-m341/$s/viaA/_compare_341.txt) <(grep '^- ' $M/2026-09-<dd>-m341-<motiu>/$s/viaA/_compare_341.txt) | grep '^[<>]' | sed "s/^/$s: /"; done
# lectura (7 projectes, cost 0): referència viva `_reconsolida-2026-09-06-pend`
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/reconsolida_mesura.py _reconsolida-2026-09-<dd>-<motiu> _reconsolida-2026-09-06-pend
# suite (3 min) a fitxer, diff de NOMS; després esborra el diagnòstic del dia
PYTHONPATH=$PWD .venv/bin/python -m pytest tests -q --no-header -p no:cacheprovider > /tmp/suite.txt 2>&1; diff <(grep '^FAILED' /tmp/suite.txt | sed 's/ - .*//' | sort) <(grep -v '^#' docs/wizard-headless/mesures/suite-vermells-esperats.txt | sort); rm -f docs/diagnostics/ai_pipeline_demo-project_$(date +%Y%m%d).md
```

## Seqüència d'obertura suggerida

1. `git status --short` (net) i `git log --oneline -6` (5 commits del vespre del 06 sobre `a6eb0b8`).
1b. **El següent, per decisió del Josep (2026-09-06 18:20): ANÀLISI de què es pot fer per millorar la NARRATIVA**, en dos grups:
   (1) adjacents (`adjacent_*_fmt`) i descripcions de solar (`site_description`, `site_condition`, `access_street`), (2) la resta
   (`building_structure_desc`, `materials_intro`, `radon_zone_description`, `conclusions_*`, `location_sentence`, `lab_tests_text`).
   Punt de partida: `runs/2026-09-06-m341/<slug>/viaA/_compare_341.txt` (gen ↔ eva per variable) i `_context_usat.json`; les
   fonts que la lectura JA té (`street_address`, adjacents als correus/plànol?, ortofoto ICGC) i què diu el signat a cada projecte.
   Anàlisi primer, codi després (regla d'or: llegir abans de derivar; criteris, no plantilles).
2. Dirigits (650 verds) i `mesura_informe.py` amb nom nou: `diff` buit contra `-final` i Qa 3,0 / 3,5 / 3,0.
3. Dilluns: preguntes 13 (nivell de la mostra), 14 (E/c/φ) i 15 (Es) a l'Eva + 1.7 (R4, persona/despatx, 11-13). Amb les
   respostes: candidats → defecte a `geotech_criteria.py` / `settlement_criteria.py`, `KNOWN_MISSES` al dia.
4. Decideix amb el Josep: **P5** (geometria de nivells des de la taula `soil_levels` llegida quan no hi ha sondeig: de/a per
   nivell → `sondeig_layers`; «fins al fons» = obert; desbloqueja Linyola/Alcoletge/Vilanova/Anciles al càlcul) → **P6** (un sol
   classificador de sòl: `_classify` també per a `detect_soil_type`; topall 3,5 per a tot `GRANULAR_TYPES`) → cablejat de les 6
   NO_DATA → narrativa. Cada peça: `mesura_341.py` + `mesura_informe.py` + diff per cel·la.

## Què NO fer

- No tornar a posar la frase fixa d'assentament a la plantilla; no fer que l'Es per defecte «encerti» Bell-lloc i Rubí alhora
  (max(N, Nb) és ad hoc): pregunta 15.
- No «arreglar» Linyola/Vilanova canviant `soil_types` a mà al harness: M341 mesura el sistema tal com és; el fix és P5/P6.
- No tocar l'or (excepte per decisió del Josep, com avui Alcoletge) ni `geocode_coordinates.py`; no proposar pull/merge a l'Eva.
- No versionar `.docx` ni `docs/diagnostics/ai_pipeline_demo-project_<avui>.md`; no comptar diffs sense `rtk proxy`.
- No parlar en codis al Josep («D4b», «P5», «viaA»): tradueix a què passa, on, què costa; rutes absolutes.
