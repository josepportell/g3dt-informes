# FOR NEW YOU — 2026-08-24 19:10 — Wizard headless CONSTRUÏT i validat E2E (0 erroni-amb-confiança); bloquejant = latència

**Escrit:** 2026-08-24, 19:10 · **Per a:** la sessió que ataca la LATÈNCIA del wizard headless (i després els forats).
**Recap de la sessió:** del handoff de les 15:00 ("dissenyar el Pendent B") s'ha passat a disseny validat pel Josep + Fases 0-8
construïdes el mateix dia (codi amb Sonnet 5, judici aquí) + E2E real amb `claude -p` de veritat sobre Bell-lloc i Castellar
(aquest últim pel wizard sencer, navegador inclòs). Qualitat: 0 erroni-amb-confiança als dos. Temps: 37-58 min el primer
open. Cadena de commits: `a31c3d4` (disseny) → `3893a1e` (skill v1.0 + Fase 0) → `9b881d3` → `acaaf8c` → `a776bd1` →
`331d170` → `14148ea` (UI) → `bb7b4f3` → `f91fa0d` → `30920d5` → `ce01aa6` → `b8c6986` → `1321d7a` (Fase 8) → `fcb430e` → `1dab01d`.

## 1. Ordre de lectura (20 min)

1. Aquest document.
2. `docs/wizard-headless/fase8-e2e/_RESULTATS.md` — el veredicte E2E amb xifres reals, forats i palanques de temps.
3. `STATUS.md` § "Wizard headless + UI de candidats — CONSTRUÏT".
4. `docs/DISSENY-WIZARD-HEADLESS-CANDIDATS-2026-08-24.md` — el contracte (§4), la mecànica (§5-7), la UI (§8), el pla (§9).
   Ha estat corregit en calent (§4.2 22 claus, §5 timeouts i auth, Fase 4 normalització suau): llegeix la versió a disc.
5. `docs/DECISION-LOG.md` entrada "2026-08-24 (tarda)" — el perquè de cada decisió i el GO/NO-GO.
6. `.claude/commands/g3dt-llegir-projecte.md` v1.3 — Arguments, Pas 4 (nom canònic del JSON), Pas 5 (schema v1), Pas 5b.
7. `docs/wizard-headless/FASE7-UI-NOTES.md` — checklist manual pendent de la UI i decisions d'UI.
8. `docs/_FOR-NEW-YOU-20260824-1500.md` — el Pendent A (Tier B per nivell) que segueix intacte.

## 2. Regles interpretatives crítiques

- **La qualitat està provada; la latència no.** No venguis el wizard headless com a acabat. Mesures reals (WSL, claude
  2.1.241, concurrència 2, els DOS projectes solapats): mediana 264-279 s per document, cost fix ~110 s per crida (un `.txt`
  de 3 línies triga 108-118 s), consolidació 562-585 s, paret 37-58 min. Segona obertura: 0 crides (cache per md5, verificat).
  **Cal remesurar amb UN projecte sol** abans de treure conclusions sobre la concurrència.
- **Erroni-amb-confiança = 0 és l'única mètrica que mana.** El comparador (`docs/wizard-headless/fase0-acceptacio/
  compare_consolida.py`) marca ERR per diferències de FORMAT (`-4,0 m` vs `-4 m`, "PB+1 (PB+PP)" vs "PB+PP (PB+1)",
  `spt_ma` en comptes vs "1/0"): llegeix cada ERR abans de comptar-lo. CAUTELA (or segur → producte candidats amb el bo dins)
  és acceptable per disseny.
- **El productor cec desvia en forma, no en fons**: 5 desviacions a la Fase 0 i 3 a l'E2E, totes de claus/format. Per això
  existeix `soft_normalize` (a: value:=candidates[0]; b: font/quote solts → candidates; c: àlies de claus de fila → canòniques)
  ABANS de declarar un `_decisions.json` invàlid i caure al degradat.
- **Els fixtures d'or tenen 3 dialectes** (escalars `status`/`source`; taules Bell-lloc `estat`/`font`; taules Castellar
  `candidats[].valor`). `contract.adapt_legacy` els normalitza tots; no toquis els fixtures.
- **`no_trobat` a la lectura no vol dir camp buit al wizard**: el merge cau a la via B/auto (p. ex. `utm_x/y` de Bell-lloc:
  la lectura diu no_trobat perquè `COORDENADES.txt` va per Python i el consolidador cec no el veu, però auto_extract l'omple).
  La UI mostra "no trobat" al costat d'un camp ple — forat 1, pendent.
- **Leakage**: tot valida re-execució, no generalització. El test real és una carpeta nova de l'Eva.

## 3. Supòsits estructurals que aguanten el pes

| On | Què | No desfer |
|---|---|---|
| `automation/lectura/runner.py` `_child_env()` | treu `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` de l'entorn del fill (`G3DT_LECTURA_AUTH=login`) | el `.env` de la via B porta una clau SENSE crèdit; el CLI la prefereix al login → rc=1 "Credit balance is too low" |
| `runner.py` defectes | `G3DT_LECTURA_TIMEOUT=600`, `G3DT_LECTURA_CONSOLIDA_TIMEOUT=900` | 240/360 mataven els documents pesants i les dues consolidacions |
| `runner.py` `safe_doc_name` = skill v1.2 Pas 4 | nom canònic del `{doc}.json` (path relatiu sanejat, minúscules) | runner i skill han de coincidir; hi ha fallback per `source_path` però és lent i fràgil |
| `normalize.py` `ROW_REQUIRED_KEYS`/`ROW_KEY_ALIASES` + skill v1.3 | claus canòniques de fila (`sondeig`, `cota`, `profunditat`…) | la UI (`review.html` `_lecturaN30Cell` etc.) i el generador futur en depenen; `litologia` SPT NO obligatòria (l'or d'Anciles no la porta) |
| `contract.py` `validate_decisions` | rebutja n30/litologia `segur`, claus desconegudes, dialecte antic | és el criteri d'or codificat; `merge_degradat` només pot marcar `segur` senyals g3_templates ≥ 0,9 |
| `web/wizard_service.py` `_merge_prefills(..., *, skip_vision=False)` | única edició a la via B; per defecte idèntic | amb lectura OK, `skip_vision=True` evita les crides Groq/Anthropic |
| `web/api.py` `_require_lectura_enabled` (404) + `config.G3DT_USE_LECTURA_HEADLESS=False` | prod actual no canvia de comportament | la UI prova `/api/lectura-stream` i, si 404, fa el camí d'avui |
| `review.html` bloc `/* === LECTURA HEADLESS (Fase 7) === */` | autocontingut; `_lecturaUnwrap` accepta `_lectura` embolcallat (`{value, source}`) o pla | el servei l'embolcalla com les altres claus de sistema del merged |
| `review.html` `updateLecturaProgress` → `setText` | replica el progrés a `#step-files-vision` | `#lectura-progress` viu dins del formulari del wizard, OCULT fins al `prefills` |
| Pestanyes DPSH/Sondeig del CLAUDE.md | NO existeixen (codi mort d'una UI anterior) | les taules de lectura són a la pestanya Wizard, grup "Lectura headless — assaigs de camp" |
| `automation/ai_pipeline/`, `web/vision_groq.py`, `auto_extractor.py`, `web/vision_fast.py` | via B intacta | PROHIBIT editar-los |

## 4. Procediments operatius

```bash
# Estat esperat
git branch --show-current      # → experiment/nivell-a-2026-08 (in-place a g3dt-prod/)
git log --oneline -3           # → 1dab01d fcb430e 1321d7a

# Suite de lectura (14 s) i suite completa (2,5 min; línia base 32 failed IDÈNTIQUES / 1152 passed / 3 skipped)
.venv/bin/python -m pytest tests/test_lectura_*.py tests/test_utf8_read_text.py -q
.venv/bin/python -m pytest tests/ -q -p no:cacheprovider

# E2E real del runner (còpia LOCAL del projecte; mai /mnt/c). Driver: docs no; reescriu-lo en 20 línies si cal:
#   from automation.lectura.runner import run_lectura; run_lectura(Path(proj), on_event=print)
rsync -a --exclude validation/ "/mnt/c/claude/g3dt/projectes/4001612 BELL-LLOC" ~/g3dt-e2e/projectes/
PYTHONPATH=$PWD G3DT_LECTURA_CONCURRENCY=3 setsid nohup .venv/bin/python DRIVER.py "$HOME/g3dt-e2e/projectes/4001612 BELL-LLOC" > log 2>&1 < /dev/null &
# (les còpies de Bell-lloc i Castellar JA hi són a ~/g3dt-e2e/projectes/ amb validation/lectura/ ple = cache → per remesurar
#  el primer open, esborra validation/lectura/ o passa force=True)

# Wizard sencer amb la lectura activa (mode dev: dropdown local; el .env de prod porta G3DT_NETWORK_PROJECTS)
G3DT_NETWORK_PROJECTS="" G3DT_LOCAL_WORKSPACE="" G3DT_USE_LECTURA_HEADLESS=true G3DT_PROJECTS_DIR="$HOME/g3dt-e2e/projectes" \
  setsid nohup .venv/bin/python -m web > server.log 2>&1 < /dev/null &      # http://localhost:8765/review.html

# Comparar un _decisions.json real amb l'or (escalars | taules) — llegeix cada ERR: molts són format
.venv/bin/python docs/wizard-headless/fase0-acceptacio/compare_consolida.py escalars PATH/_decisions.json "3001621 CASTELLAR DEL VALLES"

# Telemetria d'una lectura
python3 -c "import json;[print(json.loads(l)['mode'],round(json.loads(l)['elapsed_s'])) for l in open('PATH/_telemetry.jsonl')]"
```

- Logs de cada crida `claude -p`: `/tmp/g3dt-lectura-{projecte}-{doc}-{intent}.log` (stdout+stderr del CLI).
- Cache: `{projecte}/validation/lectura/{doc}.json` (vàlid si `source_md5` coincideix), `_decisions.json` (vàlid si més nou
  que tots els `{doc}.json`). `?refresh=true` o `force=True` els ignora.
- **`pkill -f PATRO` es mata a si mateix** si el patró apareix a la línia d'ordres del Bash (exit 144): usa `"[c]laude -p /g3dt"`,
  `"[e]2e_run_le"`, `"[p]ython -m we"` i NO escriguis el nom del que mates a la mateixa crida que el rellançament.
- Un `sleep N` sol com a espera està bloquejat a l'eina Bash; per esperar processos: `run_in_background` + `until`, o Monitor.
- Els agents Sonnet 5 amb `review.html` (9.000 línies): PROHIBIT llegir-lo sencer (un agent es va encallar 600 s); grep + rangs.
- L'extensió de Chrome no estava connectada; Playwright MCP sí (`browser_navigate`/`browser_snapshot` a fitxer dins
  `.playwright-mcp/`, `browser_select_option`, `browser_console_messages`).

## 5. Seqüència d'obertura suggerida

1. `git branch --show-current` + `git log --oneline -3`; suite de lectura verda (70 tests).
2. Llegeix `fase8-e2e/_RESULTATS.md` §"Palanques de temps" i decideix amb el Josep quines s'apliquen (concurrència 3-4;
   prompt prim = `cwd` neutre + skill instal·lat com a user-command o skill curt per a `--only`; enrutar menys documents —
   `*_fotografies.pdf`, `LAB-SIG.pdf`, còpies Print-To-PDF quan `PDF/ANNEXES/` ja les té; consolidació Python-first).
3. **Remesura amb 1 projecte sol** (Castellar, `validation/lectura/` esborrat) ABANS i DESPRÉS de cada palanca; apunta-ho
   a `_RESULTATS.md` com a addendum datat.
4. Forat 1: lectors Python fora de g3_templates (COORDENADES.txt, Cadastre) → emet un `_python_readers.json` a `--out` amb
   el mateix format de senyals que `_g3_templates.json` i fes que el Pas 5b el llegeixi (skill v1.4) — o enruta'ls a claude.
5. Fase 8b: `getLecturaSelections()` de la UI → `save_wizard` → `user_data` → generador (taules); després
   `scripts/compare_tables_vs_eva.py` sobre el docx generat contra `docs/golden-read-taules/_eva_truth/`.
6. Commit per peça (`feat(g3dt): …`), i quan `_RESULTATS.md` tingui la remesura, entrada nova al DECISION-LOG.

## 6. Què NO fer

- No proposar pull/merge/desplegar a l'Eva (memòria `feedback_no_pull_eva_success_criterion`). Ni amb 40 min de primer open.
- No presentar el wizard headless com a acabat: GO tècnic, NO-GO de latència.
- No editar els 4 fitxers de la via B. No reescriure `docs/golden-read*` ni `docs/wizard-headless/fase*` (addendums datats).
- No córrer dues lectures del MATEIX projecte alhora (el runner no té lock entre processos: crides duplicades i cache en carrera).
- No llançar E2E amb el `.env` carregat i `G3DT_LECTURA_AUTH=api_key` sense crèdit a la clau.
- No convertir CAND en segur sense 2 fonts; n30 i litologia mai segurs (el validador ho rebutja — no l'afluixis).
- No confiar en `pytest … | tail -1` per decidir un commit (el `tail` amaga el codi de sortida; va passar un cop).
- No escriure a `/mnt/c`; els E2E van a `~/g3dt-e2e/projectes/`.

## 7. Tasques obertes (ordre)

1. Palanques de latència + remesura amb 1 projecte sol (decisió Josep sobre quines).
2. Forat 1: fonts Python fora de g3_templates visibles al consolidador.
3. Fase 8b: seleccions de taula de la UI → backend → generador; validar taules del docx amb `compare_tables_vs_eva.py`.
4. Checklist manual de la UI (`FASE7-UI-NOTES.md`): badge "no trobat" al costat de camp ple (redacció), popups, chips n30.
5. Prova a Windows (subprocess `CREATE_NEW_PROCESS_GROUP`, paths, `claude` CLI natiu) — decisió Josep, presencial.
6. Pendent A: Tier B per nivell (K, C, γ/c/φ/E) contra `docs/golden-read-taules/_eva_truth/` (handoff 15:00 §2A).
7. Preguntes a l'Eva: criteri de suma N30; sulfats d'Anciles. Projecte NOU de l'Eva = primer test de generalització.
8. Multi-informe Tulipa al wizard (decisió de model de dades amb el Josep).

## 8. Guanys ocults

- **El validador ha caçat desviacions reals del productor cec 3 cops** (Fase 0, E2E Castellar, E2E Bell-lloc) i cada cop ha
  acabat en una regla del skill o en normalització determinista: el sistema de qualitat s'autoalimenta.
- **El wizard sencer funciona amb Claude Code real** (SSE → runner → consolidació → merge → badges) sense tocar la via B:
  prod actual no ha canviat de comportament amb el flag off (suite 32 failed idèntics).
- Les lliçons de la lectura d'or es veuen en producció: Castellar `cota_referencia` relativa "-4 m (respecte el carrer)"
  com a candidat 1 (l'ERR històric no es repeteix); profunditats DPSH exactes; n30 mai tancat.
- Dos bugs que haurien matat la instal·lació a casa l'Eva han sortit aquí i no allà: la clau API sense crèdit al `.env` i els
  timeouts curts.
