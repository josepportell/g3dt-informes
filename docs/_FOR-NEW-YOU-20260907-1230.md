# FOR NEW YOU — 2026-09-07 (migdia, 12:30) — BLOC 2 del PLA FET i mesurat (format de la cua d'A). SENSE commit. Següent: GO/commit, bloc 3 (extractor)

**Escrit:** 2026-09-07, 12:30. Substitueix `_FOR-NEW-YOU-20260907-0930.md` (conserva'l: bloc 1). Branca `experiment/nivell-a-2026-08`,
in-place a `g3dt-prod/` (estat esperat). **SENSE commit**: 13 fitxers modificats + `automation/honorifics.py` + `tests/test_bloc2_format_a.py`
nous (+ `photo_selection.json` de Linyola, que ja hi era). `production/g3dt-eva-v1` @ `c46bc69` sense push.

## En una frase

La cua del grup A era format: plantes, honorífic, article del tipus d'edificació, milers, cota, id SPT i municipi del padró surten ara
com als signats i **4 forats de la plantilla porten la preposició dins el valor**; `viaA` A 74 → **77 %**, total 71 → **73 %**, taules
81 → **82 %**, calc/narrativa/resta idèntics, **cap pèrdua**. Tot a DECISION-LOG 2026-09-07 (migdia), decisions A-H.

## Estat exacte

- Runs de referència NOUS: `2026-09-07-m341-bloc2b` i `2026-09-07-informe-bloc2b` (`bloc2` = passada amb el comparador de taules
  llegint «1.167» com a 1,167; conservada com a evidència del registre #5).
- Suite: 31 vermells amb els mateixos NOMS que `suite-vermells-esperats.txt` / 2415 verds / 5 omesos.
- Pregunta 26 a l'Eva (`docs/PREGUNTES-EVA-PENDENTS.md`). Registre de pèrdues: bloc 2 = cap (#5 resolta, 3 moviments X → X).
- Partició de commit suggerida (DECISION-LOG § GO/NO-GO): (1) codi + tests, (2) plantilla, (3) comparadors + M341, (4) docs i runs.

## Què NO fer

- No fusionar el generador nou amb la plantilla VELLA de producció: el context porta l'article i imprimiria «d'un un habitatge».
- No derivar `spt_lithology` amb regles: la forma curta és de l'Eva («Limolites i bretxes»). Candidat del wizard, no fórmula.
- No «arreglar» la cota d'Anciles al generador: és inventari (I1, `PDF_V0/ANEJOS/` exclòs), es resol rellegint.
- No proposar pull/merge a l'Eva; no pujar `production/g3dt-eva-v1`.

## Bloc 3 (el que faràs després del GO), del PLA §Bloc 3

Deriva de l'extractor de referència: veritats-frase de `municipality` (Alcoletge/Vilanova/Anciles: el paràgraf de la sísmica),
`data_camp_text` ES, `superficie_parcela` («T-1») i `plantes` («C-1») d'Anciles, `geomech_*` per fila (Alcoletge, registre #3),
`spt_*` a None, `settlement` → `settlement_sentence`. Amb la plantilla nova, la re-extracció capturarà `client_de`, `building_type_de`,
`municipality_de`, `architect_company_de` (ja al grup A de M341). Mètode: `refresh_eva_narrativa.py --all-keys` per veure què mouria,
llista blanca + `diff` per clau, mesurar amb el codi SENSE tocar abans i després del refresc.

## Procediments

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"; git branch --show-current; git status --short | grep -v runs/
PYTHONPATH=$PWD .venv/bin/python -m pytest tests/test_bloc2_format_a.py tests/test_bloc1_calc_taules.py tests/test_narrative_criteria.py tests/test_lectura_tables_report.py -q --no-header -p no:cacheprovider
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_341.py 2026-09-<dd>-m341-<motiu>
M=docs/wizard-headless/mesures/runs; for s in castellar rubi bell-lloc linyola alcoletge vilanova anciles; do diff <(grep '^- ' $M/2026-09-07-m341-bloc2b/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') <(grep '^- ' $M/2026-09-<dd>-m341-<motiu>/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') | grep '^[<>]' | sed "s/^/$s: /"; done
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_informe.py 2026-09-<dd>-informe-<motiu>
for d in $(cd $M/2026-09-07-informe-bloc2b && find . -name '_compare_informe.txt'); do diff -q $M/2026-09-07-informe-bloc2b/$d $M/2026-09-<dd>-informe-<motiu>/$d >/dev/null || { echo "DIFF $d"; diff $M/2026-09-07-informe-bloc2b/$d $M/2026-09-<dd>-informe-<motiu>/$d | grep '^[<>]' | head -6; }; done
PYTHONPATH=$PWD .venv/bin/python -m pytest tests -q --no-header -p no:cacheprovider > /tmp/suite.txt 2>&1; diff <(grep '^FAILED' /tmp/suite.txt | sed 's/ - .*//' | sort) <(grep -v '^#' docs/wizard-headless/mesures/suite-vermells-esperats.txt | sort); rm -f docs/diagnostics/ai_pipeline_demo-project_$(date +%Y%m%d).md
```

Els `.doc` signats es llegeixen amb `soffice --headless --convert-to txt:Text --outdir <dir> <fitxer.doc>` (fet avui per als 7 abans de
tocar la plantilla). Gotcha del `grep`: el hook `rtk` el reescriu; `rtk proxy grep …` en pipelines. Les dues mesures i la suite es
poden llançar alhora (directoris de run i de `.docx` independents).

## Seqüència d'obertura

1. `git branch --show-current` (una línia al Josep), `git status`, `git log --oneline -5` (`b4e0286` docs del bloc 1 al capdamunt).
2. DECISION-LOG 2026-09-07 (migdia) i el PLA §Bloc 2 (estat) i §Bloc 3.
3. Si el Josep dona GO: commit en 4 (vegeu partició). Després bloc 3, mesura contra `-bloc2b`.
