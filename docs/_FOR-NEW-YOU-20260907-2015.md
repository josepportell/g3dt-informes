# FOR NEW YOU — 2026-09-07 (nit del 06, 20:15) — NARRATIVA per CRITERI: peces 0 (mesura) + 1 (fórmules) + 2 (adjacents) FETES: narrativa 30 % → 62 % (català 65 %), total 68 %, taules intactes. COMMITEJAT en 4 (GO del Josep). Següent: peça 3, candidats al wizard, preguntes 16-19, castellà

**Escrit:** 2026-09-06, 20:15. Substitueix `_FOR-NEW-YOU-20260907-1810.md` (conserva'l: els procediments, les 4 variants i els invariants
del bloc 2 continuen vigents). Branca `experiment/nivell-a-2026-08`, in-place a `g3dt-prod/` (estat esperat), **arbre NET després dels commits
`240a9dd` (mesura + extractor + veritats) · `c2582ae` (fórmules + plantilla) · `9dd329d` (adjacents + accés + ubicació) · `284f76a` (docs) (verificats en worktrees temporals: 38 + 38 tests verds als commits 2 i 3).**

**En una frase:** el Josep ha donat GO a l'anàlisi de la narrativa (`docs/ANALISI-NARRATIVA-2026-09-06.md`) i a les peces 0 + 1 + 2;
la narrativa dels 7 informes generats només amb la lectura ha passat de 30 % (mesura enganyosa) a 23 % (mesura honesta) a 54 %
(les frases de l'Eva com a criteri) a **62 %** (adjacents des de la parcel·la del projecte); el total escalars+narrativa de 58 % a
**68 %**; les 11 taules no s'han mogut. L'única explicació completa és **DECISION-LOG 2026-09-06 (nit)**.

## Els commits (fets, 2026-09-06 20:20, GO del Josep) — la partició que es va aplicar

| # | Commit | Fitxers |
|---|---|---|
| 1 | mesura + extractor + veritats | `docs/wizard-headless/mesures/mesura_341.py`, `docs/wizard-headless/mesures/refresh_eva_narrativa.py`, `automation/reference_extractor.py`, 7 × `reference-material/*/validation/eva_reference_values.json`, runs `2026-09-06-m341-mesura` |
| 2 | fórmules + plantilla | `automation/narrative_criteria.py`, `automation/report_generator.py` (hunks estructura/estat/radó/nivells), `automation/sections/section3_geologia.py`, `automation/sections/section4_conclusions.py`, `web/wizard_service.py`, `automation/report_data.py`, `templates/g3dt-jinja-template.docx` (binari: hi va sencer, bloc 2.1.1 inclòs), `tests/test_narrative_criteria.py`, runs `-narr1`, `informe-narr1` |
| 3 | adjacents + accés + ubicació | `automation/parcel_context.py`, `automation/cadastre_adjacents.py`, `automation/adjacent_formatter.py`, `automation/report_generator.py` (hunks adjacents/accés/ubicació/introducció), `automation/narrative_criteria.py` (`municipality_proper`, `location_sentence_from_streets`), `web/lectura_service.py`, `automation/auto_extractor.py`, `tests/test_adjacents_narrative.py`, runs `-narr2c`, `informe-narr2` |
| 4 | docs | DECISION-LOG, STATUS, ANALISI (§9), PREGUNTES (16-19), sessió, aquest handoff |

`report_generator.py` es va partir per hunks (`rtk proxy git diff` → patch amb els 5 hunks de la peça 1 → `git apply --cached --recount`);
`narrative_criteria.py` sencer al commit 2 (els seus dos ajudants de la peça 2 hi viatgen sense ús); la plantilla `.docx` (un sol blob amb
les 5 frases senceres i el bloc 2.1.1) al commit 2. NO versionat: `reference-material/4001607 LINYOLA/validation/photo_selection.json`
(artefacte dels runs). Els runs intermedis `-narr2` / `-narr2b` s'han esborrat; la referència viva és `-narr2c`.

## Regles noves (no òbvies llegint el codi)

- **La narrativa es mesura forat contra forat** (`mesura_341.narr_status`): residus després de treure el text comú als extrems, sense
  accents. Un residu buit = CLOSE. `csn_radon_text` no es mesura (text fix de la plantilla). Els costats agrupats dels adjacents es
  comparen amb la frase agrupada. Compara runs amb `diff` dels `_compare_341.txt`, mai titulars; l'agregat porta narrativa CA / ES.
- **Les veritats narratives es refresquen NOMÉS amb `refresh_eva_narrativa.py`** (llista blanca = grup `narr` + `location_sentence`;
  `--all-keys` per veure què més mouria l'extractor; `--drop` per a claus que la plantilla ja no té). Una re-extracció sencera
  (`python -m automation.reference_extractor`) mouria `spt_*`, dates, `geomech_*`, `settlement`: NO fer-la sense re-mesurar A/calc.
- **L'extractor de referència:** prefix primer → posició → àncora (`paragraph_anchor`, mai sobre `intelligent_analysis`);
  `location_sentence` absent si el prefix no hi és. Confiança 0,3 = «paràgraf sencer, capçalera diferent».
- **Les frases de l'Eva viuen a `automation/narrative_criteria.py`** (fórmules literals; errates seves no reproduïdes). La plantilla té
  `{{ site_condition }}` (frase sencera), `…estructura {{ building_structure_desc }}` (clàusula sencera), `{{ radon_sentence }}`,
  `{{ adjacent_intro }}` i 4 forats d'adjacents dins `{%p if %}`/`{%p endif %}` en paràgrafs PROPIS (docxtpl: l'etiqueta sola al
  paràgraf; en línia trenca el render). `csn_radon_text` = '' sempre.
- **`user_data` mana** a `site_condition` i `building_structure_desc` (abans el generador els ignorava); els valors curts antics («pla»,
  «en planta baixa») es recalculen/mapegen. Els candidats són al context `_narr_*` i al prefill del wizard (`candidates`), encara
  NO visibles a la UI.
- **Els adjacents surten de `cadastral_refs`** (lectura, `referencia_catastral` → `web/lectura_service`), llista de RC de 14 (`parcel_context.parse_rc_list`),
  polígon unió, UTM opcional. Sense RC ni UTM (Rubí, rústica) no hi ha adjacents a `viaA`. Les plantes del veí venen de `<pt>` del
  DNPRC (abans `<stl>`, la superfície). Vocabulari tancat a `adjacent_formatter` (agrupació de costats iguals, «I finalment» a
  l'última, castellà). `access_street` del costat carrer («entre X i Y» → X). `adjacent_intro` amb posició des del centre del municipi
  (Nominatim, cache `municipi_centre`) i forma del polígon.
- **Idioma:** `language_for_report` (municipi ES, marcadors, o `report_language` explícit). Vilanova (signat ES, dades CA) només amb
  `report_language`. No hi ha plantilla ES: les 30 cel·les ES són sostre.
- **La plantilla de producció (`production/g3dt-eva-v1`) té el defecte 2.1.1** (dos adjacents sobrers, sense capçalera 2.1 ni introducció).
  Mai proposar pull/merge a l'Eva; el Josep decideix quan i com.

## Procediments

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"; git branch --show-current; git status --short | grep -v runs/
# dirigits (2 s): 22 nous + els d'adjacents del wizard
PYTHONPATH=$PWD .venv/bin/python -m pytest tests/test_narrative_criteria.py tests/test_adjacents_narrative.py tests/test_adjacent_wiring.py tests/test_adjacent_synthesis.py tests/test_visual_observations.py tests/test_settlement_criteria.py -q --no-header -p no:cacheprovider
# M341 (7 projectes, 5 min): referència viva `2026-09-06-m341-narr2c`
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_341.py 2026-09-<dd>-m341-<motiu>
M=docs/wizard-headless/mesures/runs; for s in castellar rubi bell-lloc linyola alcoletge vilanova anciles; do diff <(grep '^- ' $M/2026-09-06-m341-narr2c/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') <(grep '^- ' $M/2026-09-<dd>-m341-<motiu>/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') | grep '^[<>]' | sed "s/^/$s: /"; done
# informe (3 generables, 1 min): referència viva `2026-09-06-informe-narr2` (= `-final` a les 11 taules)
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_informe.py 2026-09-<dd>-informe-<motiu>
# veritats narratives (2 min; només informa sense --apply)
PYTHONPATH=$PWD .venv/bin/python docs/wizard-headless/mesures/refresh_eva_narrativa.py [--all-keys] [--apply] [--drop clau,…]
# suite (3-4 min) a fitxer, diff de NOMS; després esborra el diagnòstic del dia
PYTHONPATH=$PWD .venv/bin/python -m pytest tests -q --no-header -p no:cacheprovider > /tmp/suite.txt 2>&1; diff <(grep '^FAILED' /tmp/suite.txt | sed 's/ - .*//' | sort) <(grep -v '^#' docs/wizard-headless/mesures/suite-vermells-esperats.txt | sort); rm -f docs/diagnostics/ai_pipeline_demo-project_$(date +%Y%m%d).md
```

Gotcha del `grep`: el hook `rtk` reescriu `grep` amb opcions (`-h`, `-A`) i pot fallar; en pipelines, `rtk proxy grep …`.

## Seqüència d'obertura suggerida

1. `git status --short` (net) i `git log --oneline -6` (5 commits de la nit sobre `97b63d4`).
2. DECISION-LOG 2026-09-06 (nit) sencer (decisions A-D, validació, limitacions) i `ANALISI-NARRATIVA` §9.
3. Dirigits (verds) i, si es toca res, M341 amb nom nou + `diff` contra `-narr2c`.
4. Dilluns: preguntes 13-19 a l'Eva (`docs/PREGUNTES-EVA-PENDENTS.md`).
5. Peça 3 (§3.2 de l'anàlisi): `site_description` per criteri editable, `lab_tests_text` del GTL, fotos «vistes generals» condicionals.
   Després: candidats `_narr_*` visibles al wizard («+N»), castellà (decisió: plantilla ES?), P5/P6, cablejat del laboratori
   (agressivitat d'Alcoletge), deriva de l'extractor fora de la narrativa (`spt_*`, dates, `geomech_*`).

## Què NO fer

- No re-extreure les veritats senceres (`python -m automation.reference_extractor`) sense aïllar-ne l'efecte a A/calc.
- No posar `{%p if %}` i `{{ }}` al mateix paràgraf de la plantilla; no reintroduir `csn_radon_text` ni la cua «, municipi amb concentracions mitjanes…».
- No «arreglar» Rubí/Linyola (`materials_intro` «dos nivells») tocant la narrativa: és P5 (recompte de nivells).
- No reproduir les errates de l'Eva («un sòl nivel», «del murs»); no fer que el comparador les perdoni més enllà dels accents.
- No tocar `geocode_coordinates.py`; no proposar pull/merge a l'Eva; no versionar `.docx` generats ni `photo_selection.json`.
- No parlar en codis al Josep («narr2c», «D4b»): tradueix a què passa, on, què costa; rutes absolutes.
