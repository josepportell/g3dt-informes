# FOR NEW YOU — 2026-08-25 23:00 — Comparador d'or v2 FET (0 ERR de format als 9 jocs; 2 erroni de fons amagats destapats; fixture `sondeig cota` revisat); següent: Fase 11, 13-17 (no començades), forat capa vegetal a `consolidate.py`

**Escrit:** 2026-08-25, 23:00 · **Per a:** la sessió següent. **Llegeix abans:** `docs/_FOR-NEW-YOU-20260825-2200.md` (§2-§6 vigents; la seva tasca 1 és la que s'ha fet aquí).
**Recap:** el Josep ha dit «arrenca el punt 1 (normalitzadors del comparador); decideix si Sonnet en pot fer part; NO comencis les Fases 11/13-17».
Fet: `compare_consolida.py` v2 + 79 tests, fixture de Castellar revisat, llibre regenerat amb traçabilitat v1→v2, docs. Sonnet només com a revisor
adversari del comparador (resultat: vegeu §2 / session log). Commits sobre `7ae3065`: `dcda24f` (comparador+tests+ledger), `4d135cd` (fixture),
`e315b64` (llibre regenerat), + el de docs d'aquest handoff.

## 1. Ordre de lectura (10 min)
1. Aquest document. 2. `docs/DECISION-LOG.md` entrada «2026-08-25 (nit, 2)» (per què cada normalitzador, què destapa). 3. `docs/wizard-headless/fase12-consolida/_RESULTATS.md` §7
(harness sota v2, patró «nivell 1 des de 0,00»). 4. `docs/wizard-headless/mesures/LEDGER.md` (taula de qualitat v2; «Condicions» porta els totals v1).
5. `compare_consolida.py` docstring de capçalera (ordre dels normalitzadors). 6. `docs/golden-read-taules/_RESULTATS.md` «Revisions de fixtures».

## 2. Estat
- **Comparador v2:** `close(a, b, field)` per tipus (adreça → spt_ma → data → nombres/intervals → num_floors → building_type → text); files de taula
  alineades per clau (`punt`/`sondeig`/capa vegetal/`nivell N`; índex si claus no úniques; `spt_ma_tests` per índex); `_expand_de_a`. CLI i format de
  sortida idèntics (ledger + harness sense canvis). `tests/test_compare_consolida.py`: 79. Suite lectura 267 passed / 2 skipped.
- **Llibre:** 0 ERR de format als 9 jocs. ERR que queden = reals o vocabulari: `soil_levels[1].de` `segur 0.00` vs or `-0,50` a `e2e-tarda-c2-solapat` i
  `sonnet-c3` (24-08) → **fons 0 → 1** (`meta.json`: `erroni_amb_confianca_fons_v1` conservat + nota); `nivell_freatic` `No indicat` a `consolida2` (estricte a posta).
- **Fase 12 sota v2:** 0 erroni de fons als 3 jocs v2; joc v1.3 = 1 (heretat de la lectura; la referència LLM el té igual); forat nou: Python deixa `no_trobat`
  les fondàries de la capa vegetal al joc Sonnet v2 (la consolidació LLM les tenia). Bell-lloc v1.2: `soil_levels[1].de` puja a `segur 0.00` fora dels candidats.
- **Fixture Castellar `sondeig_tests[0].cota`:** `candidats` [`-4 m (respecte el carrer)` (manuscrit p.5, cita real) | `570,90 msnm` | `570,9`], clau `revisio`.
  −4,20 (informe Eva) NO hi és: cap document de la carpeta ho diu per a S-1. `de_a_estat`: fixture intacte, l'absorbeix el comparador.
- **Revisió adversària Sonnet (code-reviewer):** encàrrec = parells realment diferents que `close()` doni per iguals. Resultat i correccions: session log 2026-08-25 (nit 2) i, si n'hi ha, commit posterior a `e315b64`.

## 3. Regles interpretatives noves
- **El comparador no comparteix codi amb el consolidador** (ni `value_key`): l'instrument no ha d'heretar els errors de l'objecte mesurat. Si cal la mateixa
  idea a tots dos, s'implementa dues vegades i es documenta la intenció.
- **Adreça = mateix conjunt de portals.** `Carrer Arbrells 18A` NO és `Carrer Arbrells, 18A, 18B i 20` (lectura parcial ≠ mateix valor). Formes `C/`, `Nº`, `-`/`,`/` i ` sí.
- **`abs` només a fondàries** (`lab_depth`, `profunditat`, `profunditat_assolida`, `de`, `a`, `nivell_freatic`); a cotes el signe compta (`-4` ≠ `4`).
- **Data només si TOTA la cadena és una data** (`1,5-1,75` no ho és). Els nombres són decisius: `1` ja no és "dins" de `12` (v1 ho acceptava).
- **`No indicat` ≠ `No detectat`** al comparador (vocabulari, resolt al skill v1.5); no afluixar-ho.
- **Totals del llibre:** mai editar `.txt`/`LEDGER.md` a mà; un canvi del comparador = regenerar tot + `comparator_revision` a cada `meta.json`.

## 4. Supòsits que aguanten el pes (nous)
| On | Què | No desfer |
|---|---|---|
| `close()` ordre | adreça i `spt_ma` per camp ABANS de dates/nombres; dates i nombres decisius (no cauen al text) | si el text (contenció) anés primer, `18A` ⊂ `18A-18B-20` tornaria a ser "close" |
| `parse_numbers` | `(?<=\d)\s*-\s*(?=\d)` = separador d'interval; `_NUM_RE` amb lookarounds (no agafa el `1` de `S1`) | sense el lookaround, `4 (P1…) + S1` donava (4, 1) |
| `align_rows` | només si TOTES les claus són úniques als dos costats; si no, índex | jocs amb `NIVELL 1` + `1er nivell` (dues files n1) es comparen per índex, com abans |
| `row_key` soil_levels | patró de capa vegetal té prioritat sobre «nivell N» (`1r nivell — Terreny vegetal` = capa) | Opus 4.8 etiqueta la capa vegetal com a «1r nivell» |
| `_expand_de_a` | només si la fila té `de_a_estat` amb `estat`; `de`/`a` plans → cel·les amb aquell estat | només el fixture de Castellar el té |
| `meta.json` | `comparator_revision` + `erroni_amb_confianca_fons_v1`; l'anotador és idempotent | `ledger.py` llegeix `comparator_revision` per a «Condicions» |

## 5. Procediments
```bash
.venv/bin/python -m pytest tests/test_compare_consolida.py -q -p no:cacheprovider                        # 79, 0,2 s
.venv/bin/python docs/wizard-headless/fase0-acceptacio/compare_consolida.py escalars|taules PATH/_decisions.json "3001621 CASTELLAR DEL VALLES"
.venv/bin/python docs/wizard-headless/fase12-consolida/harness.py [--write docs/wizard-headless/fase12-consolida/out]  # després: git checkout -- out/*/_decisions.json (només canvia elapsed_s)
python3 docs/wizard-headless/mesures/ledger.py
# Sonda ràpida d'un parell: importlib sobre compare_consolida.py → cc.close(A, B, FIELD)
# Mirar un annex amb els teus ulls: .venv/bin/python scripts/render_clip.py PDF --page N --clip 0,0,1,1 --dpi 110 --out /tmp/x.png
```

## 6. Què NO fer
- No afegir normalitzadors "perquè un run ho necessita" sense un parell real al test i sense mirar si afluixa (cada regla nova = cas True + cas False al test).
- No importar `consolidate.value_key` al comparador. No editar fixtures d'or per quadrar; si cal, revisió explícita amb evidència (informe Eva) + `revisio` + registre.
- No posar −4,20 al fixture de Castellar (no és a cap document). No tocar `erroni_amb_confianca_fons` sense conservar `_v1`.
- Vigents: no pull/merge a l'Eva; no `git stash`; no `docs/diagnostics/ai_pipeline_demo-*.md` ni `photo_selection.json` als commits; no Fases 11/13-17 sense dir-ho el Josep.

## 7. Tasques obertes (ordre)
1. Fase 11 (delta-sync) → 13 (`auto_result` + Cadastre/ICGC al consolidador) → 14 (UI tres botons) → 15 → 16 → 17 — quan el Josep ho digui.
2. `consolidate.py`: per què les fondàries de la capa vegetal queden `no_trobat` al joc Sonnet v2 (la consolidació LLM les tenia) — mirar senyals `soil_levels` del perdoc.
3. Skill Pas 3b: regla explícita per al `de` del nivell 1 (base de la capa vegetal no numerada; etiqueta que abasta tota la columna → candidats) — **pregunta a l'Eva** (criteri de nivells; d'on surt −4,20 per a S-1 a Castellar).
4. `consolidate.value_key`: `1,5-1,75` → data (latent; cap cas real). Un test i un fullmatch com al comparador.
5. Bell-lloc complet al llibre + files d'effort; decisió `auto` vs `python` (2-3 projectes més amb conflictes).
6. Pendents anteriors: neteja `_preext/`, pla de subscripció, Fase 8b, Tier B, objectiu 341 variables.
