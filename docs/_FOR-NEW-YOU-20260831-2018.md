# FOR NEW YOU — 2026-08-31 (20:18) — Fixos F/A/B/C fets i commitejats; Fix D és la següent feina

**Escrit:** 2026-08-31, 20:18 · **Per a:** la sessió que continua `PLA-PENDENTS-0B-0C-0D-2026-08-26.md`.
**Recap:** el Josep va recordar (erròniament) que el pla ja s'havia implementat fins al Fix D; es va verificar amb
`git reflog`/`fsck`/stash que no s'havia tocat res des del handoff del 26/08. Es va decidir implementar-lo ara mateix
i es van fer i commitejar F, A, B i C en aquesta sessió. El Josep va aturar la sessió abans de D/E/docs perquè
s'estava omplint ("això comença a estar força plena") — **D, E i el commit de docs queden explícitament per a la
propera sessió.**

## 1. Ordre de lectura

1. Aquest document.
2. `docs/PLA-PENDENTS-0B-0C-0D-2026-08-26.md` — **§7 (Fix D) i §8 (Fix E) sencers**: tot el detall fitxer:línia,
   pseudocodi de `cadastre_reader.py`, tests, mesures i acceptació hi és. Aquest handoff no el repeteix.
3. `docs/wizard-headless/fase12-consolida/_RESULTATS.md` §8 (afegit avui): la línia base «Comparador v3» pre-fixos
   i els resultats mesurats de F i A (§8.1, §8.2).
4. `docs/golden-read-taules/_RESULTATS.md`, secció «Fix B» (afegida avui).
5. `MEMORY.md`: `project_pla_0b0c0d_not_implemented_decisions_2026-08-31` (les dues decisions del Josep, ja
   resoltes — no tornis a preguntar), `reference_cadastre_not_truth_for_parcel_area`,
   `project_groq_residues_wrong_values_pending`, `feedback_measure_baseline_before_coding`,
   `feedback_no_pull_eva_success_criterion`.
6. `STATUS.md` capçalera i secció «Open items» 0b/0c/0d (encara **no reescrita** — reflecteix l'estat pre-sessió;
   fes-ho al commit de docs, no abans).

## 2. Fet aquesta sessió (commits, en ordre)

| Fix | Què | Commit | Mesurat |
|---|---|---|---|
| F | Comparador v3: veredictes `BUIT`, `CAUTELA candidats disjunts`, `FORA` | `3694694` | 0 `OK`→`ALERTA` als 5 jocs |
| A | `value_key`: dates amb `fullmatch`, guió entre dígits = interval | `d6aa855` | 0 verdictes canvien (forat latent) |
| B | Or de Castellar `soil_levels[1].a` en dialecte v2 | `e8d1cc8` | `sonnet-v2-c3` TAULES OK 25→26, CAUTELA 4→3 |
| C | Groq: `lab_company` fora + cap probe de mapa/foto emet superfícies | `a423f6a` | harness idèntic (no passa per Groq); 1599/32 |

Suite sencera després de C: **1599 passed / 32 failed** (els mateixos 32 coneguts — SmartScan, ai_pipeline,
fileminer Anciles, `test_groq_miner::TestCache::test_cache_hit`). Verifica't-ho abans de tocar res.

## 3. Regles interpretatives (apreses avui, no al pla original)

- **`flat_gold_scalars` (compare_consolida.py) no propagava `candidates` ni `fora_carpeta`** dels fixtures d'or
  d'escalars — només `estat`/`value`. Corregit dins del Fix F (calia perquè `candidats` vs `candidats` ara mira
  valors). Si tornes a tocar aquesta funció, recorda que el camp niuat `lab`/`cte` comparteix UNA sola llista de
  candidats per a totes les subclaus (mateix patró que `contract._flatten_nested_field`): `cte_sol` surt
  `CAUTELA candidats disjunts` perquè l'or només documenta el candidat de `cte_edificacio` — **no és un bug, és el
  fixture que no cobreix `cte_sol`**; no ho "arreglis" inventant un candidat.
- **`ALERTA` → `BUIT`/`CAUTELA disjunts` no és una regressió**: és exactament el que el Fix F havia de fer visible.
  Si el harness et sorprèn amb noves línies `CAUTELA`/`BUIT` en un fix futur, primer comprova si encaixen amb
  aquesta categoria abans de sospitar d'un bug.
- **La probe cachejada de Castellar (`superficie_parcela_m2=32980`) ja estava filtrada pel codi actual** abans del
  Fix C — `_VISION_DETECTABLE_CONCEPTS` només conté el nom nu (`superficie_parcela`), no l'àlies `_m2`, i
  `_parse_probe_result` ja rebutjava per membresia estricta. El forat REAL era que un `map`/`site_photo` amb el nom
  **nu** i correcte (`superficie_parcela`) passava sense filtre; ara els dos punts de tall ho aturen. La `concept_map.json`
  de Castellar SÍ que té l'entrada stale amb l'àlies `_m2` (evidència que el segon punt de tall — `concept_sources_to_signals`
  — és necessari, no redundant).

## 4. Supòsits que aguanten el pes (no desfer) — vigents des dels handoffs anteriors + nous d'avui

| On | Què | Per què |
|---|---|---|
| `compare_consolida.verdict()` | `BUIT`/`FORA` són rang 1/0 a `harness.RANK`; missatges de `BUIT`/`CAUTELA disjunts`/`FORA` són literals estables (el harness fa `diff` de text) | canviar el text trenca el `diff` abans/després de mesures futures |
| `groq_miner._parse_extractions` | guarda `if variable not in TARGET_VARIABLES: continue` abans de qualsevol altre filtre | perquè el model no torni variables no demanades (defensa general, no només `lab_company`) |
| `vision_probe._parse_probe_result` + `concept_scout.concept_sources_to_signals` | tots dos filtren `superficie_parcela`/`superficie_construida` (i l'àlies `_m2` només al segon) quan `doc_type in {map, site_photo}` | dos punts d'entrada diferents al pool de senyals (probes noves vs. `concept_map.json` cachejat) |
| `consolidate.py:1611-1616` (porta dels forats) | `http_field_signals`/futur `cadastre_portal_signals` només es criden si cap document diu res del camp | «cap font Python pot guanyar contra la lectura» |
| `consolidate.py:1291-1295` | la base de l'últim nivell sempre baixa a `candidats` | regla del Pas 3b — el Fix B confirma que l'or de Castellar hi coincidia, només calia el dialecte v2 |
| `web/lectura_service._apply_lectura_overlay` | `no_trobat` mai escriu ni esborra | regla 4 del contracte — el Fix C explícitament NO la trenca |
| `geocode_coordinates._pick_nearest_rc_from_numerero` (L.967) | treu la lletra del portal i tria el més proper | per això `cadastre_reader.py` (Fix D) NO pot delegar-hi el portal — ha de filtrar `(pnp, plp)` exactes |

## 5. Procediments

```bash
git branch --show-current      # experiment/nivell-a-2026-08 (in-place a g3dt-prod/)
git log --oneline -5           # a423f6a (Fix C) ha de ser HEAD o el més recent

.venv/bin/python -m pytest tests/ -q -p no:cacheprovider          # ~3-4 min → 1599 passed / 32 failed
.venv/bin/python docs/wizard-headless/fase12-consolida/harness.py --write /tmp/.../scratchpad/postC
# hauria de ser IDÈNTIC a docs/wizard-headless/fase12-consolida/_RESULTATS.md §8 (secció "Comparador v3" + 8.1/8.2/8.3 aplicats)

# abans del Fix D: mira què retorna el lookup existent de la via B (NOMÉS lectura, no l'usis directament)
grep -n "def cadastre_progressive_lookup" -A 30 automation/geocode_coordinates.py

# reproduir l'API en viu per fer fixtures (Annex B del pla)
curl -s 'https://ovc.catastro.meh.es/OVCServWeb/OVCWcfCallejero/COVCCallejero.svc/json/Consulta_DNPLOC?Provincia=BARCELONA&Municipio=CASTELLAR%20DEL%20VALLES&Sigla=CL&Calle=ARBRELLS%20DELS&Numero=18'
curl -s 'https://ovc.catastro.meh.es/INSPIRE/wfsCP.aspx?service=wfs&version=2.0.0&request=GetFeature&STOREDQUERIE_ID=GetParcel&refcat=3298012DG2039N&srsname=EPSG::25831'
```

- `.venv/bin/python` sempre; `cd` al worktree `g3dt-prod/` (el cwd fa ombra al `PYTHONPATH`).
- `rtk` reescriu `grep`/`find`: `find … -not` i `\|` fallen → `rtk proxy find …`. `unzip` no existeix.
- Workspace E2E: `~/g3dt-e2e/projectes/` (Castellar i Bell-lloc). **Mai** escriure a `/mnt/c`.
- Cache nova que el Fix D crearà: `config.cache_dir("cadastre_portals")` (TTL 90 dies, respecta `G3DT_CACHE_DIR`).

## 6. Seqüència d'obertura suggerida

1. `git branch --show-current` + `git log --oneline -5` + suite completa → confirma `1599 passed / 32 failed`. Si no
   quadra, atura't i esbrina per què abans de tocar res.
2. Harness sencer, apunta que coincideix amb `_RESULTATS.md` §8 post-C (hauria de ser el mateix que després del Fix
   C, ja que D encara no existeix).
3. Llegeix `PLA-PENDENTS-0B-0C-0D-2026-08-26.md` §7 (Fix D) sencer una vegada abans d'escriure cap línia.
4. Obre `geocode_coordinates.cadastre_progressive_lookup` (L.1006-1250 aprox.) per decidir si `cadastre_reader.py`
   el reutilitza o crida `_consulta_municipio`/`_consulta_via` directament (pla §7.2 pas 2) — **decisió tècnica
   teva, no cal preguntar al Josep**.
5. Crea `automation/lectura/cadastre_reader.py` seguint §7.2-7.6 (portals_from_address, resolve_portal,
   parcel_area_and_polygon, cadastre_portal_signals) amb fixtures reals de Castellar a
   `tests/fixtures/cadastre_castellar/` (Annex B del pla té els exemples).
6. Cablejat a `consolidate.py` (§7.4), comentaris actualitzats (§7.5), or de Castellar amb `fora_carpeta` (§7.8).
7. Mesura les DUES passades del §7.7 (Cadastre OFF per defecte, després `G3DT_LECTURA_HTTP_SOURCES=icgc,geocodificacio,cadastre`).
8. **El Josep ja ha autoritzat encendre `cadastre` per defecte** (memòria `project_pla_0b0c0d_not_implemented_decisions_2026-08-31`,
   pla §11 punt 1 marcat ✅) — pots flipar `_HTTP_SOURCES_DEFAULT` a la MATEIXA commit del Fix D en comptes de
   deixar-lo apagat i esperar un follow-up (el pla original deia "deixa'l apagat" perquè encara no hi havia decisió;
   ara sí que n'hi ha). Documenta-ho igualment al DECISION-LOG com demana el pla.
9. Fix E (§8): antes de la crida LLM real (§8.4), **el Josep també ja l'ha autoritzada** amb el model per defecte
   del runner (`sonnet`) — no cal tornar a preguntar ni el model ni el permís, només fer-la i documentar
   `source_md5` + temps.
10. Docs (commit 7, §9): DECISION-LOG (9 seccions), STATUS (reescriu 0b/0c/0d L.257-296), PREGUNTES-EVA (preguntes
    9 i 10 noves), `_RESULTATS.md` ×3, session log, i un `/for-new-you` final abans de passar a la Fase 16.

## 7. Què NO fer

- No tocar la via B: `automation/ai_pipeline/`, `web/vision_groq.py`, `automation/auto_extractor.py`,
  `web/vision_fast.py`, `automation/geocode_coordinates.py`, `automation/parcel_resolver.py`,
  `automation/cadastre_adjacents.py` (importar sí, modificar no).
- No delegar el portal amb lletra a `callejero_address_to_rc`/`_pick_nearest_rc_from_numerero` (18B → 18A és el bug
  conegut). Filtra `(pnp, plp)` exactes.
- No calcular l'àrea amb `shapely`: `areaValue` oficial del WFS (441+423+420 = 1284 exacte; shapely arrodoneix malament).
- No sumar parcel·les no contigües ni portals ambigus (número sense lletra quan el Cadastre en té amb lletra).
- No fer que `no_trobat` esborri valors de la via B (regla 4 del contracte, decisió presa al pla §6.3).
- No editar l'or de Bell-lloc a mà (les cel·les gràfiques −0,30/−0,4 són lectura humana legítima).
- No `git stash` (pila compartida entre worktrees); no `--model opus` pensant que és Opus 4.8 (és Opus 5).
- No proposar pull/merge/desplegament a l'Eva, mai (memòria `feedback_no_pull_eva_success_criterion`).

## 8. Tasques obertes (per ordre)

1. **Fix D** — `automation/lectura/cadastre_reader.py` + cablejat + or `fora_carpeta` de Castellar (pla §7, ~3-4h).
2. **Fix E** — skill v1.6 + regles E2/E2b + re-lectura del tall de Bell-lloc amb 1 crida LLM ja autoritzada (pla §8, ~2h).
3. **Docs** (commit 7): DECISION-LOG, STATUS, PREGUNTES-EVA (preguntes 9+10), `_RESULTATS.md` ×3, session log.
4. **Fase 16** — E2E dels tres botons amb temps remesurats (només després de D/E/docs, no abans; nota: amb Cadastre
   ON per defecte la primera consolidació de cada projecte farà crides HTTP noves — si mesures la 16 amb això
   actiu, digues-ho a la taula de resultats).
5. Fase 17 (Windows presencial), sense canvis respecte als handoffs anteriors.

## 9. Guanys ocults d'aquesta sessió

- El forat de `flat_gold_scalars` (§3 d'aquest doc) no era al pla — el vaig trobar mesurant el Fix F i, si no
  l'hagués corregit, hauria contaminat qualsevol mesura futura d'escalars amb candidats (rellevant per D i E, que
  toquen precisament escalars amb candidats com `superficie_parcela`/`referencia_catastral`).
- Vaig confirmar empíricament (no només per lectura de codi) que el forat de `superficie_parcela_m2` del Cadastre
  NO era on el pla pensava (`_parse_probe_result` ja el bloquejava per l'àlies) — el forat real era el nom nu des
  d'un mapa, i vaig trobar l'evidència del segon punt de tall necessari (`concept_map.json` de Castellar amb
  l'entrada stale). Si mai es reobre aquest tema, l'anàlisi ja està feta i verificada, no cal repetir-la.
