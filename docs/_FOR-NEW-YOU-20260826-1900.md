# FOR NEW YOU — 2026-08-26 (1900) — Tram 1 TANCAT; següent: **Fase 16**, i després **repassar els pendents apuntats** (no la 17)

**Escrit:** 2026-08-26, vespre · **Per a:** la sessió que faci la Fase 16.
**Recap:** el tram 1 sencer en una tarda — 13(a+b), capa vegetal, 11, 14a, 14b, 15. TEMPS 1 passa de 40,4 s a 5,6 s,
el consolidador ja veu l'ICGC i la geocodificació, la capa vegetal recupera les seves fondàries, i l'Eva té tres botons,
una taula que parla el seu idioma, delta-sync i avisos. 12 commits, de `65786a1` a `44fc447`.
**Ordre nou (Josep, 2026-08-26 vespre):** després de la Fase 16 **NO** anem a la 17; repassem els pendents apuntats.

## 1. Ordre de lectura (10 min)

1. Aquest document.
2. `STATUS.md` — la capçalera i els **open items 0b, 0c i 0d**, que són la feina de després de la 16.
3. `docs/wizard-headless/fase13-http-cache/_RESULTATS.md` i `docs/wizard-headless/fase14-15-botons-avisos/_RESULTATS.md`.
4. `docs/DISSENY-ANNEX-TRES-BOTONS-JOBS-NOTIFICACIONS-2026-08-24.md` **§2** (taula de temps a omplir), **§11**
   (criteris d'acceptació) i la fila 16 de §10. És l'especificació de la feina.
5. `docs/DECISION-LOG.md`, entrada «2026-08-26 (vespre i nit)» — les 9 decisions i per què.
6. `docs/PREGUNTES-EVA-PENDENTS.md` (8 obertes; la 8 és nova d'aquesta sessió).
7. MEMORY.md: `feedback_no_pull_eva_success_criterion`, `feedback_measure_baseline_before_coding`,
   `project_via_a_claude_code_reader`, `reference_cadastre_not_truth_for_parcel_area`,
   `project_groq_residues_wrong_values_pending`, `project_soil_levels_gold_vs_pas3b_open`.

## 2. Marc fixat (no re-litigar)

- Objectiu: **l'informe complet a la millor qualitat** (341 variables). El nivell A és el pas 1.
- **Mai** proposar pull/merge/desplegament a l'Eva. Això és feina de branca.
- Via B **prohibit editar-la** (`automation/ai_pipeline/`, `web/vision_groq.py`, `auto_extractor.py`,
  `web/vision_fast.py`). Tot el que s'ha fet avui l'envolta.
- §8 **decidit** (Josep): es mantenen els tres residus Groq. No tornar-hi.
- El Cadastre al consolidador queda **apagat** i és decisió del Josep encendre'l.

## 3. Regles interpretatives (què és senyal i què és soroll)

- **Instrument de lectura:** `docs/wizard-headless/fase12-consolida/harness.py` (comparador d'or, 5 jocs, 0,03 s).
  **Instrument d'informe:** `scripts/compare_tables_vs_eva.py`. No barrejar-los (memòria
  `reference_compare_tables_vs_eva_harness`).
- Al harness, una línia de temps que canvia (`0.029 s` → `0.039 s`) **no és un canvi**: compara els verdicts, no el text
  sencer. `diff <(grep -vE '^===== ' A) <(grep -vE '^===== ' B)`.
- `ALERTA` amb `prod puja a segur` és un **erroni-amb-confiança**: la categoria que el projecte no tolera.
  `CAUTELA` (or `segur`, prod `candidats` amb el bo dins) no ho és.
- La Fase 16 mesura **paret**, i la paret depèn de la xarxa i de la càrrega del CLI. Una diferència de 2-3 min entre
  runs no és senyal. El que és senyal: 3a per sobre d'1 min, o 3b per sobre de 10 min (§11).
- **Groq 503 «over capacity»** apareix sovint a l'ortofoto durant TEMPS 1: és soroll conegut, no una regressió.

## 4. Supòsits que aguanten el pes (no desfer)

| On | Què | Per què no desfer-ho |
|---|---|---|
| `automation/auto_result_cache.py` | empremta = md5 del **contingut**, no mida+mtime | el delta-sync recopia fitxers; amb mtime la cache no encertaria mai després d'un sync |
| idem, `_COMPANION_OUTPUTS` | `file_mapping.json` i `concept_map.json` són fora de l'empremta però es comprova que existeixin | `_merge_prefills` les rellegeix del disc; si desapareixen, un encert serviria dades incompletes |
| `consolidate.http_field_signals` | es crida a la MATEIXA porta que `derived_field_signals` (només si cap document diu res) | és la versió estructural de «cap font Python pot guanyar contra la lectura»; afinar confiança per no bloquejar és fràgil |
| `consolidate._HTTP_SOURCES_DEFAULT` | el Cadastre NO hi és | mesurat: a Castellar dona 441 m² i l'Eva escriu 1.284 |
| `consolidate._group_soil_levels` | la fila que arrenca a la superfície va a la capa vegetal si aquesta no té fondàries | sense això la fila 0,00-0,50 del full de camp feia una tercera fila espúria |
| `automation/lectura/job_text.py` | el text de la taula es redacta en Python | `review.html` fa 10.000 línies i no té cap test |
| `notify.sanitize_log` | **llista blanca**, no substitució | el log del CLI porta valors de camps; cap substitució ho cobreix |
| `notify` `technical_only=False` | només per al missatge d'`error_event` | l'escriu el nostre codi; del log del model no en controlem la forma |
| `sync_workspace._LOCALLY_PRODUCED` | l'informe i els JSON locals no es poden llegir com a «esborrats a la xarxa» | errar-hi seria apartar l'informe de l'Eva a `_esborrats/` |
| `review.html`, bloc Fase 14 | `nbSetState` s'**embolcalla**, no s'edita | treure el bloc no ha de deixar rastre |
| `GET /api/lectura/enabled` | sonda de 200 en lloc de llegir un 404 | un `fetch` d'un 404 deixa línia vermella a la consola encara que es gestioni |

## 5. Procediments

```bash
git branch --show-current      # → experiment/nivell-a-2026-08 (in-place a g3dt-prod/; esperat)
git log --oneline -1           # ha de ser 44fc447 (o el commit d'aquest handoff)

# Tests (línia base 32 failed CONEGUTS: SmartScan, ai_pipeline, fileminer Anciles)
.venv/bin/python -m pytest tests/ -q -p no:cacheprovider            # ~2,5-3,5 min → 1569 passed / 32 failed

# Comparador d'or (0 crides LLM, 0,03 s per joc)
.venv/bin/python docs/wizard-headless/fase12-consolida/harness.py
.venv/bin/python docs/wizard-headless/fase12-consolida/harness.py --only sonnet-v2-c3 --write /tmp/out

# Servidor amb la UI nova (flag ON) i xarxa simulada
G3DT_NETWORK_PROJECTS=/ruta/net G3DT_LOCAL_WORKSPACE=/ruta/ws \
  G3DT_USE_LECTURA_HEADLESS=true .venv/bin/python -m web          # http://localhost:8765/review.html
curl -s 'http://localhost:8765/api/jobs?refresh=true' | .venv/bin/python -m json.tool | head -40

# Aturar el servidor: MAI `pkill -f 'python -m web'` (es mata la pròpia shell de l'eina)
PID=$(ss -lptn 'sport = :8765' | grep -o 'pid=[0-9]*' | head -1 | cut -d= -f2); kill "$PID"

# Delta-sync a mà
.venv/bin/python -c "from automation import sync_workspace as S; print(S.sync_delta_for_leaf('3001621 CASTELLAR DEL VALLES', check_only=True))"
```

- **Workspace E2E:** `~/g3dt-e2e/projectes/` (Castellar i Bell-lloc ja hi són, amb `validation/lectura/` poblat).
  **Mai** escriure a `/mnt/c`.
- **`_auto_result.json`** és a `.gitignore` i viu a `validation/` de cada projecte. Esborrar-lo = TEMPS 1 fred.
- `rtk` reescriu `grep`/`find`: `find … -not` i patrons amb `\|` fallen → `rtk proxy find …`.
- `unzip` no existeix: obrir `.docx` amb `zipfile`/`python-docx`. `aiosmtpd` tampoc (el test SMTP és de casa).
- El navegador es fa servir amb l'MCP de Playwright (`mcp__playwright__browser_*`); netejar `.playwright-mcp/` després.
- **L'`ANTHROPIC_API_KEY` del `.env` ja té saldo** (Josep hi ha posat 10 €, 2026-08-26). Afecta la via B
  (`_synthesize_with_llm`, visió per tipus), **no** la via A, que va per subscripció del CLI.

## 6. Seqüència d'obertura suggerida

1. `git branch --show-current` + `git log --oneline -3`.
2. `.venv/bin/python -m pytest tests/ -q -p no:cacheprovider` → 1569/32. Si no, atura't: alguna cosa ha canviat sota.
3. `.venv/bin/python docs/wizard-headless/fase12-consolida/harness.py` → `sonnet-v2-c3` ha de donar
   `TAULES {'OK': 25, 'CAUTELA': 4}` **sense cap ALERTA**. És la firma de l'arranjament de la capa vegetal.
4. Llegir §2 i §11 del disseny de l'annex: la Fase 16 omple una taula que ja existeix, no n'inventa una.
5. Muntar l'escenari de la 16 **abans de cridar cap LLM**: dues carpetes locals (una fa de xarxa, l'altra de
   workspace), Castellar sol, `validation/lectura/` esborrat per al run fred.
6. Decidir amb el Josep el **model** del run (`G3DT_LECTURA_MODEL`/`G3DT_LECTURA_EFFORT`): la decisió del 25/08 és
   Fable @xhigh quan el pla ho permeti, amb Opus 4.8 @high de reserva; el defecte del runner segueix sent `sonnet`.
   Un run sencer són 25-40 min de paret i consumeix subscripció: val la pena preguntar abans.
7. Còrrer, mesurar, i escriure l'addendum. No optimitzar res durant la 16.

## 7. Què NO fer

- No fer `pkill -f` amb un patró que aparegui a la pròpia comanda (es mata la shell de l'eina).
- No editar `web/api.py` amb `str.replace(..., 1)` sobre el bloc de sync: és **textualment idèntic** al
  `POST /api/jobs` i a `/api/prefills-stream` (via B), i avui ja hi va caure un canvi per error. Ancorar en text únic.
- No tocar la via B ni `sync_to_workspace`.
- No encendre el Cadastre (`G3DT_LECTURA_HTTP_SOURCES`) sense decisió del Josep.
- No «arreglar» l'ALERTA de Bell-lloc des de `consolidate.py`: és un forat de **lectura**, no hi ha res al corpus amb
  què encaixar-ho, i partir la fila és la regla del Pas 3b que espera la pregunta 3 de l'Eva.
- No editar `docs/golden-read*` ni `_eva_truth/` per fer quadrar cap mesura.
- No commitejar `docs/diagnostics/ai_pipeline_demo-*.md` (els genera la suite), `photo_selection.json` ni `.playwright-mcp/`.
- No `git stash` (pila compartida entre worktrees); WIP commit si cal apartar feina.
- No `--model opus` pensant que és 4.8 (`claude-opus-4-8`).

## 8. Tasques obertes

1. **Fase 16 — E2E dels tres botons.** Castellar sol, còpia local amb «xarxa» simulada (segona carpeta): botó 2, tocar
   2 fitxers a la «xarxa», botó 3. Omplir la taula de §2 amb xifres reals per a 1, 2, 3a i 3b; addendum a
   `docs/wizard-headless/fase14-15-botons-avisos/_RESULTATS.md` + entrada a DECISION-LOG.
   **Acceptació (§11):** botó 3a ≤ 1 min de màquina; botó 3b ≤ 10 min amb 2 documents canviats.
   Estimacions a validar: botó 1 i 2 = 20-25 min; 3a ≈ 30 s; 3b = 6-9 min.
2. **Després de la 16: repassar els pendents apuntats** (ordre nou del Josep, substitueix la Fase 17):
   - **0b Cadastre** — per què falla i com corregir-ho. Mesurat: 441 m² vs 1.284 de l'informe signat; parcel·la
     equivocada a 4/8. Preguntes: la geocodificació cau a la parcel·la veïna? el WFS retorna una subparcel·la? l'Eva
     suma parcel·les contigües? Probablement lligat a la validació visual de parcel·la (P4).
   - **0c Residus Groq** — `superficie_parcela_m2=32980` i `lab_company='Lab. Valdemoro'` no els tapa ningú avui.
     Dues vies: que un `no_trobat` de la lectura **esborri** el valor de la via B, o filtrar aquests camps a l'origen.
   - **0d `soil_levels`** — la CAUTELA de Castellar (pregunta 8 a l'Eva) i l'ALERTA de Bell-lloc (forat de lectura).
   - Forat de la Fase 8: `consolidate.value_key("1,5-1,75")` es parseja com a data (latent).
3. **Fase 17** (Windows presencial) queda per més endavant: `claude` CLI natiu + login, `schtasks /sc onlogon`, toast
   real, experiment Outlook COM, `--strict-mcp-config`, i la línia de transparència de §6.4 a `INSTALL-EVA-v1.md`.
4. **Decisions del Josep encara obertes** (§12 del disseny): pla/model, contingut del correu de l'Eva, Brevo vs bústia
   a `mail.eficients.cat`, encàrrec de tractament amb G3, servidor viu a Windows.
5. Pendents menors: Bell-lloc complet al llibre; hold-out amb carpetes noves de l'Eva.

## 9. Guanys ocults de la sessió

- **Hi havia una via per la qual el wizard podia servir dades velles sense dir-ho**: `sync_to_workspace` feia `skipped`
  quan el workspace existia, i el job es posava a llegir fitxers antics. Ningú ho hauria vist fins que un informe
  hagués sortit amb un plànol superat.
- **El test de notificacions va destapar una fuita real**: el log del CLI porta valors de camps, no només noms de
  fitxer. El disseny proposava una substitució que no hi arribava. Ara el criteri és de llista blanca, i és
  precisament el tipus d'error que no s'hauria vist fins que hagués sortit per correu.
- **13(a) va resultar ser l'habilitador de 13(b)**, cosa que no estava prevista: dona al consolidador un registre a
  disc, datat i validat per empremta, de les consultes HTTP — sense xarxa i sense dependre de l'ordre dels fils.
- **La mesura va convertir «connectar el Cadastre» en una troballa**: al projecte de referència el Cadastre no és una
  font de veritat per a la superfície de parcel·la. Sense mesurar, s'hauria encès i hauria semblat una millora.
- **Els dos jocs de Castellar ara coincideixen** al comparador d'or: la fila espúria de la capa vegetal emmascarava una
  regla del Pas 3b que `sonnet-c3-v13` ja aplicava.
