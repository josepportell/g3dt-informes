# FOR NEW YOU — 2026-09-07 (suffix 1130) — IMATGES de l'informe: inventari, veritat des dels signats i disseny figura a figura. Pas 1 sense tocar el generador

**Escrit:** 2026-09-07, després del bloc 4 (handoff `_FOR-NEW-YOU-20260907-1100.md`, que descriu el bloc 4 sencer; conserva'l i
llegeix-ne la secció «Obert»). Josep, en veure els resultats de les imatges: «No estic gaire satisfet dels resultats que hem obtingut
en aquesta fase. Penso que ho podem fer força millor. […] revisar de quines imatges disposem a la documentació dels projectes. I
mappejar-ho amb les que aniran a l'informe (versus les que són auxiliars per redactar-lo, no per mostrar-les a l'informe). […]
cadascuna pot requerir un anàlisi amb cura.» Aquesta sessió és per fer-ho bé, amb tota la finestra de context.

Branca `experiment/nivell-a-2026-08`, in-place a `g3dt-prod/` (estat esperat). **Bloc 4 COMMITEJAT** (GO del Josep, 2026-09-07):
`78d8d1d` (extractor de numeració + tests) · `64ce2d9` (mesura `fix` + presència d'imatges + `--label`) · `85b9c6b` (99 veritats
`*_num`) · `4afcc80` (cau d'imatges per contingut, `_cache_name`) · `ea699ea` (docs i runs). Arbre net (només
`reference-material/4001607 LINYOLA/validation/photo_selection.json` sense versionar, que ja hi era). El pas 1 d'aquí no toca
`automation/` (nou codi a `docs/imatges/` o `docs/wizard-headless/mesures/`).
`production/g3dt-eva-v1` @ `c46bc69` sense push; mai proposar pull/merge a l'Eva (memòria `feedback_no_pull_eva_success_criterion`).

## 1. Per què: el que sabem avui de cada figura (bloc 4, `runs/2026-09-07-m341-bloc4b/_IMATGES.md` + `imatges/*.jpg`)

La mesura del bloc 4 només diu **presència** (present / pendent / absent): 53 · 8 · 16. «Hi és» no diu «és la que l'Eva posaria».
El full de control visual, fet a mà una vegada, va destapar el que cap mesura veia: la cau d'imatges compartia el tall de correlació
de Bell-lloc amb 5 projectes (arreglat, `ImageManager._cache_name` per md5 del PDF), i aquests defectes de contingut, encara oberts:

| Forat de la plantilla | Com s'omple AVUI (`automation/image_manager.py`) | Què posa l'Eva als signats | Defecte vist |
|---|---|---|---|
| `fig_cadastre_image` (Figura 1, esquerra) | retall del 38 % esquerre de l'annex «plànol de situació» (`_render_situation_plan_left`) | cada signat diferent: topogràfic + ortofoto ICGC en UNA figura (Castellar, Rubí), mapes cadastrals (Bell-lloc), «Ubicació de la parcel·la» (Linyola), Sede del catastro (Anciles) | depèn de la maquetació del seu annex: A3 bé, A4 (Linyola) tira de 629×2339 il·legible |
| `fig_aerea_image` (Figura 2) | ortofoto ICGC + polígon cadastral, per UTM (`_download_icgc_images`) | sovint fusionada amb el topogràfic en la Figura 1 | sense UTM a la via A (Rubí, Vilanova, Anciles) no hi ha res |
| `fig_main_plan_image` (Figura 3) | pàgina sencera de l'A.01 (rol `architect_plan`), o bbox de la visió, o «A.*.pdf» | gairebé sempre el plànol **amb els punts d'assaig dibuixats per ella** («Emplaçament de l'habitatge projecte i els assaigs realitzats», «Situació de l'estructura projectada i els assaigs realitzats», «Situación de los ensayos realizados») i, a Linyola/Anciles, figures extra (perfil, topogràfic, tipologia) | Rubí: foto WhatsApp d'un plànol imprès; Anciles: portada «IV. DOCUMENTACIÓN GRÁFICA» (pàgina 1 d'`IV_PLANOS.pdf`); Castellar/Vilanova: pendent (cap rol). La memòria `report_figure_sources` (Eva, 2026-03-14) diu «SENSE punts»: **contradiu els signats; reobrir** |
| `fig_spt_cullera_image` (Figura 4) | estàtica (`templates/…`) | la mateixa | cap |
| `fig_geological_image` (Figura 5) | ICGC WMS per UTM (composite amb punt vermell) | «Mapa geològic 1:50.000 ICGC, modificat» que ELLA compon (annex `6_mapa Geologic_CAT_VS.FH11`, PNG `M1.png` / `F4 MGEOL.png` al projecte); Anciles (Aragó): IGME 1:1.000.000 | preferim baixar l'ICGC al PNG que ja hi ha (Vilanova té `F4 MGEOL.png` i surt pendent); fora de Catalunya no hi ha font |
| `fig_correlation_image` (Figura 6) | `tall.pdf` (rol `correlation_section` o glob), pàgina 1 | el seu annex de tall | ara correcte 7/7 (cau arreglada); Rubí sense `tall.pdf` va per un PNG |
| `photo_site_image_1/2` (Fotografia 1 i 2, condicionals) | `photo_selection.json` amb `source=user` (Bell-lloc), si no 0 | 0 (Castellar, Linyola, Alcoletge, Anciles), 1 (Rubí: Google Earth; Vilanova), 2 (Bell-lloc) | la via A no en tria; el número de la resta de fotos s'arrossega (Rubí 2 X) |
| `photo_dpsh_image` | tria de fotos: heurística de nom + Groq/Claude (`select_photos_ai`, `PHOTO_SELECTION_PROMPT`, ranures site_1/site_2/dpsh/sondeig/materials) sobre `FOTOGRAFIES/` | foto de la màquina | Alcoletge: foto del FULL DE CAMP manuscrit |
| `photo_sondeig_image` | idem | foto de la màquina de sondeig | Anciles: caixes de testimonis |
| `photo_materials_image` | idem, UNA foto | una foto de materials (o una per SONDEIG: Anciles Foto 3 S-1 i Foto 4 S-2) | la plantilla la repeteix per NIVELL amb el mateix número (p254-262, `{%p for level in soil_levels %}`) |

Conseqüència sobre la numeració (bloc 4): 10 de les 25 X del grup `fix` són perquè la plantilla imprimeix sempre 3 figures del
projecte i els signats en tenen 2 (Castellar, Rubí, Alcoletge) o 4 (Anciles). Si les figures segueixen el que hi ha, aquestes X cauen soles.

## 2. La idea que canvia el joc: els signats PORTEN les imatges

Fins avui les imatges no tenien veritat. En tenen: els 7 signats (`RE.KNOWN_REPORTS`) convertits a `.docx` amb soffice conserven
les imatges a `word/media/` (comprovat 2026-09-07: Castellar 12 media (5 png, 6 jpeg, 1 **wmf**), Rubí 11, Linyola 10, Bell-lloc 13,
Alcoletge 9, Vilanova 12, Anciles 14; 2-7 MB per informe). Amb el peu que segueix cada imatge (mateix mètode que el full de control:
`document.xml` → `a:blip r:embed` → `word/_rels/document.xml.rels` → `word/media/*`; peu = següent paràgraf «Figura / Fotografia /
Taula N.») tenim, per projecte i per figura, **la imatge que l'Eva va posar**. I amb un hash perceptual (`imagehash` 4.3.2 al venv;
PIL 12) contra tots els fitxers d'imatge del projecte sabem de quin fitxer va sortir, o si la va compondre ella. D'aquí surt sol el
mapa que demana el Josep: imatge del projecte → va a l'informe / és auxiliar per redactar / no s'usa. I la mesura pot passar de
«hi és» a «és la mateixa font que la de l'Eva».

Segona palanca: **els annexos de l'Eva existeixen abans del wizard** (memòria `eva_workflow_annexes_before_wizard`, Josep 2026-08-23):
plànol de situació, tall, sondeig i, sobretot, l'**annex de fotografies** (`ANNEXES/<exp>_fotografies.FH11` i l'export
`PDF/ANNEXES/<exp>_fotografies.pdf`): és la SEVA selecció de fotos, avui no la mirem per a res. Els FH11 es converteixen amb
`soffice --convert-to pdf` (memòria `reference_fh11_readable_via_libfreehand`; text reflueix, imatges fidels).

## 3. El pla (3 passos; aquesta sessió = pas 1 sencer i preparar el 2)

### Pas 1 — Inventari, veritat i aparellament (determinista, cost 0, cap canvi a `automation/`)

1. **Veritat**: per als 7 signats, extreure (imatge, peu, ordre) → `docs/imatges/veritat/<projecte>/NN_<peu curt>.<ext>` + un
   `index.json`. Treure les estàtiques de la plantilla (logo, cullera, capçaleres: la mateixa md5 en ≥ 5 signats). Convertir `.wmf`
   a png (soffice o `wand`) si n'hi ha. Els peus d'Anciles/Vilanova són en castellà («Figura», «Fotografía», «Tabla»).
2. **Inventari del projecte** (els 7 de `~/g3dt-e2e/projectes/<nom>/`, que són còpia de `reference-material/<nom>/`): tot fitxer
   d'imatge (jpg/jpeg/png/heic/tif; 6-30 per projecte, carpetes `FOTOGRAFIES/` o «FOTOS DE CAMP + PLANOL PUNTS» a Alcoletge),
   tot PDF renderitzable pàgina a pàgina (annexos `PDF/ANNEXES|ANEJOS/*.pdf`, plànols de l'arquitecte `A.01.pdf`, `A.01 amb
   punts.pdf`, `IV_PLANOS.pdf`, `1.0.pdf`, `SITE_prop A.pdf`, `pl situ.pdf`, `tall.pdf`), els FH11 (3-5 per projecte, via soffice),
   els PNG de mapes (`M1.png`, `F4 MGEOL.png`), adjunts dels `.msg` (memòria `project_email_attachment_blindspot`: 117 adjunts
   sense rol). Per a cada un: tipus (foto de camp / plànol / annex de l'Eva / mapa / captura / full de camp escanejat), mida,
   pàgines, `file_mapping.json` rol si en té (`photos_dir`, `architect_plan`, `architect_plan_with_points`, `situation_plan`,
   `figure_geological_map`, `figure_situation_map`, `figure_test_points`, `correlation_section`; `ROLE_TO_FIGURE_VAR` a
   `image_manager.py` l.≈76).
3. **Aparellament**: phash/dhash de cada imatge de la veritat contra cada candidat (imatges senceres i pàgines de PDF renderitzades;
   l'Eva sovint RETALLA o COMPON, així que a més del hash sencer cal un segon nivell: hash de la imatge de la veritat contra finestres
   del candidat, o `cv2.matchTemplate` a escala reduïda, si el venv té OpenCV; si no, PIL + finestres lliscants). Registrar per figura:
   fitxer d'origen (o «composició pròpia de l'Eva» o «no trobat» — Zero fabrication: mai inventar la font).
4. **Sortides**: `docs/imatges/INVENTARI-I-VERITAT-2026-09-<dd>.md` amb (a) una taula per projecte: figura del signat · peu · fitxer
   d'origen · el que posem avui (del `.docx` de `runs/2026-09-07-m341-bloc4b`) · candidats; (b) el mapa «va a l'informe / auxiliar /
   no s'usa» per fitxer; (c) un full visual per projecte amb tres columnes (Eva | nosaltres | candidats), `docs/imatges/fulls/<projecte>.jpg`
   (JPEG q80, ≈ 100 KB; el script del bloc 4 és el punt de partida, descrit a `_IMATGES.md`); (d) una taula-resum per TIPUS de figura
   (8 tipus) amb el que s'ha après als 7. Mesura de presència a M341 intacta.

### Pas 2 — Revisió figura a figura, Josep i jo, sobre les taules del pas 1

Per a cada tipus, decidir font i regla entre: (A) reutilitzar l'annex de l'Eva tal qual (existeix abans del wizard), (B) compondre
nosaltres (ICGC/Cadastre/plànol + punts UTM), (C) demanar-ho a l'Eva. Punts que ja sabem que sortiran:
- Figura 1: quina composició vol (topogràfic + ortofoto ICGC? cadastre?) i si l'annex de situació és la font o el resultat.
- Plànol: amb punts o sense (memòria de març diu sense; 5 signats diuen amb). Si amb punts, els dibuixa ella (`A.01 amb punts.pdf`,
  «Punts de Sondeig_Silvia_Jaume.jpg» a Alcoletge) o els podem posar nosaltres (UTM dels assaigs + georeferència del plànol: difícil).
- Mapa geològic: PNG de l'Eva quan hi és; ICGC quan no; fora de Catalunya (Anciles) IGME o el seu PNG.
- Fotos: l'annex de fotografies com a selecció; criteri de vistes generals (pregunta 19a ja oberta); materials per nivell o per sondeig.
- Nombre de figures del projecte (2/3/4) i numeració que en depèn.
Les preguntes a l'Eva es formulen aquí, amb evidència del pas 1, a `docs/PREGUNTES-EVA-PENDENTS.md` (última: 29).

### Pas 3 — Disseny i mesura per tipus (només després del 2)

Una regla per figura, el seu test, i una cel·la a M341 per figura («mateixa font que l'Eva» per hash, no només presència); la
numeració segueix les figures presents (cau el 3 fix); `photo_materials` una vegada (plantilla, decisió del Josep); cau per contingut
ja feta. Mesurar abans i després amb el codi quiet (feedback `feedback_measure_baseline_before_coding`); pèrdues al
`docs/REGISTRE-PERDUES-MESURA.md`; DECISION-LOG per bloc.

## 4. Fets, fitxers i gotchas

- Signats: `RE.KNOWN_REPORTS` (`automation/reference_extractor.py` l.55); conversió `RE.convert_doc_to_docx(path)` o `soffice
  --headless --convert-to docx --outdir <dir> <fitxer.doc>`; text: `--convert-to txt:Text`.
- Generats del bloc 4: `~/g3dt-e2e/informes-mesura/2026-09-07-m341-bloc4b/<slug>_viaA.docx`; context a
  `docs/wizard-headless/mesures/runs/2026-09-07-m341-bloc4b/<slug>/viaA/_context_usat.json` (`<InlineImage>` / `[Imatge pendent]`).
  Les 11 primeres `word/media/imageN` són estàtiques de la plantilla (mateixa md5 als 7).
- `image_manager.py`: `build_context` (l.955), `_download_icgc_images` (827; sense `utm_x/utm_y` a `user_data` no fa res),
  `_find_situation_plan` (713), `_render_situation_plan_left` (733; 38 % esquerre, pensat per a l'A3 1191×842 pt; Linyola és A4
  842×595), `_find_architect_plan_with_points` (751), `select_photos_ai` (339; cau `validation/photo_selection.json`, claus site_1,
  site_2, dpsh, sondeig, materials, `source`), `_find_photos_dir` (610), `_cache_name` (bloc 4), `PLACEHOLDER_TEXT` «[Imatge pendent]».
- Memòries a llegir: `report_figure_sources` (Eva 2026-03-14: fonts de les 3 primeres figures; reobrir el «sense punts»),
  `eva_workflow_annexes_before_wizard`, `reference_fh11_readable_via_libfreehand`, `project_email_attachment_blindspot`,
  `project_streetview_adjacents` (l'Eva vol Street View / Google Earth per a adjacents i descripcions).
- `rtk` reescriu `grep` / `git show` / `git diff`: `rtk proxy …` en pipelines. Mai `str()` d'un `InlineImage` viu. `unzip` no hi és:
  `zipfile` de Python. Els `.doc` convertits queden a `/tmp/…/scratchpad` de la sessió, no al repo.
- Cost: tot determinista (hash, render); només si es vol classificar per visió una imatge dubtosa, i llavors pregunta primer.

## 5. Què NO fer

- Cap canvi a `automation/` ni a la plantilla al pas 1: primer saber què hi ha i què hi posa l'Eva (Companions, not apps: modela
  el CRITERI de l'Eva, no una heurística nova).
- No refer la numeració (bloc 4 fet); no re-extreure veritats de text (`refresh_eva_narrativa.py`) sense dry-run.
- No omplir forats amb suposicions: una figura sense origen identificat es registra «no trobat».
- No proposar-li res a l'Eva sense l'evidència del pas 1 (i mai un pull).

## 6. Procediments

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"; git branch --show-current; git status --short | grep -v runs/ ; git log --oneline -3
# signats → docx (7) en un directori temporal de la sessió
for f in "reference-material/4001612 BELL-LLOC/4001612_informe.doc" ...; do soffice --headless --convert-to docx --outdir "$S/docx" "$f"; done
# extracció (imatge, peu) d'un docx: zipfile + lxml sobre word/document.xml (a:blip r:embed) i word/_rels/document.xml.rels; peu = paràgraf següent que comença per Figura|Fotografia|Fotografía|Taula|Tabla
# hash: from PIL import Image; import imagehash; imagehash.phash(Image.open(p)); distància = h1 - h2 (≤ 6-8 = mateixa imatge sencera)
# inventari: find "$HOME/g3dt-e2e/projectes/<nom>" -iregex '.*\.\(jpg\|jpeg\|png\|heic\|tif\|pdf\|fh11\)' -not -path '*/validation/*'
# FH11 → PDF: soffice --headless --convert-to pdf --outdir <dir> <fitxer.FH11>   (pàgines → PNG amb fitz/PyMuPDF a 100-150 dpi)
```

## 7. Seqüència d'obertura

1. `git branch --show-current` (una línia al Josep), `git status` (net), `git log --oneline -6` (els 5 commits del bloc 4 + aquest).
2. Llegir `_FOR-NEW-YOU-20260907-1100.md` § Obert, `runs/2026-09-07-m341-bloc4b/_IMATGES.md`, i les 5 memòries del § 4.
3. Pas 1 sencer (veritat → inventari → aparellament → document i fulls). Ensenyar-li al Josep les taules abans de cap conclusió.
4. Preparar el pas 2: per a cada tipus, les opcions A/B/C amb l'evidència, i les preguntes candidates a l'Eva.
