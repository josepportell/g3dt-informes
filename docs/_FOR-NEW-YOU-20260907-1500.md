# FOR NEW YOU — 2026-09-07 (tarda, 15:00) — BLOC 3 del PLA FET i mesurat (extractor de referència + veritats re-extretes). SENSE commit. Següent: GO/commit, bloc 4 o preguntes a l'Eva

**Escrit:** 2026-09-07, 15:00. Substitueix `_FOR-NEW-YOU-20260907-1230.md` (conserva'l: bloc 2). Branca `experiment/nivell-a-2026-08`,
in-place a `g3dt-prod/` (estat esperat). Bloc 2 commitejat (`bb1e82b` · `33410c5` · `b23a901` · `d8c7698`). **Bloc 3 SENSE commit**:
`automation/reference_extractor.py`, `scripts/compare_prefills_vs_eva.py`, `mesura_341.py`, `refresh_eva_narrativa.py`, 7 veritats
`reference-material/*/validation/eva_reference_values.json`, `tests/test_bloc3_extractor.py`, docs i runs. `production/g3dt-eva-v1` @ `c46bc69`.

## En una frase

Una part del vermell d'A i calc era soroll de la mesura: l'extractor de referència s'ha arreglat per clau (bucle SPT, fila portant
pel que DIU el signat, taules per etiqueta i assignació global, capçaleres ES, àncora amb forat) i les 7 veritats s'han re-extret a
totes les claus; **total 73 → 74 %, Alcoletge 66 → 75, Bell-lloc 84 → 87, Vilanova 60 → 55 (honest: recolza els pous al 2n nivell)**,
taules intactes. DECISION-LOG 2026-09-07 (tarda), decisions A-G.

## Estat exacte

- Runs de referència NOUS: `2026-09-07-m341-bloc3b` (mesura) i `2026-09-07-m341-bloc3-veritats` (només veritats, generador intacte).
  `2026-09-07-informe-bloc3-veritats` = idèntic a `-bloc2b` (els 12 informes no llegeixen les veritats).
- Suite: Suite sencera: **31 vermells amb els mateixos NOMS que `suite-vermells-esperats.txt` / 2431 verds / 5 omesos (333 s, amb les mesures en paral·lel)**.
- Pregunta 27 a l'Eva. Registre de pèrdues #6 i #7 (Vilanova, mateixa arrel).
- Partició de commit suggerida: (1) extractor + tests, (2) comparadors + M341 + refresc, (3) 7 veritats, (4) docs i runs.

## Què NO fer

- No re-extreure veritats sense `refresh_eva_narrativa.py --all-keys` (dry-run) i classificar cada clau que mou.
- No tornar la Df de Vilanova a 0,3: el signat diu «pozos … apoyada en los materiales del 2do nivel saneado».
- No «arreglar» p60 amb un regex de forats adjacents sense mirar què faria a les claus externes (`intelligent_analysis`): «new wins».
- No proposar pull/merge a l'Eva; no pujar `production/g3dt-eva-v1`.

## Què queda (del PLA)

- **Bloc 4** (numeració `fig_*_num`/`table_*_num`/… contra els 7 signats; presència d'imatges): mai mesurat.
- **Bloc 5** castellà: plantilla ES + prefixos ES a l'extractor (`municipality_de`, dates, `num_dpsh_tests`, `expedient` de
  Vilanova/Anciles són paràgrafs sencers).
- **Bloc 6**: preguntes 22-27 a l'Eva (Alcoletge plantes, Linyola «3, 4 i 5», Rubí assentament, Alcoletge Qa, antecedents, Vilanova SPT).
- p60 multi-forat (`client_de`, `building_type_de`, `architect_company_de`): només Bell-lloc té l'esquema; pregunta 26 abans.

## Procediments

```bash
R=/home/josep/projects/claudecode-job/clients/g3dt-prod; cd "$R"; git branch --show-current; git status --short | grep -v runs/
PYTHONPATH=$PWD .venv/bin/python -m pytest tests/test_bloc3_extractor.py tests/test_reference_extractor.py tests/test_bloc2_format_a.py -q --no-header -p no:cacheprovider
PYTHONPATH=$PWD .venv/bin/python docs/wizard-headless/mesures/refresh_eva_narrativa.py --all-keys      # dry-run: ha de dir 0 diferències
PYTHONPATH=$PWD G3DT_CACHE_DIR=/home/josep/g3dt-prod-cache .venv/bin/python docs/wizard-headless/mesures/mesura_341.py 2026-09-<dd>-m341-<motiu>
M=docs/wizard-headless/mesures/runs; for s in castellar rubi bell-lloc linyola alcoletge vilanova anciles; do diff <(grep '^- ' $M/2026-09-07-m341-bloc3b/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') <(grep '^- ' $M/2026-09-<dd>-m341-<motiu>/$s/viaA/_compare_341.txt | sed 's/ gen=.*//') | grep '^[<>]' | sed "s/^/$s: /"; done
PYTHONPATH=$PWD .venv/bin/python -m pytest tests -q --no-header -p no:cacheprovider > /tmp/suite.txt 2>&1; diff <(grep '^FAILED' /tmp/suite.txt | sed 's/ - .*//' | sort) <(grep -v '^#' docs/wizard-headless/mesures/suite-vermells-esperats.txt | sort); rm -f docs/diagnostics/ai_pipeline_demo-project_$(date +%Y%m%d).md
```

Gotcha: el hook `rtk` reescriu `grep`; `rtk proxy grep …` en pipelines. Les mesures i la suite es poden llançar alhora.

## Seqüència d'obertura

1. `git branch --show-current` (una línia al Josep), `git status`, `git log --oneline -5` (`d8c7698` docs del bloc 2 al capdamunt).
2. DECISION-LOG 2026-09-07 (tarda) i el PLA (§Bloc 3 estat, §Bloc 4).
3. Si el Josep dona GO: commit en 4. Després bloc 4 o preguntes; mesura contra `-bloc3b`.
