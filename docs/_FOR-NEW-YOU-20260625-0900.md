# FOR-NEW-YOU — 2026-06-25 (target session: 2026-06-25 ~09:00)

**Escrit:** 2026-06-25 (sessió de tarda anterior).
**Per a:** sessió nova amb context buit que ataca **A3 — extractor positiu de laboratori (GTL)**.
**Recap d'una frase:** la sessió anterior va tancar les 3 llacunes del parser Via A (commit `00c7def`) i va deixar A3 com a següent prioritat; aquest doc captura el que NO és obvi del codi de laboratori perquè comencis A3 sense re-investigar.

---

## 1. Ordre de lectura (abans de tocar codi)

1. **Aquest doc** (sencer).
2. `STATUS.md` — estat actual, open items per prioritat, què s'ha shippat.
3. `docs/ANALISI-PIPELINE-DEBUG-VACARISSES.md` **§7** (GTL report) + **§9 A3** + **§3.3** (`client_nif`/entitat) + **§10 Error 5**.
4. `docs/DECISION-LOG.md` — entrada **2026-06-24** (fix NIF de proveïdor, la meitat NEGATIVA d'A3) + **2026-06-25** (llacunes Via A).
5. `automation/lab_extractor.py` — l'extractor que JA existeix (vegeu §4 d'aquest doc).
6. MEMORY.md (auto) — context Eva, metodologia, feedbacks.

---

## 2. Què és A3 EXACTAMENT (redefinit amb dades reals — NO és el que diu §9)

§9 A3 diu "afegir un miner específic per GTL que extregui lab_testing_company". **Això està desactualitzat.** L'estat real, verificat aquesta sessió:

- **`automation/lab_extractor.py` JA EXISTEIX (402 línies) i JA està connectat** al pipeline:
  - `auto_extractor.py:1257-1259` → `extract_lab_results(project_path)`
  - `report_data.py:1365-1366` → idem
  - Consumit per `report_generator.py`, `web/wizard_service.py`, `parametrize_template.py`.
  - Ja extreu: sulfats, sample info, test type, `lab_location` (de la mostra), `lab_tests_text`, idioma CA/ES.
- **El bug real:** `lab_testing_company` i `lab_testing_description` estan **HARDCODED** a `lab_extractor.py:392-393`:
  ```python
  results.lab_field_company = 'TPS PROSPECCIÓ DEL SUBSÒL SL'
  results.lab_testing_company = 'TPS PROSPECCIÓ DEL SUBSÒL SL'
  ```
  Mai es parseja del GTL. Per això Eva diu "no identifica el laboratori" — sempre posa el mateix lab.

**A3 = substituir el hardcode pel nom + adreça REALS del laboratori del GTL report, amb el hardcode com a fallback.** No és construir res de nou.

---

## 3. Fet crític: HI HA MÚLTIPLES LABORATORIS (no es pot hardcodejar)

Capçaleres GTL reals (text PyMuPDF, pàg. 1):

| Projecte | GTL file | NIF lab | Adreça(es) al GTL |
|---|---|---|---|
| Castellar | `4687-GTL-25 Castellar del Vallés.pdf` | **B25364589** | "C/ Vallbona núm. 22 - 25268 Els Omells de Na Gaia (Lleida)" + "Polígon industrial Golparc, Avgda. Mediterrània 20, 25241 Golmés (Lleida)" |
| Rubí | `4703-GTL-25 Rubí.pdf` | **B25364589** | mateixes dues adreces |
| Vacarisses | `4849-GTL-26 Vacarisses.pdf` | **B64803075** (TPS Perforaciones, email `laboratorio@tps-perforaciones.com`) | (veure §7 ANALISI) |

→ Almenys **2-3 labs diferents**. El hardcode "TPS PROSPECCIÓ DEL SUBSÒL SL" és INCORRECTE per Castellar/Rubí. **Opció a valorar:** un petit registre `NIF → {nom, adreça}` (G3 fa servir un grapat de labs) — això és una decisió per a Eva, NO la prenguis sol. Anota-la al DECISION-LOG.

---

## 4. Per què el parsing del nom no és trivial (load-bearing)

El text del GTL està **desordenat per columnes** (PyMuPDF reordena, igual que la taula del pressupost). Exemple Castellar: després de `Nom:` apareix `B25364589` (que és el NIF, no el nom — etiquetes i valors barrejats). **No pots assumir "valor a la línia següent de l'etiqueta".**

Patrons que ja funcionen al repo per a aquest mateix problema — REUTILITZA'LS, no n'inventis de nous:
- **Parser de bloc ancorat per etiqueta** (com el parser OBRA per `site_address`): `web/vision_fast.py` → `_parse_docs_fields()` (commit `00c7def`). Llegeix en ordre, ancora a un marcador, agafa línies relatives.
- **Via B2 (visió Claude dedicada)** si el text és inservible: `web/vision_fast.py` `_run_vision_fast` Step 2b + `automation/validation/prompts.py` `PRESSUPOST_EXTRACTION_PROMPT`. Per a un GTL podries afegir `vision_type=gtl` anàleg.
- **Registre NIF→lab** (més fiable si són pocs labs): mapeja el NIF (fàcil d'extreure amb regex `B\d{8}` etc.) al nom canònic.

Recomanació personal: comença pel **registre NIF→lab** (robust, determinista, els labs són pocs) + fallback al hardcode actual. Vision només si Eva vol cobrir labs nous automàticament.

---

## 5. Invariants estructurals — NO desfacis

- `web/vision_fast.py::_parse_docs_fields()` — helper PUR (sense IO), creat `00c7def` per testabilitat. El guard de `num_planned_sondeig` talla la finestra de campanya al header `UNITATS D'ASSAIG`/`UNIDADES DE ENSAYO` **per CONTINGUT, no per distància**. Si "simplifiques" tornant a finestra fixa, re-introdueixes el fals positiu de la taula. Vegeu DECISION-LOG 2026-06-25 punt 4.
- `automation/internal_addresses.py` + `competition.py` (commit `ad3a369`, 24 juny) — blocklist de NIFs de proveïdor (G3 + lab) per excloure'ls de `client_nif`. **És la meitat NEGATIVA d'A3.** No la desfacis; A3 hi construeix a sobre (la meitat positiva). Si afegeixes un registre de labs, considera reutilitzar el mateix conjunt de NIFs de proveïdor.
- `_find_gtl_pdf()` i `_find_lab_pdf()` (lab_extractor.py) ja resolen el fitxer via `file_mapping.json` roles `gtl_report` / `lab_results_pdf`. Reutilitza, no re-implementis el descobriment.
- 3 documents de lab DIFERENTS, no els confonguis: `lab_order` (comanda laboratori `.xls`, entrada) vs `gtl_report` (informe de resultats PDF, sortida del lab) vs `lab_results_pdf` (`LAB*.pdf`).

---

## 6. Procediments operatius

- **Python amb deps (fitz, pydantic...):** `/home/josep/projects/claudecode-job/clients/g3dt/.venv/bin/python`. NO existeix `.venv` dins `g3dt-fix/`.
- **GOTCHA worktree:** executa SEMPRE des de `clients/g3dt-fix/` (cwd) perquè el codi carregat sigui el d'aquesta branca, no el del worktree `g3dt/`. Verifica amb `python -c "import web.vision_fast as v; print(v.__file__)"` → ha de contenir `g3dt-fix`.
- **Dades de projectes reals:** `/mnt/c/claude/g3dt/projectes/` (NO `reference-material/` — vegeu feedback_windows_folders). 7 projectes amb GTL: Castellar, Rubí, Bell-lloc, Alcoletge, Vilanova, Anciles (+ Vacarisses a `dades-eva/_DEBUG PRODUCCIO`).
- **Tests:** `cd clients/g3dt-fix && /home/josep/projects/claudecode-job/clients/g3dt/.venv/bin/python -m pytest -q` (timeout 10 min; triga ~3 min). **Baseline a respectar: 32 failed / 1012 passed.** Els 32 fails són preexistents (smartscan ES ground-truth, test_data_schema, test_template) — NO els toquis ni els comptis com a teus.
- **Test del parser Via A:** `pytest tests/test_docs_fields_parser.py -q` (17 tests, ràpid).

---

## 7. Seqüència d'obertura suggerida (A3)

1. `git -C clients/g3dt-fix branch --show-current` → confirma `fix/pipeline-routing`. `git log --oneline -4` → has de veure `00c7def`.
2. Llegeix `automation/lab_extractor.py` sencer (especialment `extract_lab_results` línies 296-402 i els helpers d'extracció).
3. Dump real dels 6-7 GTL: extreu el text de pàg. 1 i mira on viuen NIF/nom/adreça del lab (script ràpid amb fitz, com a §3). Confirma quants NIFs de lab diferents hi ha.
4. Decideix l'estratègia (registre NIF→lab vs parser ancorat vs vision) i **escriu un pla curt + porta'l a Josep ABANS de codar** (ell ho va demanar explícitament).
5. Pregunta pendent per Eva: "quins laboratoris fa servir G3? Mapatge NIF→nom/adreça?" — anota-ho; pot desbloquejar el registre.
6. Implementa via code-implementer → code-reviewer → code-tester (workflow del CLAUDE.md global). Afegeix tests amb fixtures de capçalera GTL reals (CA + ES; Anciles/Vilanova són ES).
7. Verifica 7/7 contra els GTL reals + suite completa (baseline inalterat) + DECISION-LOG.

---

## 8. Què NO fer

- **No** assumeixis que A3 és "construir un extractor nou" — JA existeix i està connectat (§2).
- **No** hardcodegis un altre lab; n'hi ha diversos (§3).
- **No** parsegis el nom del lab assumint ordre de línies — el text està column-scrambled (§4).
- **No** desfacis el guard per-contingut de sondeig ni la blocklist de NIFs (§5).
- **No** comptis els 32 fails preexistents com a regressió teva.
- **No** facis push ni toquis `production/g3dt-eva-v1` sense que Josep ho demani. Treballa a `fix/pipeline-routing`.
- **No** estagis els artefactes de dev del working tree (`reference-material/**/mined_images`, `file_mapping.json`, `docs/diagnostics/*`) — són sortides de runs, no codi.

---

## 9. Tasques obertes (snapshot, per prioritat — de STATUS.md)

1. **A3** — extractor positiu de GTL (AQUESTA sessió).
2. **A1** — validació de rol amb `doc_type` del concept_map (plano topogràfic = `architect_plan` → dimensions fabricades). Risc de fabricació; co-prioritat amb A3. Arrossega Error 2 (sondeig_annex rep el mateix fitxer).
3. **A4 / entity confusion** — `architect_company` etiqueta client/promotor. Requereix lògica > regex. **Consultar Eva** abans.
4. StreetView adjacents (Eva ho ha demanat).
5. **A7** — Wizard UX: nivells de sòl manuals quan no hi ha sondeig_annex.
6. Linyola: `_find_pressupost_pdf` no descobreix PDF de nom no-estàndard.

Tancades recentment: llacunes Via A (`00c7def`), A2 (descartat, no-op), A8 (superat), fix NIF (`ad3a369`).

---

## 10. Estat git en obrir

- Branca: `fix/pipeline-routing`, **ahead 3** d'`origin/production/g3dt-eva-v1` (commits 24-25 juny no pujats: `ad3a369`, `40c2d14`, `00c7def`). Josep decideix quan fer push/FF a production.
- Working tree "brut" amb artefactes de dev (mined_images esborrats, file_mapping.json, docs/diagnostics untracked) — IGNORA'LS, no són canvis de codi.
- Eva no fa pull fins ~la setmana vinent; sense pressa de deploy.
