# FOR NEW YOU — 2026-09-01 (2000) — Code review de la branca sencera: 21 bugs reparats en 3 voltes, APPROVE, i dues coses grosses per decidir

**Escrit:** 2026-09-01, nit · **Per a:** la sessió nova que el Josep obre expressament per analitzar l'assumpte
adreça/municipi amb tota la finestra de context disponible.

**Recap:** la sessió va començar per una pregunta del Josep («quin nivell de qualitat entregarem, i com ho podem
avaluar abans que l'Eva ens deixi fer el merge?»). Va derivar en (a) un hold-out headless de 2 projectes, aturat a
mitges a posta, i (b) un `/code-review` de la branca sencera que va destapar 30 troballes confirmades, reparades en
**tres voltes** amb revisió creuada entre cadascuna. Veredicte final: **APPROVE**. La mesura de qualitat **encara
no s'ha fet**: el Josep la va posposar deliberadament fins després de l'anàlisi d'adreces.

## 1. Ordre de lectura

1. Aquest document.
2. **`docs/PROPOSTA-JOSEP-ADRECES-I-MUNICIPI-2026-09-01.md`** — la feina d'aquesta sessió. Comentari literal del
   Josep, seqüència pactada (§1b), preguntes obertes (§3), restriccions (§4). **És el primer que has de fer.**
3. **`docs/audit/CODE-REVIEW-BRANCA-2026-09-01.md`** — l'inventari sencer: 15 troballes originals + les 3 voltes de
   correcció + els 3 veredictes de revisió + **17 punts de deute conegut**. Llarg però és la font de veritat.
4. `STATUS.md` — punt obert `0e` (la proposta d'adreces) i la capçalera.
5. `MEMORY.md`: `project_address_interpretation_llm_proposal` (nova avui),
   `feedback_no_pull_eva_success_criterion`, `feedback_full_report_341_variables_goal`,
   `reference_cadastre_not_truth_for_parcel_area`, `feedback_measure_baseline_before_coding`.
6. `docs/_FOR-NEW-YOU-20260901.md` (el d'ahir) — §4 té els supòsits estructurals anteriors, tots encara vigents.

## 2. Estat del repositori — LLEGEIX AIXÒ ABANS DE TOCAR GIT

**Hi ha 29 fitxers de codi modificats i sense commitejar** (24 modificats + 5 tests nous). És **tota la feina del
dia**: 21 bugs reparats, ~135 tests nous. HEAD segueix sent `fca71b9` (el handoff d'ahir).

- El working tree sobreviu un `/clear` sense problema; no hi ha urgència, però **no facis cap
  `git checkout`/`restore`/`reset`/`clean` sense mirar-t'ho abans**.
- **Mai `git stash`** — la pila és compartida amb els altres worktrees.
- Si el Josep vol commitejar, agrupa per tema (lectura / infraestructura / UI-servei), no un commit únic.

Fitxers tocats: `automation/{auto_result_cache,g3_templates,sync_workspace}.py`,
`automation/concept_scout/scanner.py`, `automation/fileminer/__init__.py`,
`automation/lectura/{cadastre_reader,consolidate,normalize,notify,runner,tables_report}.py`,
`web/{api,lectura_service,wizard_service}.py`, `templates/validation/review.html`, + tests.

## 3. Regles interpretatives (no evidents llegint el codi)

- **La suite dona `1897 passed / 32 failed` amb `.venv/bin/python -m pytest tests/ -q`.** Els 32 són el baseline
  conegut i **preexistent** (ai_pipeline ranking/trace, smartscan, fileminer Anciles, groq_miner cache,
  bearing_stratum): fallen perquè falten fitxers de `reference-material/` al worktree, **no pel diff**. El baseline
  a HEAD era 1640/32, amb **exactament els mateixos noms de test**.
- **Corre pytest des de `tests/`, no des de l'arrel.** Des de l'arrel surten **13 errors extra de recol·lecció**
  (`automation/test_data_schema.py`, `templates/test_template.py`) que no són tests reals. Dos agents van reportar
  recomptes diferents per això.
- **`tests/test_lectura_runner.py::test_telemetry_has_cli_metrics_when_json` i `::test_telemetry_without_json_is_marked`
  són inestables sota càrrega** (fan subprocessos amb `G3DT_LECTURA_TIMEOUT=2`, fixture preexistent). Sota agents en
  paral·lel poden fer pujar el recompte a 34. **No són una regressió.**
- **El 0-diff és la prova de no-regressió d'aquest projecte, i té una metodologia concreta:** exportar HEAD a un
  arbre ombra (`git archive HEAD automation web schemas templates`), re-consolidar els **9 corpus reals** amb
  `G3DT_LECTURA_HTTP_SOURCES=""` (determinisme) i comparar ignorant `generated` i `consolidation.elapsed_s`. Els 9
  corpus són `docs/wizard-headless/mesures/runs/2026-09-01-holdout-v16/{castellar,linyola}/` i els 7 `*/perdoc` de
  `docs/wizard-headless/mesures/runs/`.
- **`build_report_tables` byte-idèntic és el criteri que mana**, no el `_decisions.json`. Avui Linyola té 12
  diferències al `_decisions.json` (renom `valor`→`value`) i **0 al `.docx`**. La segona és la que compta.
- **Els 9 corpus NO exerciten el camí «components en forma de dict sense total».** Per això quatre errors latents
  hi han sobreviscut i el 0-diff no els veu. Si toques superfície construïda, prova sintèticament.

## 4. Supòsits estructurals nous d'avui (no els desfacis)

| On | Què | Per què |
|---|---|---|
| `normalize.py` `COMPONENT_VALUE_KEY`/`COMPONENT_VALUE_ALIASES` | **única** llista d'àlies del projecte; clau canònica `"value"` | Hi havia dues llistes ad-hoc divergents (`consolidate` i `tables_report`) que van causar un bug real. Si en tornes a crear una de local, reobres la classe sencera |
| `normalize.py`, **no** `contract.py` | on viu la normalització de components | `contract.adapt_legacy` **NO és porta de producció**: només l'invoca `docs/wizard-headless/fase0-acceptacio/compare_consolida.py` per llegir fixtures d'or. Verificat. Posar-hi normalització no arregla res a producció |
| `consolidate.py` `load_corpus` | l'escapatòria d'orfes compara contra `inv_known` (**totes** les rutes, quarantena inclosa), no contra `inv_paths` | Comparar contra les vives feia que «tots els documents són a `_esborrats/`» desactivés el filtre i un document mort decidís un camp **a `segur`** |
| `sync_workspace.py` | amb escaneig incomplet **o** desaparició massiva i `check_only=False`: `status="ok"`, `deleted=[]`, avís | La protecció és **no apartar en massa**, no bloquejar. Abans tornava error → `web/api.py` feia 404 → l'Eva no podia ni començar |
| `sync_workspace.py` | amb `check_only=True` segueix sent `"error"` | Allà no bloqueja ningú i val més callar que dir «cap canvi» amb un escaneig coix |
| `sync_workspace.py` `_write_marker` | atòmic, i el camí de salt **mai** l'escriu | Si el marcador falta, el destí és incomplet. «Reparar-lo» convertia una còpia morta al 40 % en un workspace vàlid per sempre |
| `cadastre_reader.py` `_STREET_PREFIX_RE` | formes llargues abans que curtes + lookahead `(?![^\W\d_])` | `av\.?` casava dins d'«Avda» (`'da. Catalunya'`) i `cam[ií]` dins de «Camino» (`'no Viejo'`). Amb el lookahead, una abreviatura desconeguda **degrada a deixar l'adreça intacta** en comptes de donar mig nom |
| `cadastre_reader.py` | tall carrer/portals per la **primera** coma amb cua de portals (no l'última) | L'última trencava `"Carrer Arbrells, 18A, 18B i 20"` |
| `wizard_service.py` | `_build_lectura_block` escriu `lectura_selections` **sempre**, encara que buida | `wizard.py:1049` fa `existing.update(extra)`: si no s'escriu, la clau morta sobreviu i el `user_data` queda contradictori |
| `wizard_service.py` | tota tria es valida contra el `_decisions.json` **actual**; text lliure només amb marca `_lliure` | Les claus són posicionals: si canvia l'ordre de files, una tria vella aterra sobre una altra fila |
| `review.html` | `lecturaState.selections` porta marca de projecte, i `wizardUserDataProject` | Dues portes de fuita entre projectes, totes dues **reproduïdes**, no hipotètiques |
| `review.html` | `_jobsRenderSyncWarnings` **sempre després** d'`onProjectChange` | `onProjectChange` neteja els avisos; si es crida abans, se l'endú. Cobert amb test de mutació |

Els supòsits d'ahir (`_FOR-NEW-YOU-20260901.md` §4) segueixen **tots** dempeus: verificat pel revisor final.

## 5. Procediments

```bash
git branch --show-current      # experiment/nivell-a-2026-08 (in-place a g3dt-prod/)
git status --porcelain | wc -l # ha de mostrar la feina sense commitejar (§2)

.venv/bin/python -m pytest tests/ -q          # ~3 min → 1897 passed / 32 failed. SEMPRE tests/, mai l'arrel
.venv/bin/python -m pytest tests/test_lectura_consolidate.py -q   # dirigits, molt més ràpid

# Hold-out headless (el driver d'avui, reutilitzable):
#   /tmp/claude-*/scratchpad/holdout_driver.py  — es perd amb el scratchpad; el patró és
#   automation.lectura.runner.run_lectura(project_path, out_dir=..., on_event=...)
#   amb la config de producció (sonnet, xhigh, concurrència 2, preext, consolida auto).
```

- `.venv/bin/python` sempre; `cd` al worktree `g3dt-prod/` (el cwd fa ombra al `PYTHONPATH`).
- `rtk` reescriu `grep`/`find`: `find … -not` i `\|` fallen → `rtk proxy find …`.
- Cache real d'aquest entorn: `/home/josep/g3dt-prod-cache/` (`G3DT_CACHE_DIR`), **no** `~/.g3dt/cache/`.
- **Workspaces E2E: `~/g3dt-e2e/projectes/` té ara els 8 projectes** (abans només 2). Còpies netes, sense
  `validation/` ni `*_generated*`. Origen: `/mnt/c/claude/g3dt/projectes{,-debug}/` (Tulipa és a `-debug`).
  **`reference-material/` d'aquest worktree NO té els fitxers reals**, només artefactes `validation/`.
- **Mai escriure a `/mnt/c`.**

## 6. Seqüència d'obertura suggerida

1. `git branch --show-current` + `git status --porcelain | wc -l` → confirma la feina sense commitejar del §2. Si
   el working tree és net, **atura't**: algú ha commitejat o descartat, i has de saber què.
2. `.venv/bin/python -m pytest tests/ -q` → confirma `1897 passed / 32 failed`. Si no quadra, esbrina per què
   abans de res (recorda els 2 inestables i el `tests/` vs arrel).
3. Llegeix `docs/PROPOSTA-JOSEP-ADRECES-I-MUNICIPI-2026-09-01.md` **sencer**, i el §4 (restriccions) dues vegades.
4. Llegeix el deute conegut de `docs/audit/CODE-REVIEW-BRANCA-2026-09-01.md` (secció «Cinquena passada»). Els 4
   punts de P1 són ERR latents i el Josep els va ajornar **a posta** a aquesta sessió.
5. **Fes l'anàlisi d'adreces amb el Josep abans de tocar codi.** És el motiu pel qual la sessió existeix.
6. Només després: els 4 ERR latents de P1, i llavors la mesura de qualitat dels 8 projectes.

## 7. Què NO fer

- **No mesuris abans de l'anàlisi d'adreces.** Decisió explícita del Josep: si aquella capa canvia
  d'arquitectura, els números caducarien de seguida.
- **No implementis res de la proposta d'adreces sense la conversa.** El document ho diu a la capçalera.
- **No tornis a crear una llista local de noms de camp** per llegir components (vegeu §4).
- **No posis normalització a `contract.py`** pensant que és la porta de producció. No ho és.
- **No et refiïs d'un comentari que promet una protecció**: avui n'hem trobat un que menteix (§8, punt 1).
- **No proposis mai pull/merge/desplegament a l'Eva** (`feedback_no_pull_eva_success_criterion`).
- **No `git stash`**; **no `--model opus`** pensant que és Opus 4.8 (cal `claude-opus-4-8`).
- **No comptis les fallades d'una sola passada de la suite** sense mirar si són els 2 inestables.

## 8. Tasques obertes, per ordre

1. **Anàlisi adreça/municipi amb el Josep** (`docs/PROPOSTA-JOSEP-ADRECES-I-MUNICIPI-2026-09-01.md`). Inclou-hi,
   com a tema germà, la pregunta A1 del Josep sobre **restringir la sortida del LLM** (prompt amb exemples vs API
   amb sortida estructurada). Nota per a la conversa: `claude -p` **no** dona temperatura ni descodificació
   restringida; tenir-ho significaria llegir per API i **desfer la decisió de la via A** del 23 d'agost.
2. **Els 4 ERR latents de P1** (audit doc, secció «Cinquena passada»): camí DICT de `_component_number` sense
   ancoratge, les «bessones» que no ho són (parèntesis), `_has_figure` acceptant qualsevol dígit,
   `_anchored_key` triant l'etiqueta. ~3 línies cadascun, tots al codi estrenat avui.
3. **El comentari que menteix** (`web/wizard_service.py`): diu que `ConceptScout`/`Deep folder classify` vetan la
   cache degradada, i **no ho fan** — `_layer2_vision` i `concept_scout/vision_probe.py` s'empassen l'excepció, així
   que `steps_skipped` arriba buit. Corregir el comentari com a mínim; la correcció bona toca **via B** i demana
   decisió del Josep.
4. **Aprenentatge d'àlies — decisió de disseny pendent** (pregunta del Josep). El rastre de claus noves va a
   `notes_estructurals` **per projecte** i ningú l'agrega. Falten (a) un agregador sobre tots els projectes i (b)
   fer-lo viatjar pel correu de telemetria, l'únic canal des de la màquina de l'Eva. **L'adopció d'un àlies ha de
   seguir sent un canvi de codi revisat**, mai automàtica.
5. **La mesura de qualitat** (el que va originar la sessió): hold-out headless dels 8 projectes sobre codi reparat
   → % camps bé / % popup / % blanc / **erronis-amb-confiança (objectiu 0)** / minuts per projecte, i
   `compare_tables_vs_eva` contra els informes signats. Llindars pactats: OK ≥ 80 %, candidats ≤ 20 %, ERR = 0.
6. La resta del deute P3 (17 punts a l'audit doc), Fase 16 i 17.

## 9. Guanys que no sortiran a cap mètrica

- **La lliçó més cara del dia: dos "fixos" no funcionaven.** El veto de cache per a `ConceptScout` es va escriure
  a la 2a volta i el de `Deep folder classify` a la 3a; **cap dels dos fa res**, perquè els passos de visió
  s'empassen l'excepció aigües amunt. Ho va descobrir el revisor final executant-ho, no llegint-ho. Moral: en aquest
  projecte, **verificar que un fix dispara** és una passa separada de verificar que el codi és correcte.
- **Els temps reals de lectura, mesurats per primera vegada de punta a punta amb la config de producció:**
  Castellar **27,8 min** (13 documents, 0 conflictes de consolidació), Linyola **41,9 min** (20 documents + 1
  duplicat saltat, 1 conflicte resolt per LLM). Són del codi **anterior** a les correccions; serveixen de línia base.
- **Tres agents implementadors van rebutjar instruccions meves i tenien raó els tres:** un va demostrar que
  `contract.py` no era la porta de producció; un altre va rebutjar una regla de tall d'adreces que hauria creat un
  portal fals; un tercer va demostrar que vetar la cache per qualsevol `steps_skipped` l'hauria desactivada de facto
  (barreja fallades transitòries amb condicions estructurals normals). **Els briefs es discuteixen, no s'obeeixen.**
- **La quarantena `_esborrats/` no l'excloïa cap dels 4 escàners.** Vol dir que qualsevol document substituït —cosa
  normal a la vida d'un projecte, no un accident— es tornava a llegir i competia amb el viu. Estava viu des que
  existeix el delta-sync.
- **El bug de l'`Avda.` és més gran del que sembla:** el mateix patró («abreviatura curta que és prefix d'una de
  llarga») afectava també `Camino`. Amb el lookahead nou, els tipus de via que **no** tenim llistats degraden a no
  tocar res, en comptes de retallar el nom. Això canvia el perfil de risc de tot el lector d'adreces, i és context
  directe per a l'anàlisi de la tasca 1.
