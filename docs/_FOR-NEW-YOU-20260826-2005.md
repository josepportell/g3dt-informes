# FOR NEW YOU — 2026-08-26 (2005) — Sessió de fixos segons `PLA-PENDENTS-0B-0C-0D` (F→A→B→C→D→E→docs), i **després** la Fase 16

**Escrit:** 2026-08-26, 20:05 · **Per a:** la sessió d'implementació (Sonnet) que executa el pla, i a continuació la Fase 16.
**Recap:** sessió d'anàlisi (Fable) sobre els pendents 0b/0c/0d + forat `value_key`. Cap línia de codi tocada. Tot mesurat
de nou: el diagnòstic del Cadastre era **equivocat** (la parcel·la era correcta; l'Eva suma tres portals), els dos residus
Groq tenen causa coneguda, la CAUTELA de Castellar és un artefacte del dialecte de l'or, l'ALERTA de Bell-lloc és l'or
mesurant píxels contra la taula de l'annex, i el comparador té dos forats que amagaven tot això. Commit del pla: `9e23ebc`.
**Ordre nou (Josep, 2026-08-26 vespre):** primer els fixos del pla, **després** la Fase 16 (abans era al revés).

## 1. Ordre de lectura (15 min)

1. Aquest document.
2. **`docs/PLA-PENDENTS-0B-0C-0D-2026-08-26.md`** — és l'especificació sencera: 6 fixos + docs, amb fitxer:línia, tests,
   fixtures, mesures i criteris d'acceptació. Aquest handoff no el repeteix; li afegeix el que el pla dona per sabut.
3. `STATUS.md`: capçalera i open items 0b/0c/0d (L.253-296) — són el text que hauràs de reescriure al final.
4. `docs/_FOR-NEW-YOU-20260826-1900.md` — §2-§5 (marc, regles interpretatives, supòsits que aguanten pes, procediments) segueixen
   vigents; la Fase 16 hi és descrita a §8.1. No cal rellegir la resta.
5. `docs/DECISION-LOG.md`, entrada «2026-08-26 (vespre i nit)»: format de les 9 seccions que hauràs de reproduir.
6. `docs/PREGUNTES-EVA-PENDENTS.md` (8 obertes; hauràs d'anotar la 3 i la 8 i afegir la 9 i la 10).
7. MEMORY.md: `reference_cadastre_not_truth_for_parcel_area` (**corregida avui**: el Cadastre sí que és la font),
   `project_groq_residues_wrong_values_pending`, `project_soil_levels_gold_vs_pas3b_open` (totes dues actualitzades avui),
   `feedback_measure_baseline_before_coding`, `feedback_no_pull_eva_success_criterion`, `reference_compare_tables_vs_eva_harness`.

## 2. Marc fixat (no re-litigar)

- Via B **prohibit editar-la**: `automation/ai_pipeline/`, `web/vision_groq.py`, `automation/auto_extractor.py`, `web/vision_fast.py`,
  i, per aquest pla, també `geocode_coordinates.py`, `parcel_resolver.py`, `cadastre_adjacents.py` (importar sí, modificar no).
- Els tres residus Groq **es mantenen** (probes, `groq_miner`, ortofoto): només es filtren valors a l'origen.
- **Mai** proposar pull/merge/desplegament a l'Eva. Objectiu: l'informe complet (341 variables); el nivell A és el pas 1.
- L'or (`docs/golden-read*`, `_eva_truth/`) no s'edita per fer quadrar mesures. Els dos únics canvis a l'or del pla (§5 i §7.8)
  són de dialecte / anotació de font externa i **no canvien cap valor que l'or afirmi**.
- Cap crida LLM sense preguntar al Josep. Només §8.4 del pla en necessita una (un sol document, el tall de Bell-lloc).
- Decisions que són del Josep, no teves (pla §11): encendre `cadastre` per defecte; `de` de la cobertura `segur` vs `candidats`;
  autoritzar la crida LLM; enviar preguntes a l'Eva.

## 3. Regles interpretatives (què és senyal i què és soroll)

- **Harness = `docs/wizard-headless/fase12-consolida/harness.py`** (5 jocs: 4 de Castellar + Bell-lloc; 0 crides LLM; 0,03 s).
  Compara **verdictes**, no text: `diff <(grep -vE '^===== ' A) <(grep -vE '^===== ' B)`. La línia de temps canvia sempre.
- Línia base a `HEAD 9e23ebc` (mesurada avui): `sonnet-v2-c3` → `ESCALARS {NOU 1, OK 14, CAUTELA 7}`, `TAULES {OK 25, CAUTELA 4}`,
  0 ALERTA. Les 4 CAUTELA de taules: `soil_levels[0].a`, `[0].de`, `[1].a`, `spt_ma_tests[0].n30`. **Els altres 4 jocs no els tinc
  apuntats: apunta'ls tu abans de tocar res** (pla §10).
- Després del Fix F (comparador v3) **la línia base canvia de forma legítima**: línies `ALERTA … or=X prod=no_trobat` passen a
  `BUIT` (rang 1) i parells `candidats/candidats` sense cap valor comú apareixen com a `CAUTELA candidats disjunts`. Cap `OK` pot
  passar a `ALERTA`. Aquesta segona línia base és la que mesura D i E.
- `ALERTA` amb «prod puja a segur» = erroni-amb-confiança (intolerable). `CAUTELA`/`BUIT` = tolerats, documentables.
- Verdictes esperats per fix (pla §4-§8): A → 0 canvis; B → `sonnet-v2-c3` `TAULES {OK 26, CAUTELA 3}` (i el mateix flip als altres
  3 jocs de Castellar); C → 0 canvis (el harness no passa per Groq); D amb `G3DT_LECTURA_HTTP_SOURCES` sense definir → 0 canvis,
  amb `…,cadastre` → Castellar `superficie_parcela`/`referencia_catastral` `FORA`, Bell-lloc intacte; E → només `belloc-ws-v12` i
  només després de la re-lectura del tall: `[0].de` OK, `[0].a` `BUIT`, `[1].de` `CAUTELA disjunts`, `[1].a` OK, **cap ALERTA**.
- Si un fix mou un verdicte que el pla no preveu, **atura't i mira quin valor l'ha causat** abans de continuar. No «arreglis» el
  verdicte tocant l'or.
- Suite: `1569 passed / 32 failed` són els 32 coneguts (SmartScan, ai_pipeline, fileminer Anciles). Un 33è és teu.

## 4. Supòsits que aguanten el pes (no desfer) — a més dels del handoff 1900 §4

| On | Què | Per què |
|---|---|---|
| `consolidate.py:1611-1616` (porta dels forats) | `http_field_signals` i el futur `cadastre_portal_signals` només es criden si **cap document** diu res del camp | «cap font Python pot guanyar contra la lectura» en forma estructural; no afinar confiances |
| `consolidate.py:1291-1295` | la base de l'últim nivell sempre baixa a `candidats` | és la regla del Pas 3b; l'or de Castellar hi coincideix (`≥ -1,20`), el que falla és el dialecte de l'or, no la regla |
| `consolidate._COVER_RE` + `_group_soil_levels` bloc posicional (L.1505-1512) | la fila que arrenca a la superfície va a la cobertura **només** si la cobertura no té fondàries de ningú | sense això Castellar tenia una fila espúria; E2 del pla ha de conviure-hi (a Castellar la cobertura **té** `a` = −0,50 → E2 no dispara) |
| `web/lectura_service._apply_lectura_overlay` | `no_trobat` **mai** escriu ni esborra (regla 4 del contracte) | el pla rebutja explícitament «`no_trobat` esborra la via B» (§6.3) |
| `geocode_coordinates._pick_nearest_rc_from_numerero` (L.967) | treu la lletra del portal i tria el més proper | per això el lector nou **no** pot delegar-hi el portal: filtra `(pnp, plp)` exactes al JSON de DNPLOC |
| `normalize.wrap_flat_cells` (L.150-151) | una cel·la dict amb `estat` dins d'una fila de cadenes planes es respecta | és el que permet el Fix B (una sola cel·la en dialecte v2 dins d'una fila antiga) |
| `contract.validate_decisions` | tolera claus extra a les cel·les (`rule`, `note`, `sources_checked`) | el Fix F hi afegeix `fora_carpeta`; comprova-ho amb `test_golden_scalar_fixture_adapts_clean` abans de confiar-hi |

## 5. Procediments

```bash
git branch --show-current      # experiment/nivell-a-2026-08 (in-place a g3dt-prod/; esperat)
git log --oneline -1           # 9e23ebc o el commit d'aquest handoff

.venv/bin/python -m pytest tests/ -q -p no:cacheprovider                       # ~3 min → 1569 passed / 32 failed
.venv/bin/python docs/wizard-headless/fase12-consolida/harness.py --write /tmp/claude-…/scratchpad/base   # 5 jocs
.venv/bin/python docs/wizard-headless/fase12-consolida/harness.py --only sonnet-v2-c3 --write …/after
G3DT_LECTURA_HTTP_SOURCES=icgc,geocodificacio,cadastre .venv/bin/python docs/wizard-headless/fase12-consolida/harness.py   # Fix D, 2a passada

# reproduir el forat A abans de tocar-lo
.venv/bin/python -c "from automation.lectura.consolidate import value_key as v; print(v('1.5-1.75'), v('0,50-1,20'), v('0,50 - 1,20'))"

# API del Cadastre en viu (per fer fixtures del Fix D; respostes petites, desa-les a tests/fixtures/cadastre_castellar/)
curl -s 'https://ovc.catastro.meh.es/OVCServWeb/OVCWcfCallejero/COVCCallejero.svc/json/Consulta_DNPLOC?Provincia=BARCELONA&Municipio=CASTELLAR%20DEL%20VALLES&Sigla=CL&Calle=ARBRELLS%20DELS&Numero=18'
curl -s 'https://ovc.catastro.meh.es/INSPIRE/wfsCP.aspx?service=wfs&version=2.0.0&request=GetFeature&STOREDQUERIE_ID=GetParcel&refcat=3298012DG2039N&srsname=EPSG::25831'
```

- Les respostes WFS de Castellar (parcel·les 08-14 del bloc 32980) i de Bell-lloc (171-174 CG1141S) d'avui són a l'scratchpad
  de la sessió d'anàlisi (`/tmp/claude-1000/…/7f90f120…/scratchpad/wfs_*.xml`); si encara hi són, estalvien crides. Si no,
  torna-les a baixar (2,6 KB cadascuna).
- Cache del lector nou: `config.cache_dir("cadastre_portals")` (respecta `G3DT_CACHE_DIR`); TTL 90 dies. Sense xarxa → `[]`.
- `rtk` reescriu `grep`/`find`: `find … -not` i `\|` fallen → `rtk proxy find …`; el `grep -o` amb patrons llargs sobre JSON
  grans dona «exceeds complexity limits» → usa Python. `unzip` no existeix.
- `.venv/bin/python` sempre; `cd` al worktree `g3dt-prod/` (el cwd fa ombra al `PYTHONPATH`).
- Workspace E2E: `~/g3dt-e2e/projectes/` (Castellar i Bell-lloc). **Mai** escriure a `/mnt/c`. El perdoc de `belloc-ws-v12` és
  `~/g3dt-e2e/projectes/4001612 BELL-LLOC/validation/lectura/`: el JSON nou del tall (Fix E) va **allà**, amb còpia prèvia.

## 6. Seqüència d'obertura suggerida

1. `git branch --show-current` + `git log --oneline -3` + suite → 1569/32. Si no, atura't.
2. Harness `--write base` dels 5 jocs i **apunta els comptes** al principi d'una secció nova «Comparador v3» a
   `docs/wizard-headless/fase12-consolida/_RESULTATS.md` (és la línia base pre-F que el pla no té).
3. Llegeix el pla sencer una vegada. Després, un fix per commit en l'ordre F → A → B → C → D → E, mesurant abans/després de cadascun.
4. Abans del Fix D, obre `geocode_coordinates.cadastre_progressive_lookup` (L.1006-1250) per veure què retorna (nom oficial de via
   i municipi?) i decideix si el lector el reutilitza o crida `_consulta_municipio` + `_consulta_via` directament (pla §7.2 pas 2).
5. Abans del Fix E §8.4 (la crida LLM), **pregunta al Josep** model i autorització; deixa el JSON del tall antic a l'scratchpad.
6. Docs (commit 7) + STATUS + preguntes 9 i 10 + memòria. Handoff nou amb `/for-new-you`.
7. **Només llavors, Fase 16** (handoff 1900 §8.1 i §6.5-6.7): muntar l'escenari abans de cridar cap LLM (dues carpetes locals: una
   fa de xarxa, l'altra de workspace; Castellar sol; `validation/lectura/` esborrat per al run fred), decidir model amb el Josep,
   córrer, mesurar, omplir la taula de §2 del disseny de l'annex, addendum a `fase14-15-botons-avisos/_RESULTATS.md`. **No
   optimitzar res durant la 16.** Acceptació §11: 3a ≤ 1 min, 3b ≤ 10 min amb 2 documents canviats. Nota: el Fix D amb el
   commutador ON afegeix crides HTTP a la consolidació (primera vegada; després cache) — si mesures la 16 amb `cadastre`
   activat, digues-ho a la taula.

## 7. Què NO fer

- No tocar la via B (llista a §2), ni per «una línia».
- No fer que un `no_trobat` esborri valors de la via B (pla §6.3); no desactivar probes/groq/ortofoto.
- No delegar el portal amb lletra a `callejero_address_to_rc` (18B → 18A). No calcular l'àrea amb shapely: `areaValue` oficial
  (441+423+420 = 1284 exacte; shapely dona 440,76…).
- No sumar parcel·les no contigües ni portals ambigus (número sense lletra quan el Cadastre en té amb lletra).
- No editar l'or de Bell-lloc (les cel·les gràfiques −0,30/−0,4 són lectura humana legítima; el comparador v3 les deixa visibles).
- No escriure a mà la fila de cobertura al JSON del tall de Bell-lloc: la prova del skill v1.6 és el skill (una crida, preguntant).
- No canviar `_HTTP_SOURCES_DEFAULT` (encendre el Cadastre és decisió del Josep); el commit del Fix D surt amb el defecte apagat.
- No `pkill -f` amb patró que aparegui a la teva comanda; no `git stash`; no `--model opus` pensant que és 4.8.
- No commitejar `docs/diagnostics/ai_pipeline_demo-*.md`, `photo_selection.json` ni `.playwright-mcp/` (ja apareixen com a `??`).

## 8. Tasques obertes

1. **Fixos del pla** (aquesta sessió), 7 commits: F comparador v3 · A `value_key` · B or Castellar `[1].a` · C Groq (lab_company
   fora; cap `superficie_*` des de `map`/`site_photo`, als dos punts de tall + prompt) · D `automation/lectura/cadastre_reader.py`
   + cablejat + `fora_carpeta` a l'or de Castellar · E skill v1.6 + regles E2/E2b + re-lectura del tall · docs.
2. **Fase 16** — E2E dels tres botons amb temps remesurats (handoff 1900 §8.1).
3. **Decisions del Josep** (pla §11): Cadastre ON per defecte (recomanat sí); E2b segur/candidats; crida LLM §8.4; preguntes 3+8
   juntes + 9 (Vilanova 100/406 intercanviades?) + 10 (Castellar: suma de portals és el criteri habitual?).
4. Pendents que aquest pla NO tanca: Rubí («Carrer de la Miranda, 39») no es resol al Callejero → blanc honest; matcher de portals
   de la via B (O7 del diagnòstic, Tulipa 3→11) segueix obert a la via B; variant `via_b_no_confirmat` (badge àmbar) per a quan
   es toqui `review.html`; pregunta 3 de l'Eva (el `de` del nivell 1 queda `candidats` a Bell-lloc fins llavors).
5. Fase 17 (Windows presencial) i la resta del handoff 1900 §8.3-8.5, sense canvis.

## 9. Guanys ocults de la sessió d'anàlisi

- **El Cadastre estava «apagat per una diagnosi equivocada».** Sense mesurar la unió de parcel·les amb els punts d'assaig, el
  «441 vs 1.284» semblava un error de font; era un projecte de tres portals. Amb l'adreça de la lectura, 6/7 projectes coincideixen
  amb l'Eva (annex A del pla). La fila de l'informe es diu «segons plànols cadastrals»: és *la* font.
- **Vilanova:** l'informe signat diu parcel·la 100 / construïda 406 i el Cadastre diu parcel·la 406. Probable intercanvi de cel·les
  a la referència, no un error nostre: entra com a pregunta 9 i com a advertència per a qualsevol comparació amb `_eva_truth`.
- **El comparador donava OK a `candidats` vs `candidats` sense mirar valors.** Qualsevol mesura anterior de taules amb parells
  `candidats/candidats` pot amagar desacords de valor; el Fix F els farà visibles com a `CAUTELA disjunts`. No és una regressió:
  és informació que abans no es veia.
- **`Lab. Valdemoro` no arribava a l'informe** per pura sort estructural (no és camp del wizard). El Fix C ho fa explícit.
- **Bell-lloc no és un forat de lectura de gràfics**: l'annex de l'Eva diu 0,00–1,80 a la taula; separar-hi la cobertura és una
  decisió seva (pregunta 3). La via mínima deixa la incertesa on és sense inventar cap xifra.
